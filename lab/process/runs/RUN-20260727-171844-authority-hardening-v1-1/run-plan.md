---
run_id: RUN-20260727-171844-authority-hardening-v1-1
status: completed
completed_at: 2026-07-27T17:31:19-05:00
repository: dynamic-unity
authority: "Joe direct chat: one-swing implementation of the six-persona Triple Diamond plan, with no dangling leftovers"
starting_revision: ce313532fa19
work_id: DU-AUTHORITY-HARDENING-V1-1
claim_grade: "REPOSITORY GOVERNANCE AND AGENT-USABILITY ONLY / NO SCIENTIFIC PROMOTION"
lanes:
  - lane_A
channels:
  - CH-SYN
  - CH-FORMAL
external_action_authorization: "Repository-local edits, validation, explicit-path commit, and non-force GitHub push only; no publication, submission, hardware, provider, contact, or other external action."
---

# Dynamic Unity authority hardening v1.1

## Objective

Finish the bounded Triple Diamond refinement in one swing:

1. make `CURRENT-RESEARCH.yaml` genuinely singular by removing current IDs
   from validator source;
2. add a structural schema and normalized machine fields;
3. make the active action directly executable from a compact packet;
4. restore stable-charter consistency assurance;
5. distinguish the legacy and governed run homes without relocating history;
6. prove the result with mutated-state and cold-start controls.

## Triple Diamond convergence

### Diamond 1 — defined problems

- duplicated current authority in hard-coded validation constants;
- an action pointer into a long scaffold without a compact execution packet;
- stable charter copies without parity assurance;
- untyped dependency target namespaces and free-text-only grades;
- two run homes without explicit legacy/current semantics.

### Diamond 2 — selected bounded architecture

- one JSON Schema plus one semantic validator;
- one `programs` collection retained;
- action packet embedded in the active program, not a second live file;
- structured charter remains in `LANES.yaml`;
- root `runs/` receives a legacy pointer only; no receipt relocation;
- historical evidence remains in place.

### Diamond 3 — assurance

- mutate program, action and publication IDs in memory and prove the same
  validator accepts the structurally equivalent state;
- validate schema, semantic invariants, charter parity, typed dependencies,
  action completeness, locators, references, WIP, historical guards, and
  cold-start budget;
- perform one context-isolated recovery assay from only the required
  cold-start bundle;
- preserve the scientific portfolio byte-for-meaning: same active program,
  action, paper candidate, parked branches, grades and dependencies.

## Writable surfaces

- `CURRENT-RESEARCH.yaml`
- `CURRENT-RESEARCH.schema.json`
- `LANES.yaml`
- `AGENTS.md`
- `START-HERE.md`
- `runs/README.md`
- `tests/du_agent_orientation_contract_probe.py`
- `tests/artifacts/du_agent_orientation_contract_result.json`
- `tests/README.md`
- this run plan and its closeout receipt

## Overbuild stops

- no new registry, repository, dashboard, workflow or state service;
- no second live action file;
- no split into separate science/publication registries;
- no historical relocation or mass path churn;
- no schema fields that merely encode scientific prose;
- no change to current priority, scientific grade, claim, prediction, paper
  readiness, hardware, or external state.

## Acceptance

- current IDs occur in `CURRENT-RESEARCH.yaml`, dated evidence, and generated
  artifacts only—not validator constants or stable routing surfaces;
- a structurally equivalent in-memory ID mutation passes validation;
- the active execution packet supplies exact question, inputs, ledger,
  evidence anchors, return classes, grade ceiling, kill, stop and output;
- the charter copies match the structured charter;
- `runs/` is explicitly legacy and `lab/process/runs/` explicitly governed;
- all tests, links, YAML/JSON parsing, CapacityOS architecture validation,
  diff checks, commit, push and finish pass in this run.

## Closeout

Completed without changing scientific priority, claims, predictions, paper
custody, hardware posture, or external state. See `run-receipt.md` in this
directory for the exact result and validation record.
