---
run_id: RUN-20260727-171844-authority-hardening-v1-1
status: completed
completed_at: 2026-07-27T17:31:19-05:00
repository: dynamic-unity
work_id: DU-AUTHORITY-HARDENING-V1-1
claim_grade: "REPOSITORY GOVERNANCE AND AGENT-USABILITY ONLY / NO SCIENTIFIC PROMOTION"
---

# Dynamic Unity authority hardening v1.1 — receipt

## Result

The repository now has one machine-checkable live authority and no parallel
action surface:

- `CURRENT-RESEARCH.yaml` is version 1.1 and is governed by
  `CURRENT-RESEARCH.schema.json`.
- Every program has normalized primary/supporting lanes, numeric-or-null
  current and maximum evidence grades, and dependency target namespaces.
- The one executable action carries its exact question, source locator,
  required inputs, unchanged ledger, arenas, return classes, control fields,
  local method, and durable output in one embedded `execution_packet`.
- The current-state transition protocol requires additive evidence, one live
  update, a revision increment, validation, explicit commit/push, and session
  finish.
- `LANES.yaml` remains stable topology and now names its structured
  `program_charter` as charter authority. Every stable charter copy is checked
  against it.
- New governed runs go only to `lab/process/runs/`. Root `runs/` is explicitly
  preserved as non-routing legacy evidence; no historical receipt moved.

The active scientific program, executable action, prepared paper candidate,
parked programs, dependency meaning, stop/reopen rules, and successor gate are
unchanged. Numeric grades make the existing prose explicit; they do not
promote it.

## Adversarial controls

The permanent governance validator contains no current program, action, or
paper identifiers. Its positive control deep-copies the live state, replaces
the active program ID, action ID, publication-program ID, and candidate ID
everywhere in memory, and reruns the unchanged validator. The mutated state
passes.

A context-isolated assay recovers all seven required cold-start answers using
only `AGENTS.md`, `START-HERE.md`, `CURRENT-RESEARCH.yaml`, and `LANES.yaml`.
The bundle is 5,189 words, below the 6,000-word ceiling.

## Validation

- JSON Schema draft 2020-12 plus semantic governance probe: `PASS 35/35`.
- Mutated-state genericity control: `PASS`.
- Stable charter parity: `PASS`.
- Active source heading and 29 evidence/action/input references: `PASS`.
- Stable entrypoint links: `PASS 38`, zero missing.
- Near-term approach-atlas regression: `PASS 16/16`.
- Python compile and YAML/JSON parse: `PASS`.
- Shared operating-architecture extension validator: `PASS` (`[]`).
- `git diff --check`: `PASS`.

The shared Canon Lane-manifest v1 validator is not an applicable contract for
this repository's existing local `lane_contract_version: 2.0`; it rejects that
pre-existing shape as expected. Dynamic Unity remains
`unregistered_manual_pending_owner_decision`, and this bounded run did not
authorize a Lane-schema migration or registration.

## Non-promotions and external state

No scientific result, theorem, ontology, novelty claim, prediction, paper
readiness, publication action, provider action, hardware action, schedule,
registration, or external research state changed.
