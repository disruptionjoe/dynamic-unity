---
run_id: RUN-20260725-133517-program-charter-orientation
status: completed_orientation_contract
repository: dynamic-unity
workflow: ratified program charter, dependency lanes, work channels, and cold-start assurance
authority: "Joe direct chat: implement the ratified identity and lane/channel architecture in one big swing, then rigorously verify incoming-agent orientation."
starting_revision: 2f4b92eca4ef93ce0962ff5baad2a249c430a0ac
claim_grade: "PROGRAM GOVERNANCE / ORIENTATION / NO SCIENTIFIC CLAIM PROMOTION"
---

# Program Charter and Agent Orientation

## Purpose

Ratify the agreed Purpose, Vision, Mission, North Star, and operating
principle across Dynamic Unity's authoritative entrypoints; reorganize active
lanes around scientific dependencies while separating work channels as
methods; and make a cold-start agent able to select correctly scoped work
without conversational context or a full historical read.

## Scope

- `START-HERE.md`
- `README.md`
- `AGENTS.md`
- active root topology in `LANES.yaml`
- `docs/certified-causal-reality-research-program.md`
- supporting index and test documentation
- one deterministic incoming-agent orientation contract probe

Historical scientific receipts, claim grades, hypotheses, predictions,
paper priority, Drafting-Factory state, and physical results remain unchanged.
The revision-38 active topology is preserved as a historical snapshot rather
than silently rewritten.

## Design contract

1. The program identity is exact and consistent across human and
   machine-readable authorities.
2. The charter remains ontology-neutral and preserves record-first
   reconstruction, operational duality, and physics-first remainder as
   coequal outcomes.
3. Lanes encode scientific responsibility/dependency; channels encode ways
   of working.
4. The forward coherent picture and inverse reconstruction-or-remainder test
   are both visible, with explicit evidence grades.
5. The current physical instrument-selection bottleneck is named once in
   plain language and routed consistently.
6. Historical lane maps remain discoverable but cannot masquerade as active
   authority.
7. New agents receive a minimal read order, work-selection contract, artifact
   destinations, and vocabulary/failure traps.

## Validation

1. Run the deterministic incoming-agent orientation contract probe.
2. Parse `LANES.yaml` and all JSON/YAML files.
3. Verify all local Markdown links in modified entrypoints and then across
   the repository.
4. Re-run the current `31/31` physical instrument-selection flagship to
   ensure governance changes do not disturb scientific evidence.
5. Compile the new Python probe and run `git diff --check`.
6. Perform a manual cold-start audit against six questions: purpose, North
   Star, earned evidence, current bottleneck, lane, and channel.
7. Record completion, update workspace memory, stage explicit paths, commit,
   push, and finish the mounted repository session.

## Stop conditions

- Do not promote an ontology, theorem, hypothesis, prediction, paper, or
  experimental claim.
- Do not rewrite dated receipts as though the new charter existed earlier.
- Do not collapse lanes back into methods or turn dependencies into a
  standing verification bureaucracy.
- If two authoritative entrypoints give different answers to a cold-start
  question, the run is incomplete.

## Completion receipt

The ratified identity and operating architecture are installed without
changing scientific state:

- `README.md`, `AGENTS.md`, `START-HERE.md`, the governing research program,
  and `LANES.yaml` carry one exact Purpose, Vision, Mission, North Star, and
  operating principle;
- the forward coherent picture is separately graded from the inverse
  reconstruction-or-remainder test;
- active `LANES.yaml` routing now contains eight dependency lanes
  (`1`–`7`, `A`) and seven cross-lane work channels with purposes, required
  receipts, current objectives, success signals, stops, and canonical
  references;
- both prior lane topologies remain discoverable as
  `legacy_lanes_revision_17` and `legacy_lanes_revision_38`;
- the live Lane-3 physical selector lift is consistent across the cold-start,
  machine-readable, and governing scientific surfaces; and
- the six manual cold-start questions resolve directly from `START-HERE.md`:
  purpose, decisive North-Star result, honestly earned evidence, current
  bottleneck, owning lane, and best work channel.

Validation completed:

- `tests/du_agent_orientation_contract_probe.py` passes `54/54`;
- every active lane's canonical references resolve;
- the active lane IDs, one-North-Star invariant, work-channel IDs, manifest
  revision, and provenance revision parse exactly;
- all `84` JSON files and the repository YAML parse;
- all `37` repository-local Markdown links resolve;
- the existing robust physical instrument-selection flagship regresses at
  `31/31`;
- the new Python probe compiles and `git diff --check` passes; and
- workspace memory records the durable charter and routing decision.

No scientific claim, theorem, hypothesis, concept, prediction, ontology,
paper priority, Drafting-Factory state, draft, submission, publication, or
external action changed.
