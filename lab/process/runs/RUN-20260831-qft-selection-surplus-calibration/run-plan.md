---
title: "QFT selection-surplus accounting and dynamical-holonomy calibration — run plan"
status: complete
doc_type: governed_run_plan
created: 2026-08-31
run_id: RUN-20260831-qft-selection-surplus-calibration
work_id: CCR-QFT-SELECTION-SURPLUS-CALIBRATION
claim_id: HC-DU-212
owner_repo: dynamic-unity
---

# Exact question

Can the `HC-DU-211` landing spine distinguish, without target refit:

1. an ordinary action selecting a state or sector inside a frozen QFT;
2. an upstream theory deriving a proper QFT completion subfamily; and
3. a selector key that merely copies the QFT choice into the antecedent?

The run must also determine whether any existing DU candidate already earns
cross-theory structural selection under that distinction.

# Frozen construction and order

Use one four-edge `Z2` gauge plaquette coupled to the unchanged four-site
signed oscillator of `HC-DU-207`.

1. Promote the edge signs to admitted gauge configurations.
2. Add the gauge-invariant Wilson energy `E_kappa(sigma)=-kappa h(sigma)`.
3. Prove the minimizing holonomy orbit for each nonzero `kappa`.
4. Land the selected orbit in the ordinary quantized oscillator sector and
   compare its spectrum with the two-sector incumbent family.
5. Separate within-law sector selection from cross-theory input selection.
6. Apply the distinction unchanged to `HC-DU-207--211`, finite-time infrared
   memory, direct action, causal action, and the conditional GU/K77 route.

# Selection contract

Let `Q_I(d)` be the frozen incumbent QFT completion family. Let upstream
antecedent `a` induce a QFT input/sector map `lambda`. Credit:

- `WITHIN_LAW_SELECTION` when a supplied QFT action dynamically chooses a
  state or sector;
- `CROSS_THEORY_STRUCTURAL_SELECTION` only when independently fixed upstream
  structure derives a proper subset of `Q_I(d)`;
- `PREDICTIVE_SHARPENING` only when the held-out target image contracts; and
- `NO_SELECTION_SURPLUS` when the antecedent simply contains an isomorphic
  copy of the QFT choice, or is fitted after target reveal.

No canonical scalar novelty score is assumed. The run records premise
relocation instead of pretending that information theory alone measures
explanatory depth.

# Grade, absorbers, cheapest kill, and stops

Maximum grade is scoped Grade 4.

Strongest absorbers:

- Wegner/Wilson `Z2` lattice gauge theory and Wilson-loop actions;
- gauge-orbit and twisted-sector quantization;
- sufficient statistics, data processing, and boundary relocation;
- ordinary QFT vacuum/sector selection and EFT matching; and
- `HC-DU-137/185/186/207/211`.

Cheapest kill: the plaquette result is only ordinary within-law selection,
and every current upstream candidate either supplies the QFT-selecting datum,
leaves multiple QFT inputs live, or has no locked target.

Stop if:

- a coupling sign inserted in the action is reported as derived;
- a selected vacuum/sector is reported as a selected record interface;
- QFT expressibility is treated as redundancy;
- target-visible spectral sharpening is confused with new physics; or
- GU, a provider, hardware, or Drafting Factory is written.

# Local-learning boundary and output

Use exact finite enumeration and closed-form spectra only. No simulation,
training, provider, external hardware, or sibling-repository write.

Durable output:

- one exact finite calibration probe and deterministic artifact;
- one theorem/accounting exploration with primary-source collision;
- one unchanged candidate disposition ledger;
- one counter-assumptive correction and live routing disposition; and
- one governed run receipt.
