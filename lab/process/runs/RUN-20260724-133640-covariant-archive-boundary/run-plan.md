# Run Plan: RUN-20260724-133640-covariant-archive-boundary

Plan created at: 2026-07-24T13:36:40-05:00

Status: completed

Run type: `progress`

Target: `dynamic-unity / HC-DU-031C`

Workflow: `one-spine formal build, hostile audit, and source-bounded synthesis`

Workflow source: `Joe direct chat: "Go ahead and orchestrate the next meaningful swing" (2026-07-24)`

## Objective

Extend the exact finite coherent Clock-QCA counter from `HC-DU-031B` with one
explicit finite archive interaction. Quantify how much source interference is
lost as archive distinguishability is gained, test record survival under carrier
loss and access after a declared reunion, and prove the strongest correct
boundary-relocation statement without confusing algebraic, observational,
channel, or interventional equivalence.

The target is a theorem-and-probe spine, not a new council wave and not a claim
that an archive, pointer basis, or irreversible law is selected by covariance.

## Governance References

- `AGENTS.md`
- `CONNECTIONS.md`
- `LANES.yaml`
- `lab/process/known-challenges-and-adversaries.md`
- `explorations/covariant-recorder-naturalization-swing-2026-07-24.md`
- `explorations/covariant-recorder-naturalization-adversarial-audit-2026-07-24.md`
- `explorations/cross-repo-gap-hardening-and-nonintuitive-reuse-map-2026-07-21.md`

## Selected Work References

- `LANES.yaml`, Channels `1.5`, `2.2`, `2.4`, and `4.4`
- `explorations/covariant-recorder-naturalization-swing-contract-2026-07-24.md`
- `tests/du_covariant_recorder_naturality_probe.py`
- Arrighi, Facchini, and Forets, arXiv:1404.4499
- Englert, *Phys. Rev. Lett.* 77, 2154 (1996),
  DOI `10.1103/PhysRevLett.77.2154`
- Kretschmann, Schlingemann, and Werner, arXiv:quant-ph/0605009
- Zurek, arXiv:0903.5082, as comparator context only

## Lane Selection

- Owner ID and scope: `dynamic-unity / repository`
- Primary target: `HC-DU-031C`
- Manifest revision and SHA-256:
  `16 / 35bd9d8d903977c5924bfcc2e62fe9d2cf26590c8a4a52c06da327373194a48e`
- Definition/control head: `5b297b16b8be3c396c5247b88485e258be1c6512`
- Selection basis: `HC-DU-031B constructed the coherent counter and made an explicit finite archive/backreaction assay the only admitted reopener on this branch. Joe authorized the next meaningful swing.`
- Context capsule: `Exact finite counter and q-transparent covariance closure complete; covariance admits every active-sector unitary and selects no record law; physical informative recording necessarily changes cross-history coherence.`
- Effective permission intersection: `repo-local research, probes, artifacts, Lane/hardening disposition, memory receipt, and routine GitHub versioning; no sibling-repo writes and no non-GitHub external writes.`
- Emergency-revocation evidence: `none; repository session sync start passed, branch contains origin/main and is even with its upstream, writer lock absent.`

## Orchestration

- One formal implementation lead may write only:
  - `explorations/covariant-archive-boundary-swing-2026-07-24.md`
  - `tests/du_covariant_archive_boundary_probe.py`
  - `tests/artifacts/du_covariant_archive_boundary_result.json`
- The root pass owns the frozen contract, source audit, hostile theorem audit,
  shared research-state integration, validation, receipt, memory, and Git.
- The lead may not modify `LANES.yaml`, registers, Run Plans, memory, or Git.
- Root independently checks every load-bearing formula and reruns every executable
  result before integration.

## Recent Run Collision Check

- The two older Run Plans are completed and do not overlap this target.
- `HC-DU-031B` is completed and pushed at the definition head.
- No Run Plan or receipt created within the prior hour overlaps the declared paths.
- No writer lock is present.
- Collision decision: `proceed on frozen non-overlapping paths`.

## Flow Patterns

- `source_exact_formal_extension`
- `proof_checking`
- `hostile_empirical_kill`
- `abductive_model_comparison`
- `publication_spine_synthesis`

## Candidate Construction

Use a two-history source fixture with a finite pointer/archive:

```text
|h=0>|0_A> -> |h=0>|a_0>
|h=1>|0_A> -> |h=1>|a_1>
```

with `|a_0>=|0>` and
`|a_1>=cos(theta)|0>+sin(theta)|1>` in the minimal qubit case. The
source-effective event controls this rotation; encoding-added `q` crossings
transport the pointer unchanged. A separately declared export interface may
coherently copy an orthogonal pointer-basis value into a fresh archive cell.

The construction must keep these distinct:

1. pointer carried by the particle;
2. exported archive cell;
3. source history/path degree;
4. algebraic quotient of labels;
5. physical partial trace or carrier loss;
6. a declared reunion/recombination measurement.

## Pre-registration

### Primary questions

1. Does the finite pointer/archive extension retain the exact source-sense
   rectangular-patch covariance square without frame-specific refits?
2. For the two pure archive states, do fixed source reunion visibility `V` and
   optimal archive distinguishability `D` obey the expected exact relation
   `V=|<a_0|a_1>|`, `D=sqrt(1-|<a_0|a_1>|^2)`, and `V^2+D^2=1`?
3. What record remains accessible if the carrier is lost before export versus
   after coherent export to a separate finite cell?
4. Which observational algebras and channels are invariant when an external
   archive is internalized into an enlarged state, and which fail once the
   archive is informative or intervenable?
5. Does reusing a finite cell merely produce aliases/revivals, while monotone
   accumulation requires fresh cells or another explicitly charged resource?

### Required theorems and witnesses

- Complete-unitary finite pointer interaction and source covariance inherited
  from transparent `q` transport.
- Exact two-history visibility/distinguishability relation for pure records.
- Gram-matrix channel formula: tracing the archive multiplies history coherence
  by archive-state overlap.
- Boundary Relocation and Informative-Record Obstruction:
  internalizing an external degree can preserve a declared subsystem
  observational algebra, but full coherent source-channel equality requires
  identical archive states on the relevant histories; interventional
  equivalence additionally fails whenever the relocated state can be acted on
  to change later source outcomes.
- Fresh-cell scaling:
  `V_m=|gamma|^m`,
  `D_m=sqrt(1-|gamma|^(2m))`, with explicit archive dimension/cell cost.
- One-cell reuse control showing finite recurrences or aliases rather than
  irreversible accumulation.

### Required executable controls

- recorder off: `theta=0`;
- maximally distinguishing record: `theta=pi/2`;
- at least one intermediate angle;
- direct active gate versus canonical and all-placement `q`-insertion patches;
- a held-out larger rectangular patch;
- raw micro-crossing increment rival that fails refinement;
- source populations preserved while cross-history coherence changes;
- loss before export versus loss after export;
- coherent export distinguished from cloning an arbitrary unknown state;
- fixed reunion readout, not a fitted decoder;
- reused-cell recurrence/alias versus fresh-cell scaling;
- arbitrary active-unitary covariance control showing admission is not selection;
- archive interventions that distinguish observational from interventional
  equivalence.

### Terminal outcomes

1. `COVARIANT_FINITE_ARCHIVE_WITH_EXACT_COMPLEMENTARITY`:
   the finite pointer/archive closes the same source covariance square, record
   access and loss controls work, and the exact information/interference
   relation is established.
2. `INTERFACE_ONLY_ARCHIVE`:
   the coherent pointer is covariant, but export/reunion requires a separately
   declared interface not selected or naturalized by the source law.
3. `ARCHIVE_BREAKS_NATURALITY`:
   the proposed archive coupling cannot close the frozen covariance square
   without a frame-specific gate, decoder, or schedule.
4. `NO_LOSS_SURVIVING_RECORD`:
   the construction exposes no independent archive that remains accessible
   after carrier loss.
5. `NO_NEW_DEPENDENCY_STATE`:
   all results reduce to already banked `HC-DU-031B` statements without a
   sharper theorem, construction, or falsifier.

Multiple scoped outcomes may coexist. In particular, a covariant pointer can
coexist with `INTERFACE_ONLY_ARCHIVE`.

### Hard kills

- Do not call a pointer rotation a classical irreversible record.
- Do not call coherent basis export cloning of an arbitrary quantum state.
- Do not treat the archive interface, fresh blank cells, or growing dimension
  as free.
- Do not infer law selection from covariance admission.
- Do not call a fixed subspace recombiner a realistic spatial reunion.
- Do not promote the known visibility/distinguishability or
  information-disturbance structure as novel.
- Do not claim full boundary invariance when only a quotient or declared
  observable algebra is preserved.
- Do not infer continuum covariance, observerhood, proper time, gravity,
  cosmology, or Dynamic Unity identity.

## Expected Outputs

- Frozen research contract.
- One publication-shaped technical note.
- One deterministic standard-library/NumPy probe and JSON artifact.
- One independent adversarial theorem-and-novelty audit.
- Updated Lane/hardening dispositions and test index.
- Completed receipt in this Run Plan.

## Required Checks

- Probe syntax/static check.
- Deterministic repeated artifact generation.
- Independent analytic check of every reported visibility, distinguishability,
  trace-distance, and scaling value.
- Positive and negative controls capable of flipping each major verdict.
- `HC-DU-031B` regression remains green.
- Relevant source Clock-QCA and foundational-record regressions remain green.
- YAML and JSON parse.
- `git diff --check` and explicit changed-path review.
- Revalidate Lane hash, ownership, permissions, writer lock, directed flow, and
  emergency revocation before consequential effects.

## Source Boundary

- Primary literature may establish the source Clock-QCA covariance,
  visibility/which-way inequality, channel information-disturbance structure,
  or the prior use of proliferated environmental records.
- Literature does not establish this run's finite extension, exact test
  fixture, boundary theorem scope, or Dynamic Unity interpretation.
- The two-history pure-state equality is derived directly in the note and
  checked independently; it is not a novelty claim.
- Novelty remains `SEARCH-INCOMPLETE`.

## External Action Authorization

`GitHub commit/push is authorized as routine versioning within Joe's approved repository work. No other external action is authorized.`

## Stop Conditions

- A new run or writer lock overlaps a declared writable surface.
- Covariance requires a decoder, pointer basis, or archive gate refitted per
  encoding.
- The only apparent result depends on silently discarding coherence or charging
  no resource for fresh archive states.
- Local resource headroom becomes unsafe.

## Working Notes

- Frozen before implementation.
- One formal implementation lead wrote only the predeclared technical note,
  probe, and artifact surfaces. Root independently reran and audited every
  load-bearing result before integration.
- The orthogonal modulo counter was deliberately not retained in the partial
  visibility/distinguishability fixture: it would make the zero-event and
  one-event histories perfectly distinguishable for every `theta`. The swing
  instead uses a tunable one-event pointer and keeps modulo counting as the
  predecessor/reuse control.
- The archive-operation audit separated retention, SWAP transfer,
  pointer-basis CNOT export, fanout redundancy, and independent fresh record
  formation. This prevented a basis predicate from being mislabeled as a
  copied arbitrary quantum record.
- The theorem audit narrowed boundary relocation to observational,
  channel-level, and interventional grades and rejected unqualified
  invariance.

## Result

The terminal outcomes are:

```text
COVARIANT_FINITE_ARCHIVE_WITH_EXACT_COMPLEMENTARITY
INTERFACE_ONLY_ARCHIVE
```

The tunable particle-carried pointer is a complete local unitary and exactly
closes the source-sense Clock-QCA covariance square. In the frozen two-history
witness:

```text
gamma = cos(theta)
V = |gamma|
D = sqrt(1-|gamma|^2)
V^2 + D^2 = 1
```

A history-independent unitary on pointer plus blank archive preserves
conditional overlap and joint trace distance. CNOT redundantly exports a
pointer-basis predicate but leaves only `sin(theta)^2` in the archive after
pointer loss. SWAP transfers the full `sin(theta)` distinguishability and
retains no pointer copy. Repeated CNOT fanout does not improve the archive-only
marginal; independent fresh interactions give
`V_m=|gamma|^m`, `D_m=sqrt(1-|gamma|^(2m))` and charge archive dimension
`2^m`.

Finite one-cell reuse recurs or aliases. Exact nonaliasing record semantics
must weaken, acquire fresh support/environmental capacity, or add a modeled
nonunitary/coarse-graining mechanism. This trichotomy is relative to the
declared completion class.

State augmentation can preserve a declared history-diagonal observational
algebra. An informative archive cannot preserve the full coherent source
channel. An interventional difference requires admitted archive access plus a
later fixed recoupling. The surviving theorem is therefore **Boundary
Relocation and Informative-Record Obstruction**.

The build does not select the pointer, basis, archive coupling, blank support,
reunion, or record law and does not establish classical irreversibility,
observerhood, proper time, continuum or higher-dimensional covariance,
gravity, cosmology, or Dynamic Unity identity. No claim was banked or seeded.

## Validation Receipt

- New probe: `31/31` deterministic checks passed on repeated runs.
- Stable archive artifact SHA-256:
  `f280617b9309af21dbceae13283dd317240ff7fe48fbe4647368ddebe79dd018`.
- Predecessor regression:
  `du_covariant_recorder_naturality_probe.py` passed `31/31`.
- Foundational regressions:
  orthodox `15/15`, heterodox `16/16`, commercial `16/16`, and philosopher
  `15/15`.
- Probe compilation, JSON parse, YAML parse, and `git diff --check` passed.
- Final manifest revision and SHA-256:
  `17 / 441f5207deacdf9b28dc3b84eaacf2576517d4014e977410b1e36d7d9d524161`.
- Contract SHA-256:
  `b886fdf8d36448cf11dc493cf8c09bdc1b7d2a28b63e6a5f9b6a802540e48caf`.
- Technical-note SHA-256:
  `f1520f40eb8d315ed2b3ab69aaf4983acfcb883754c417a0cd225407de077b70`.
- Adversarial-audit SHA-256:
  `16600ed6151e2bce045349fea88e3e5db1ac2f28f63325b8e1f2a6ed0aca4984`.
- Probe SHA-256:
  `c7b052e2b95b72ae5575f6e8b36706af8f4bf0d9d3f15d7263d59219b9c045c8`.
- Novelty remains `SEARCH-INCOMPLETE`; all literature comparisons are
  primary-source bounded.
- Revalidated repository ownership, directed flow, absence of a writer lock,
  and the unchanged authorization boundary before versioning.

## Disposition

The abstract record/archive branch stops at `INTERFACE_ONLY_ARCHIVE`. Reopen
it only with an independently motivated local autonomous archive-emission law
that selects or constrains the interface and pays recurrence/support costs, or
use `HC-DU-032` as an explicitly interface-relative control without promoting
the archive to a physical observer. The next primary Dynamic Unity build
returns to `HC-DU-011A`, the independent causal-post generativity and held-out
geometry frontier.
