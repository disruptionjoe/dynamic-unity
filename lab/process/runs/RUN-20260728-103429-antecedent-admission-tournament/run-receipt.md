---
title: "Selected-record campaign Swing 1 — antecedent admission tournament receipt"
status: completed
doc_type: governed_run_receipt
created: 2026-07-28
run_id: RUN-20260728-103429-antecedent-admission-tournament
work_id: N10-CCR-S1-ANTECEDENT-ADMISSION
program_id: CCR-SELECTED-RECORD-TO-NORTH-STAR
claim_id: HC-DU-074
authority: "Joe direct chat: Go"
owner_repo: dynamic-unity
---

# Antecedent admission tournament receipt

## Return

```text
NO_READY_SCIENTIFIC_SUCCESSOR
```

No candidate was scored because no candidate passed all six hard admission
gates. Swing 2 was not activated.

## Scientific result

The run checked the exact reopeners against:

- current Time as Finality and GU Formalization source-owned results;
- a current gravitational quantum-filtering construction;
- an autonomous nonequilibrium quantum detector;
- localized-QFT realizability;
- finite-time infrared memory; and
- topological, metastable, and autonomous memory classes.

The decisive control is source-pinned. Gough and Rees derive a
Hudson--Parthasarathy output-field dilation of the Diósi--Penrose generator
and obtain its diffusive stochastic equation after choosing output
homodyning. The same source explicitly gives number counting as an
alternative monitoring strategy. Those interfaces have different record
algebras and conditional histories but the same unconditional dynamics.
The antecedent therefore does not select the record unravelling.

Schwarzhans et al. provide a real autonomous detector construction and
derive useful thermodynamic performance tradeoffs, but the detection channel,
input-conditioning step, macroscopic-click interpretation, and no-memory
reset contract are supplied. This remains inside `HC-DU-061`'s prior class.

Current sibling results change no DU physical reopener, and the previously
audited QFT, infrared, and memory classes add no new premise.

## Durable disposition

`HC-DU-074` records:

```text
selected output-field coupling
  != selected output observable/unravelling
  != bounded retained provenance record
  != held-out target reconstruction.
```

The exact future reopener is a physics-selected output or interface
equivalence class with finite access, retained provenance, and an
independently escrowed target.

No physical law, universal no-go, prediction, model, hardware result, paper,
or ontology is promoted. Quantum-trajectory unravelling freedom and detector
engineering absorb the components.

## Routing

`CURRENT-RESEARCH.yaml` advances from revision 22 to 23 and remains
quiescent:

- no active scientific program;
- no executable action;
- no selected successor;
- `DU-PAPER-013-HARDENING` remains separately prepared; and
- the successor intake now explicitly recognizes a physically selected
  output/unravelling with bounded provenance archive.

The completed campaign evidence remains in its dated exploration and
governed run. It is not copied into the live program portfolio because the
hard gate admitted no successor.

## Hardware and computation

No model or hardware was used. The primary-source equations themselves give
the exact same-dynamics/different-record counterfactual. External hardware is
unavailable and irrelevant to this disposition.

## Validation

- `python3 tests/du_agent_orientation_contract_probe.py` — **PASS**, 35/35;
  56 current references resolve; 5,993/6,000 cold-start words
- `python3 tests/du_capability_indexed_north_star_probe.py` — **PASS**, 20/20
- new local Markdown targets — **PASS**
- `git diff --check` — **PASS**

