---
run_id: RUN-20260725-140945-conservation-certified-record
status: completed_absorbed_interface_necessity_no_go
repository: dynamic-unity
workflow: conservation-to-certified-record flagship gate
authority: "Joe direct chat: All right, go ahead and run that."
starting_revision: 00d19db751e9359d36de6cb7234f982f66114099
claim_grade: "OPEN RESEARCH / RESULTS GRADED BY EXECUTED EVIDENCE"
---

# Conservation to Certified Record

## Purpose

Run the highest-value physical lift left by the revision-38 instrument-orbit
selector:

> Can an independently motivated additive conservation law, a bounded
> apparatus-asymmetry resource, a declared perturbation ball, and complete
> instrument/archive access select a robust record instrument, or do they
> only constrain a symmetry orbit and expose the extra physical structure
> selection requires?

The run may return:

1. a scoped physical Record Selection Theorem;
2. an Interface-Necessity No-Go;
3. an exact minimal-extra-structure classification; or
4. a source-pinned absorption result that closes this selector branch.

It may not promote standard Wigner--Araki--Yanase, quantum-reference-frame,
state-discrimination, interferometry, or instrument-tomography mathematics as
new Dynamic Unity physics.

## Frozen physical contract

### Systems and conservation law

- Source \(S\): one qubit with charge
  \(N_S=|1\rangle\langle1|\).
- Apparatus reference \(R\): one qubit with
  \(N_R=|1\rangle\langle1|\) and calibrated state
  \[
  \rho_R(\alpha,v)=\frac12
  \begin{pmatrix}
  1 & v e^{-i\alpha}\\
  v e^{i\alpha} & 1
  \end{pmatrix},
  \quad 0\leq v\leq1.
  \]
- Additive conserved quantity:
  \(N_{\rm tot}=N_S+N_R\).
- Pointer and archive have four orthogonal, charge-degenerate labels
  \(0,+,-,2\). Their writing interaction may not change \(N_{\rm tot}\).

### Symmetric processor and formed archive

- The joint pointer PVM is
  \[
  P_0=|00\rangle\langle00|,\quad
  P_2=|11\rangle\langle11|,
  \]
  with \(P_+\) and \(P_-\) resolving an orthonormal basis inside the
  total-charge-one sector.
- At the balanced setting,
  \[
  |\chi_\pm\rangle=(|10\rangle\pm|01\rangle)/\sqrt2.
  \]
- A declared imbalance \(\beta\) rotates that basis while remaining inside
  the same conserved sector.
- The explicit formation isometry writes the PVM result to a pointer and then
  to a durable four-label archive. All projectors commute with
  \(N_{\rm tot}\), so the isometry is charge intertwining and has a
  charge-conserving unitary extension.
- Archive noise swaps \(+\leftrightarrow-\) with probability \(q\) and leaves
  the charge-sector labels fixed.

### Target ensemble and task

- The held-out source ensemble is equiprobable
  \[
  |\pm_\phi\rangle=(|0\rangle\pm e^{i\phi}|1\rangle)/\sqrt2.
  \]
- The action task is to guess the sign from the accessible archive. A second
  action contract permits abstention on charge-sector outcomes \(0,2\).
- Source preparations, number of source specimens, symmetric processor,
  pointer/archive dimensions, interaction count, decoder, and archive-noise
  channel are matched. Reference coherence, phase orientation, and any
  additional copies are charged explicitly.

## Predeclared perturbation ball

- reference visibility \(v\in[v_{\min},1]\);
- reference/target phase mismatch
  \(|\phi-\alpha|\leq\delta_{\max}<\pi/2\);
- beam-splitter imbalance
  \(|\beta-\pi/4|\leq b_{\max}<\pi/4\); and
- archive sign-flip probability \(q\in[0,q_{\max})\), with
  \(q_{\max}<1/2\).

No parameter is refit after verdicts are read.

## Exact targets

1. Verify the induced source POVM
   \[
   E_\pm=\frac14(I\pm vX_\alpha),\quad
   E_0=\frac12|0\rangle\langle0|,\quad
   E_2=\frac12|1\rangle\langle1|
   \]
   at the balanced setting.
2. Derive and verify, including imbalance and archive noise,
   \[
   D_{\rm access}
   =\frac12(1-2q)v
     |\sin(2\beta)\cos(\phi-\alpha)|
   \]
   for \(0\leq q<1/2\), where \(D_{\rm access}\) is total-variation
   distinguishability of the two archive distributions.
3. Verify forced-guess success
   \[
   P_{\rm guess}
   =\frac12+\frac14(1-2q)v
     |\sin(2\beta)\cos(\phi-\alpha)|.
   \]
4. Verify the perturbation-ball floor
   \[
   D_{\rm access}\geq
   \frac12(1-2q_{\max})v_{\min}
   \cos(2b_{\max})\cos(\delta_{\max}).
   \]
5. Reconstruct every selective source instrument map by exact
   informationally complete linear inversion and compare its Choi matrix
   with the model, rather than checking probabilities alone.
6. Hold fixed charge distribution, charge variance, dimension, symmetric
   processor, and asymmetry magnitude while rotating only \(\alpha\). Test
   whether the effective record axis rotates with it.
7. Compare the coherent reference with its \(U(1)\)-twirled state at a
   matched declared resource ledger, and price any capability delta to the
   oriented reference token rather than to conservation or "finality."

## Landscape and novelty gate

Treat the following as primary collision targets:

- Wigner--Araki--Yanase restrictions and Ozawa quantitative bounds;
- the Marvian--Spekkens no-programming/resource-theory account;
- the Ahmadi--Jennings--Rudolph U(1) reference-frame construction and
  optimal state-discrimination examples;
- recent general-symmetry quantitative WAY bounds; and
- standard quantum instrument and process tomography.

The phase-rotated resource states are expected to be equally asymmetric under
every symmetry-invariant resource measure. If their effective record axes
differ under the same symmetric processor, conservation plus a scalar
asymmetry budget does not select one instrument. The oriented program state is
then necessary extra structure.

## Matched foils and absorbers

- **Twirl foil:** same reference populations and charge moments, but
  \(v=0\).
- **Orientation foil:** same \(v\), dimension, charge distribution,
  variance, and processor; \(\alpha\) changes by \(\pi/2\).
- **Conservation foil:** every joint projector must be checked against
  \(N_{\rm tot}\).
- **Archive foil:** pointer distinction is fixed while \(q\) changes.
- **Tomography foil:** equality of terminal outcome probabilities may not
  substitute for equality of selective Choi maps.
- **Resource foil:** any capability gain disappears from the claim if the
  reference token is omitted from the ledger.

## Stopping rule

Stop this selector line after the present run if primary literature and the
executed model show that:

1. conservation defines a feasible symmetric processor class;
2. asymmetry magnitude prices attainable accuracy;
3. orientation of the apparatus reference supplies the measurement axis;
4. complete tomography identifies the implemented instrument but does not
   causally select it; and
5. no independently derived Dynamic Unity constraint narrows the remaining
   orbit.

In that case, bank the Interface-Necessity No-Go/minimal-structure
classification, merge it into the existing paper spine, and move the live
research bottleneck. Do not run another fitted QND or finite-reference
selector.

## Downstream gate

If the formed interface survives, run exactly one vertical slice:

- compare sign-discrimination capability with coherent oriented reference,
  misaligned reference, and twirled reference;
- hold the task, source count, processor, archive, decoder, and risk contract
  fixed;
- report both forced-guess success and conclusive coverage;
- classify the result as standard resource-enabled capability unless it
  exceeds the complete model.

Do not infer public finality, regional composition, time, geometry, ontology,
or new physics from this slice.

## Validation

1. deterministic self-checking probe and canonical JSON artifact;
2. exact matrix checks of conservation, POVM completeness, complete-positive
   selective maps, tomography reconstruction, and analytic identities;
3. off-grid fixed-seed stress test over the perturbation ball;
4. related physical-selector, formed-sharp, and history-compiler regressions;
5. Python compilation; JSON/YAML parse; local-link check;
6. `git diff --check`, explicit-path staging, commit, push, and repository
   session finish; and
7. durable workspace memory receipt.

## Completion receipt

The frozen gate completed without refitting its source ensemble, reference
family, joint PVM, perturbation ball, archive channel, decoder, or capability
task.

### Exact result

- `tests/du_conservation_certified_record_probe.py` passes `21/21`.
- The four joint record projectors commute exactly with
  \(N_S+N_R\).
- The explicit pointer/archive formation isometry is charge intertwining.
- The induced source POVM matches the analytic effects, every selective map
  is completely positive, and four informationally complete source
  preparations reconstruct every selective Choi map with maximum error
  `7.850e-17`.
- The accessible distinction and forced-guess capability obey the two
  predeclared exact formulas on the full grid.
- The perturbation ball has exact floor `0.307369759040`.
- A fixed-seed `1000`-case off-grid stress test has maximum surface error
  `2.776e-16` and minimum sampled floor margin `1.158e-02`.
- Phase-rotated references retain identical charge mean/variance, purity,
  phase QFI, trace-asymmetry magnitude, dimensions, symmetric processor,
  pointer, archive and decoder while programming \(X\) and \(Y\) record axes.
- The aligned reference gives forced-guess success `3/4`, coverage `1/2`,
  and conditional success `1`; twirled and quarter-turn references give
  forced-guess success `1/2`.
- The reference's trace-asymmetry resource is consumed, and archive noise
  separately multiplies visible distinction by \(1-2q\).

### Scientific verdict

The positive construction is a robust, formed, completely tomographable
\(U(1)\)-conserving record instrument.

The no-go is:

> Additive conservation plus a symmetry-invariant apparatus-asymmetry budget
> selects at most a measurement orbit. The oriented apparatus reference
> programs the axis.

The classified additional structure is an oriented reference or equivalent
symmetry-breaking preparation, fixed processor, pointer/archive,
decoder/action, and selective-map access.

Primary WAY, asymmetry-resource, quantum-reference-frame, no-programming,
state-discrimination, interferometric-measurement, and instrument-tomography
literature absorbs the component physics. The result receives no new theorem
ID, law, hypothesis, prediction, ontology, or standalone paper.

### Branch and paper disposition

- Stop further fitted finite-QND or finite-reference selector work.
- Merge the Interface-Necessity No-Go and physical-interface passport into
  `DU-PAPER-007`.
- Retain the resource-accounted capability slice as a hostile normalization
  control for `DU-PAPER-010`.
- Create no new Factory seed and change no Factory priority or production
  state.
- Move the live foundational paper target to an implementation-complete
  physical interventional-sufficiency arm with full provenance, frozen
  physical refinements, and a calibrated finite-shot null.

### Validation

- New artifact is byte-stable at SHA-256
  `bd9c736317ec0bc53c393a980ba6ffed6ed919150a866480b52757933266b237`.
- The cold-start orientation contract passes `54/54`.
- The paper-opportunity portfolio audit passes `20/20` over all `27`
  candidates.
- Related physical-selector, formed-instrument, compiler, physical-history,
  record-composition, regional-descent, formed-descent, and hostile
  quantum-selection probes pass `31/31`, `34/34`, `11/11`, `16/16`, `24/24`,
  `24/24`, `36/36`, and `16/16`.
- The new and modified Python sources compile.
- All JSON files parse; all YAML files parse through Ruby Psych.
- All `32` local Markdown links on the modified surfaces resolve.
- Repository whitespace checks pass.
- Explicit-path staging, commit, push, memory logging, and repository session
  finish follow this receipt.
