---
title: "ETH actuality-algebra naturality — run receipt"
status: completed
doc_type: governed_run_receipt
created: 2026-07-28
run_id: RUN-20260728-183138-eth-actuality-algebra-naturality
work_id: CCR-ETH-ACTUALITY-ALGEBRA-NATURALITY
claim_id: HC-DU-091
authority: "Joe direct chat: Go, 2026-07-28"
owner_repo: dynamic-unity
---

# ETH actuality-algebra naturality

## Receipt

The exact gate returns:

```text
REPRESENTATION_NATURALITY
+ ALGEBRA_RELATIVE_EVENT_AMBIGUITY
+ NO_PHYSICAL_COFILTRATION_SELECTOR
+ STANDARD_INSTRUMENT_ABSORPTION
+ STOP_LOWER_SELECTOR_FAILED
```

For any state-carrying star-isomorphism \(\alpha\), the ETH centralizer and its
center transform covariantly:

\[
\alpha(C_\omega(A))=C_{\omega\circ\alpha^{-1}}(\alpha(A)),
\qquad
\alpha(Z_\omega(A))=Z_{\omega\circ\alpha^{-1}}(\alpha(A)).
\]

The stronger restriction law fails. With \(A=M_2(\mathbb C)\), the tracial
state, and identity dynamics, the ambient actuality algebra is
\(\mathbb C I\). Restriction to either
\(\operatorname{span}\{I,Z\}\) or \(\operatorname{span}\{I,X\}\) produces the
entire two-dimensional Abelian subalgebra as the actuality algebra. The two
event families are incompatible and exchanged by a state- and
dynamics-preserving Hadamard symmetry.

A faithful nontracial state supplies the positive hostile control:
the ambient actuality algebra is diagonal, while restriction to the
\(X\)-algebra makes the \(X\)-algebra actual. Thus the failure is not only a
maximally mixed-state degeneracy.

## Selector and absorber boundary

Once either two-dimensional atomic algebra is fixed, its minimal event
projections are unique up to labels. The failure is therefore at the earlier
future-algebra/co-filtration selector, not at a gratuitous generator
convention.

Once those projections and the event time are fixed, ETH's Born weights and
conditioned state are exactly the matched Lüders projective instrument. The
finite fixture provides no operational excess over standard quantum
instruments.

This does not refute ETH or prove that no physical QFT net can select a
co-filtration. It proves that the center-of-centralizer rule alone cannot be
credited with selecting the physical net to which it is applied.

## Decision

The lower selector failed, so event-to-record, provenance, access,
certification, and regional-finality work does not continue from this branch.
Dynamic Unity remains quiescent with no activated successor.

Reopen only when a concrete physical theory:

1. derives the future-algebra co-filtration from an independently frozen
   system, dynamics, locality/boundary, and state contract;
2. proves covariance under its physical equivalences;
3. resolves or physically types restriction and time-refinement ambiguity;
   and
4. yields a no-refit consequence beyond the matched standard instrument.

## Resource disposition

- direct operator-algebra analysis established the result;
- the local exact script is a proportional post-proof regression under
  `LMLG-01`, not an admitted research model;
- no external hardware is relevant;
- no paper, prediction, publication, submission, provider, or contact action
  was authorized or performed.

## Durable files

- `explorations/eth-actuality-algebra-naturality-restriction-counterexample-and-instrument-boundary-2026-07-28.md`
- `tests/du_eth_actuality_algebra_naturality_probe.py`
- `tests/artifacts/du_eth_actuality_algebra_naturality_result.json`
- `docs/quantum-foundations-orientation-surface.md`
- `explorations/concept-register.md`
- `COUNTER-ASSUMPTIVE-FINDINGS-REGISTER.md`
- `CURRENT-RESEARCH.yaml`
- this run plan
- this run receipt

## Validation

- exact finite regression — **PASS**, `19/19`;
- deterministic artifact repeat — **PASS**;
- Python compilation — **PASS**;
- orientation/governance probe — **PASS**, `37/37`, 248 unique
  counter-assumptive findings, and 5,972/6,000 cold-start words;
- `git diff --check` — **PASS**.
