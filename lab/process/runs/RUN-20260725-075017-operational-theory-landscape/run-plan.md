---
run_id: RUN-20260725-075017-operational-theory-landscape
status: completed
repository: dynamic-unity
workflow: exact operational-theory landscape calibration and transfer
authority: "Joe direct chat: orchestrate the strongest swing applying the GPT, two-sided-foil, force-versus-permit, and constructive causal-inference framing across Dynamic Unity"
starting_revision: 6a7ae4032764abad17db0e45a431ff4c191eec56
claim_grade: "EXACT FINITE OPERATIONAL CALIBRATION / KNOWN BELL-GPT MATHEMATICS / DYNAMIC-UNITY SELECTION PRINCIPLE OPEN"
---

# Operational theory landscape calibration and transfer

## Objective

Install one reusable contract that prevents Dynamic Unity from confusing:

1. a normalized conditional behavior;
2. a no-signalling behavior;
3. a local/global-hidden-variable realization;
4. an explicit quantum realization;
5. a behavior excluded by a quantum bound; and
6. a behavior whose quantum status remains unresolved.

Calibrate the contract exactly on the binary-input/binary-output CHSH
landscape, then transfer its receipt language unchanged to the existing
authenticated-route and Mermin--Peres fixtures. Use the transfer to sharpen,
not replace, the open certified-overlap-identity problem in `HC-DU-035C` and
`H-CCR-16`.

## Frozen operational contract

- Inputs are freely indexed binary settings `x,y`; outputs are binary values
  `a,b`.
- A behavior is the complete table `p(a,b|x,y)`, frozen before
  classification.
- Conditional validity, no-signalling, local extendibility, quantum
  realization, quantum exclusion, and unresolved quantum membership are
  separate receipts.
- Local extendibility means one nonnegative normalized joint distribution on
  `A0,A1,B0,B1` reproduces all four setting-pair marginals.
- A local obstruction must carry either an independently verified exact
  Farkas certificate or a directly substituted Bell-functional witness.
- Quantum membership is positive only with an explicit state-and-measurement
  realization or another complete constructive certificate.
- Satisfying the CHSH/Tsirelson bound does not by itself certify quantum
  membership.
- A behavior exceeding `2 sqrt(2)` in this frozen CHSH scenario is excluded
  from ordinary quantum realizations by the Tsirelson bound; no-signalling
  alone still permits values up to `4`.
- Bell/noncontextual nonexistence remains conditional on the declared
  cross-context identities, setting independence, and causal boundary.
- Theory membership or exclusion licenses no metaphysical substrate claim.

## Work packages

### A. Reusable theory-landscape contract

- Define the required operational object, assumptions, nested comparison
  classes, lower absorber, upper permissive foil, membership and exclusion
  receipts, selection principle, held-out discriminator, and
  force/permit/underidentified disposition.
- Make invalid, signalling, locally realizable, proven nonlocal,
  explicitly quantum, proven post-quantum, and quantum-unresolved outcomes
  impossible to alias.

### B. Exact CHSH calibration

- Construct exact deterministic-local and isotropic local-boundary controls.
- Construct an exact rational, explicitly quantum, Bell-violating point using
  a Bell state and rational Pauli-plane measurement directions with
  `CHSH=14/5`.
- Construct the exact algebraic Tsirelson-saturating point and verify its
  state, observables, probabilities, and state-supported squared CHSH
  operator.
- Construct an isotropic post-quantum point, a PR box, a signalling control,
  and an invalid-table control.
- Reuse the exact marginal-extension compiler wherever the behavior is
  rational; independently substitute all local primal/dual receipts.
- Demonstrate that withholding a valid quantum realization changes the
  evidence status to unresolved without changing the behavior.

### C. Existing-fixture transfer

- Build a typed certified-overlap pilot that separates a repeated label,
  equality of outcome effects, equality of selective instruments, equality of
  multi-time continuations, and provenance/value identity.
- Require an implementation-gauge positive control and a matched-effect,
  context-dependent disturbance rival.
- Require authenticated consistency, equivocation, and cross-epoch controls.

### D. Existing-fixture landscape transfer

- Reclassify the authenticated route fixture as:
  coarse identity excluded, route-provenance realization formed, adversarial
  safe rejection still required.
- Reclassify the Mermin--Peres fixture as:
  noncontextual global value assignment excluded, explicit context-indexed
  quantum instruments realized, maximal occurrence splitting merely
  permissive.
- Preserve certified overlap identity as the load-bearing open selection
  principle between rigid global biography and unconstrained context
  splitting.

### E. Research consequence

- Record a repo-wide theory-landscape card for active central hypotheses.
- Identify the nearest possible Dynamic Unity advance as a physically
  warranted overlap-identity selection principle, not another contextuality
  witness.
- Hold a possible regional-finality quantum bound as a conjectural research
  direction only after collision checking and an operational quantity are
  specified.

## Validation

1. run the new landscape and certified-overlap probes twice and require
   byte-identical output;
2. compile all new Python;
3. rerun the exact marginal-transfer and adjacent regional/finality probes;
4. parse generated JSON and `LANES.yaml`;
5. run `git diff --check`;
6. stage explicit paths, commit, push, and finish repository synchronization.

## Stop conditions

- Stop quantum-membership language if only an outer bound or lack of
  exclusion is available.
- Stop post-quantum language if the operational scenario or Tsirelson
  assumptions change.
- Stop local-hidden-variable conclusions if overlap identity, setting
  independence, or the causal boundary is refit after seeing outcomes.
- Stop Dynamic Unity novelty language if the result is only CHSH,
  Tsirelson, PR-box, GPT, contextuality, or standard quantum-causal-model
  mathematics.
- Stop a regional-finality-bound claim until it has a defined observable,
  resource ledger, standard quantum comparator, super-quantum foil, and
  finite falsifier.
- Promote no ontology, physical law, prediction, paper priority, Factory
  state, submission, publication, or external action from this control swing.

## Completion result

- `lab/process/operational-theory-landscape-contract.md` now requires one
  frozen operational interface, separate validity/causal/membership/exclusion
  receipts, restrictive and permissive foils, an independent selection
  principle, and a held-out discriminator.
- `tests/du_operational_theory_landscape_probe.py` passes `28/28` twice with
  byte-identical SHA-256
  `96c9c417eaa9a07c34df0292c017bd682d0e63069b179f335a4ccc8ba40bd33f`.
- Its exact controls locate deterministic and isotropic-local behaviors,
  construct a rational Bell-state point at `CHSH=14/5`, construct the
  algebraic Tsirelson boundary at `2 sqrt(2)`, exclude isotropic `CHSH=3` and
  PR `CHSH=4` from ordinary quantum theory under the frozen scenario, reject
  signalling and invalid tables, and keep a same-behavior
  realization-withheld twin unresolved.
- The same landscape-card fields transfer unchanged to the authenticated
  route and complete Mermin--Peres fixtures without changing their prior
  dispositions.
- `tests/du_certified_overlap_identity_probe.py` passes `16/16` repeatedly
  with byte-identical SHA-256
  `b6c76cef34c28e1640db5747bc05326204de0dc972fc0a2fd42bcd5221a28652`.
- The quantum identity pilot proves at exact finite-control grade that equal
  outcome effects and all single-time probabilities need not imply equal
  selective instruments or multi-time continuations. It also quotients an
  implementation-different phase that induces identical complete CP maps.
- The authenticated twin separates authentic occurrences, consistent
  cross-route value receipts, equivocation, and cross-epoch nonidentity.
- The durable research correction is that overlap identity is a typed vector
  over label, effect, selective instrument, multi-time continuation,
  provenance/value, and upper action. Theory-class expressiveness and identity
  resolution are independent coordinates.
- Neighboring `33/33`, `21/21`, `20/20`, `24/24`, and `18/18` regional,
  marginal, and finality regressions pass.
- The next scientific dependency is a robust calibrated typed-identity assay
  on an implementation-complete sequential contextuality or proper-time
  instrument, followed by recursive vector-defect composition and sparse
  exact scaling.
- No new Bell/GPT/contextuality mathematics, generalized causal theory,
  regional-finality bound, physical law, prediction, ontology, paper
  priority, Factory state, submission, publication, or external action is
  promoted.
