---
run_id: RUN-20260725-071641-multitime-marginal-transfer
status: completed
repository: dynamic-unity
workflow: instrument-derived regional grammar and exact marginal transfer
authority: "Joe direct chat: run the prepared multi-time regional grammar, general marginal compiler, and cross-platform transfer swing"
starting_revision: 850d05c0067dc4040b52553eb04524eb164b92b9
claim_grade: "EXACT FINITE RATIONAL COMPILER + COMPLETE FINITE INSTRUMENT TRANSFERS / COMPONENT MATHEMATICS KNOWN / PHYSICAL THEORY OPEN"
---

# Multi-time regional grammar and marginal transfer

## Objective

Execute the three prepared successors of `HC-DU-035C` without refitting the
adjudicator:

1. derive candidate interaction regions from independently fixed multi-time
   instruments rather than endpoint functions;
2. build a general exact-rational finite marginal-extension compiler that
   returns either a primal joint distribution or a checkable dual/Farkas
   obstruction; and
3. transfer the unchanged selection, compatibility, boundary-action, and
   provenance contract to one replayable authenticated protocol and one
   complete quantum-instrument specimen.

## Frozen contract

- The finite variable alphabets, context cover, complete local marginals,
  instrument operations, accessible outcomes, representation quotient, and
  causal-break action are fixed before compilation.
- Region extraction reads complete declared instrument kernels, not the
  downstream task response.
- Deterministic implementation relays and label permutations are quotient
  controls, not new physical regions.
- Marginal compatibility is exact rational feasibility of one joint
  distribution over the declared global variables.
- Incompatibility carries a dual vector `y` satisfying
  `A^T y >= 0` and `b^T y < 0`.
- A global extension does not imply action-sufficient node promotion,
  protocol safety, public finality, joint measurability, or physical
  fundamentality.
- Route-, epoch-, or measurement-context provenance may repair a false
  identification only when that register is independently formed and
  charged.

## Work packages

### A. Exact marginal compiler

- Accept arbitrary finite variable alphabets and arbitrary context
  hypergraphs.
- Validate complete normalized rational marginal tables.
- Construct the global-state marginalization matrix.
- Run an exact rational Phase-I simplex with Bland tie-breaking.
- Return a sparse primal extension or normalized integer Farkas witness.
- Verify every returned certificate independently.
- Recover binary-cycle controls, two glued-cycle controls, and a genuinely
  nonacyclic contextual cover.

### B. Multi-time regional grammar

- Use three primitive binary events and two overlapping record instruments.
- Derive each minimal parent region from the complete stochastic kernel.
- Compare the instrument process with an endpoint-equivalent rival.
- Use one held-out causal break to separate the histories.
- Check relabeling covariance and deterministic-relay quotienting.
- Apply the same extractor to the authenticated protocol without refit.

### C. Authenticated transfer

- Enumerate signed regional transcripts and delivery/epoch provenance.
- Show that authentication alone does not identify one origin value across
  equivocated routes.
- Compile coarse canonical-origin marginals and return a dual obstruction.
- Lift to independently recorded route-specific provenance and return a
  primal extension.
- Preserve safe rejection as distinct from successful protocol descent.

### D. Quantum transfer

- Construct the six commuting contexts of the two-qubit
  Mermin--Peres square from exact Gaussian-rational matrices.
- Verify Hermiticity, commutation inside contexts, projector completeness,
  completely positive selective maps, and trace preservation of every
  nonselective instrument.
- Derive all local outcome marginals from the maximally mixed state.
- Compile the six-context cover and return an exact dual obstruction.
- Verify that every one-context deletion admits an extension.
- Classify the obstruction as standard contextuality/joint-measurability
  failure, not a Dynamic Unity physical deviation.

## Validation

1. run compiler unit/regression controls;
2. run the integrated multi-time/authenticated/quantum probe twice and require
   byte-identical artifacts;
3. rerun the regional-selection, noisy-descent, regional-kernel,
   stochastic-transfer, and entangled-absorption neighbors;
4. compile all new Python;
5. parse generated JSON and `LANES.yaml`;
6. run `git diff --check`;
7. stage explicit paths, commit, push, and finish repository synchronization.

## Stop conditions

- Stop physical-selection language if the extracted region depends on the
  downstream task or changes under an inert relay.
- Stop a marginal verdict if the primal or dual certificate fails independent
  substitution.
- Stop protocol-finality language if the lifted process merely exposes
  equivocation that the protocol must reject.
- Stop beyond-standard-quantum language if the quantum result is absorbed by
  ordinary contextuality, incompatibility, disturbance, leakage, or omitted
  context.
- Promote no claim, prediction, ontology, law, paper priority, or Factory
  state without a separately justified result.

## Completion result

- `tests/du_exact_marginal_extension_compiler.py` now accepts arbitrary finite
  alphabets and context hypergraphs and returns an independently verified
  exact primal extension or integer-normalized Farkas obstruction.
- The integrated probe passes `33/33` twice with byte-identical artifact
  SHA-256
  `a82702a50471cf1dfa28db00cfe0af62100cbb47b7ff78a0d286ab1a696747ad`.
- The general solver agrees with the prior signed-sector oracle on all `343`
  denominator-six binary-triangle marginals: `119` primal and `224` dual.
- Complete commuting QND parity instruments derive overlapping `AB` and `BC`
  regions, preserve relabeling and deterministic-relay quotienting, and
  separate an endpoint-equivalent rival on four of eight histories under the
  held-out causal break.
- The authenticated coarse gluing obstruction is absorbed by formed route
  provenance; the process is globally representable but carries equivocation
  requiring safe rejection.
- The complete Mermin--Peres instrument family returns the inclusion-minimal
  standard noncontextual gluing obstruction while context-indexed outcomes
  possess an exact normalized `4096`-support product extension.
- The durable next dependency is certified overlap identity before marginal
  descent, followed by sparse exact scaling and recursive vector-defect
  composition.
- No claim, prediction, ontology, physical law, paper priority, Factory state,
  submission, publication, or external action is promoted.
