# Run Plan: RUN-20260721-233305-causal-memory-frontier

Plan created at: 2026-07-21T23:33:05-05:00

Status: validated_pending_versioning

Run type: `progress`

Target: `dynamic-unity / SWING-DU-CMF-01`

Workflow: `orchestrated heterodox build-and-kill`

Workflow source: `Joe direct chat: "Orchestrate and take a big wild frontier heterodox swing" (2026-07-21)`

## Objective

Attempt a genuine class-relative escape from the COV-02 local-cone failure by
replacing the local counter with a retarded causal-memory object. Build and compare
the literal causal-past event count and a hyperbolic bi-wave memory proxy, then test
whether either produces late acceleration without becoming an always-on tracker or
silently importing the present cosmic age, a free history, or a fitted kernel.

## Governance References

- `AGENTS.md`
- `CONNECTIONS.md`
- `LANES.yaml`
- `lab/process/known-challenges-and-adversaries.md`
- `explorations/cross-repo-gap-hardening-and-nonintuitive-reuse-map-2026-07-21.md`
- `explorations/50-persona-blind-spot-register-and-priority-map-2026-07-21.md`

## Selected Work References

- `LANES.yaml`, Lane 2, Channels 2.2/2.3, with possible cross-feed to 1.4/1.5
- `explorations/lambda-N-covariant-perturbation-kill-2026-07-21.md`
- `tests/du_lambda_N_covariant_perturbation_kill.py`
- `tests/bianconi_entropy_amplitude_probe.py`

## Lane Selection

- Owner ID and scope: `dynamic-unity / repository`
- Primary Lane: `2`, Channels `2.2` and `2.3`
- Receiving surfaces if earned: Channels `1.4` and `1.5`
- Manifest revision and SHA-256: `6 / 799605dda2d230910588eca95f117b6328ce5ebb8a441963552b9818bb1a65e8`
- Definition/control head: `d2447ea7d5d81ec7ebe91de85c5f01d5f77cca14`
- Selection basis: `Joe requested an orchestrated frontier heterodox swing. The typed-N diagram is useful housekeeping, but this run instead attacks the largest surviving physical escape explicitly left open by COV-02.`
- Context capsule: `COV-01 conserved background; COV-02 local covariant lift killed except Lambda limit; literal causal-past count explicitly open; source-is-observer/external-involution work completed at the admission head.`
- Effective permission intersection: `repo-local research, probes, artifacts, Lane/hardening disposition, and routine GitHub versioning; no non-GitHub external writes.`
- Emergency-revocation evidence: `none; session sync start passed, main clean/even with origin/main at d2447ea.`

## Orchestration

- Physics attack: strongest explicit causal-past/retarded construction, equations,
  action caveats, principal support, and structural kill conditions.
- Empirical attack: minimal executable cross-era and history discriminators, positive
  controls, and signatures that cannot be tuned away cheaply.
- Systems/no-go-escape attack: class-relative exceptions, typed-object boundaries,
  sibling-repo reuse, and a ranked candidate tournament.
- All three attacks are read-only. The root pass alone owns writable surfaces and
  independently re-derives every load-bearing result before banking it.

## Recent Run Collision Check

- The only prior local Run Plan is the completed COV-02 run.
- A concurrent record-immutability/external-involution build was initially untracked,
  then completed and pushed as `d2447ea` before this run's sync start.
- That work changes only its named exploration/probe/artifact and does not overlap
  this run's causal-memory object, probe, artifact, Lane update, or Run Plan.
- Collision decision: `proceed on explicit non-overlapping paths`.

## Flow Patterns

- `frontier_abduction`
- `candidate_tournament`
- `proof_checking`
- `hostile_empirical_kill`
- `cross_repo_synthesis`

## Candidate Fork

1. `N_J(x)`: the literal unweighted event count in
   `J^-(x) cap J^+(Sigma_0)`, represented on FLRW by the exact light-cone moment
   hierarchy. This is the actual count candidate.
2. `N_box(x)`: a retarded bi-wave memory scalar proportional to
   `Box_ret^-2 1`, represented by two auxiliary wave equations with retarded
   initial data. This is a causal-memory proxy, not silently called an event count.
3. Offset, reset, weighted-kernel, stochastic-sign, and dynamically generated-scale
   variants are controls or escape classes. None is promoted merely by avoiding a
   deterministic tracker.

## Pre-registration

### Question

Can a retarded nonlocal `N` evade the local model's superluminal scalar cone and
produce a physically nontrivial late dark-energy era without arbitrary history,
an observer gradient, an inserted present-age scale, or a post-selected realization?

### Required build

- Exact FLRW equations and analytic power-law coefficient for `N_J`.
- Retarded auxiliary equations, analytic power-law coefficient, and principal
  characteristic statement for `N_box`.
- A complete phenomenological transfer vector or an explicit declaration that
  stress closure is absent; no partial `Q^0` masquerading as a model.
- Universal-coefficient cross-era calibration: fit a late matter-era fraction and
  predict the radiation-era fraction, then reverse the test under a deliberately
  weak early-fraction cap.
- Positive controls that recover Minkowski equality of the two memories, radiation
  and matter analytic coefficients, numerical light-cone quadrature, and a
  deliberately inserted offset that demonstrably can delay activation.
- A typed escape ledger: literal count, weighted memory, stochastic martingale,
  reset/prehistory, and generated spectral scale must remain distinct objects.

### Terminal outcomes

1. `PROMOTE_NONLOCAL_CANDIDATE`: causal support, stress closure, stable principal
   structure, standard early eras, and non-Lambda late prediction all survive.
2. `CAUSAL_ESCAPE_TRACKER_FAIL`: nonlocality genuinely escapes the COV-02 cone,
   but the unweighted/scale-free memory is an always-on tracker incompatible with
   a standard early era at the coefficient required for late acceleration.
3. `ONLY_STOCHASTIC_OR_GENERATED_SCALE_REMAINS`: deterministic positive branches
   fail; only a separately built stochastic-sign law or endogenously generated
   kernel scale remains open.
4. `REIMPORTS_COSMIC_CLOCK`: a reset, initial offset, or kernel timescale comparable
   to the present age does the phenomenological work.
5. `NO_COVARIANT_COMPLETION`: even the retarded construction cannot be made causal
   and Bianchi-compatible at declared grade.

### Hard kills

- A weighted or signed memory is not called an event count.
- Retarded support is not called ghost freedom or a fundamental action.
- A late calibration that forces order-one radiation-era vacuum fraction cannot be
  rescued by declining to calculate the early era.
- An initial `N_0` or kernel scale chosen so activation occurs near today is
  `REIMPORTS_COSMIC_CLOCK` unless sourced independently.
- Stochastic realizations are evaluated by ensemble-typical predictions; a lucky
  seed is not a sourced sign or coefficient.

## Expected Outputs

- One lossless frontier exploration containing the three independent attacks,
  candidate tournament, derivations, failures, and surviving escape envelope.
- One deterministic standard-library probe and frozen JSON artifact.
- Updated Lane/hardening dispositions that preserve all earlier persona items.
- A completed receipt in this Run Plan.

## Required Checks

- Syntax/static check of the probe.
- Deterministic repeated artifact generation.
- Independent analytic and quadrature checks of causal-volume coefficients.
- Positive and negative controls capable of flipping the tracker/clock verdict.
- Existing COV-02 and COV-01 regressions remain green.
- YAML and JSON parse, `git diff --check`, explicit changed-path review.
- Revalidate Lane hash, ownership, permissions, writer lock, directed flow, and
  emergency revocation before consequential effects.

## Source Boundary

- Primary literature may establish what causal-set, everpresent-Lambda, or nonlocal
  gravity authors actually constructed. It does not establish this run's equations.
- Sibling-repo results remain candidate fixtures until re-derived here.
- No full Boltzmann likelihood, quantum action, or ghost theorem is claimed from a
  homogeneous finite probe.

## External Action Authorization

`GitHub commit/push is authorized as routine versioning within Joe's approved repository work. No other external action is authorized.`

## Stop Conditions

- A new run or writer lock overlaps any declared writable surface.
- The model requires a free function, target `H(z)`, or selected realization before
  it has passed the structural tests.
- Local resource headroom becomes unsafe.

## Working Notes

- Physics attack returned an order-first martingale/common-past covariance branch,
  a de Sitter nonstationarity check, and an explicit action-closure warning.
- Empirical attack returned the exact conserved-fluid tracker pressure and a full
  radiation-to-present integration showing no standard matter interval and no
  present acceleration.
- Systems attack returned a class-relative no-go map and converged on the coupled
  `HC-DU-011 + HC-DU-022` order-first causal scale generator as the only high-value
  unbuilt escape.
- The root pass independently re-derived the load-bearing formulas, implemented
  every decisive discriminator, and retained the three attacks losslessly in the
  frontier exploration.

## Results

- Terminal result: `CAUSAL_ESCAPE_TRACKER_FAIL`, with the concise alias
  `PRINCIPAL_ESCAPE_BACKGROUND_KILL`.
- The literal causal-past count obeys an exact four-moment hierarchy. On
  `a(t)=t^p`, it is `N_J=C_J(p)t^4`, where
  `C_J=pi/[(1+3p)(1+p)(3+p)]`.
- The retarded bi-wave proxy has metric-null principal characteristics and
  therefore genuinely exits the local superluminal class killed by COV-02. It is
  nevertheless another degree-four memory, with
  `C_box=pi/[3(1+3p)(1+p)]` and `C_J/C_box=3/(p+3)`; it is not a literal count.
- Separate conservation forces `w_X=-1+(1/6)d ln N/d ln a`, hence exact radiation
  and matter tracking. A late-calibrated numerical history gives
  `Omega_X(z~1e9)=0.997256`, `Omega_X(z=1100)=0.989055`, maximum matter fraction
  only `0.223831` over `10<=z<=1000`, and `q_0=0.526224`.
- Giving the component vacuum stress does not rescue the least-arbitrary exchange
  closure: an exact radiation solution forces
  `Omega_r=1+3 Omega_Lambda` and `Omega_m=-4 Omega_Lambda`.
- The order-first signed martingale supplies a real half-power law and a derived
  common-past covariance, but a weak early RMS cap makes the late target a
  `210.27 sigma` event; calibration in the other direction makes radiation-era
  RMS `4.205`, and positive-sign survival from `z=2` is only `0.02358`.
- A new exact Minkowski result was derived and quadrature-checked:
  `rho(r/T)=1-r/T+(r/T)^3/4-(r/T)^4/16` for `0<=r/T<=2`, zero thereafter; its
  half-correlation separation is `r/T=0.5327705`.
- A positive offset can delay activation, but matching weak early density and the
  late target requires a clock `t_c/t_0=1.004278`, so the rescue imports the present
  cosmic age rather than explaining it.
- The surviving frontier is not a more decorative kernel. It is a label-invariant
  order-first causal growth/action model with a feedback/return arrow, recovered
  geometry, a causal response/noise action, and an endogenously generated
  dimensional memory scale.

## Receipt

Receipt created at: pending

Terminal outcome: `CAUSAL_ESCAPE_TRACKER_FAIL`

Pre-run target status: `COV-02 left causal-past and other nonlocal/UV completions OPEN; no separately recomputed background, cross-era tracker test, or retarded auxiliary candidate existed.`

Post-run target status: `The local cone obstruction was genuinely escaped, but the scale-free causal-memory background and raw martingale branches were killed. HC-DU-011 and HC-DU-022 are elevated together as the P0 surviving frontier.`

External actions performed: `None yet; GitHub result/receipt versioning pending.`

Artifacts changed: `LANES.yaml; the 50-persona preservation register; the cross-repo hardening/reuse map; the full causal-memory frontier exploration; this Run Plan; one executable probe; one frozen JSON artifact.`

Checks performed: `45/45 probe checks PASS; byte-identical deterministic rerun artifact SHA-256 55DD5242F55B1EB5DA80DFB7BE1111CBC96BC7A510716601A46434360EA811B4; COV-02 regression 18/18 PASS; COV-01 regression 18/18 PASS; 20k/40k history convergence 9.496e-11; YAML and JSON parse PASS; git diff --check PASS except expected line-ending notices.`

Lane revalidation: `Admission manifest revision/hash 6/799605dda2d230910588eca95f117b6328ce5ebb8a441963552b9818bb1a65e8; post-result manifest revision/hash 7/EA1820FC9BAC65493027F65B89CE64518743C2DFC321508126ECE58B46F61D37; ownership, permission intersection, writer collision, directed flow, and emergency-revocation checks remain clear before versioning.`

Follow-ups: `Build the coupled HC-DU-011 + HC-DU-022 order-first generated-scale frontier; complete the typed N/object/action diagram; seek independent reimplementation before raising the scientific grade.`

Outcome reason:

`Retarded causal memory really does cure the old principal-cone defect, so the prior
no-go was properly class-relative. But causality alone does not create the needed
cosmic timing: every built scale-free positive branch tracks the dominant era or
fails to accelerate, the raw order-first martingale is too noisy and sign-unstable,
and the one simple phenomenological rescue works only by inserting today's clock.`
