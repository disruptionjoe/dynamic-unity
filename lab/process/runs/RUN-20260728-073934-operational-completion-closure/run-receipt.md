---
title: "Operational completion-closure trichotomy — run receipt"
status: completed
doc_type: governed_run_receipt
created: 2026-07-28
run_id: RUN-20260728-073934-operational-completion-closure
work_id: OPERATIONAL-COMPLETION-CLOSURE-TRICHOTOMY
program_id: CCR-OPERATIONAL-COMPLETION-CLOSURE
claim_id: HC-DU-069
authority: "Joe direct chat: Go"
owner_repo: dynamic-unity
---

# Operational completion-closure trichotomy

## Receipt

The swing replaced Dynamic Unity's prior completion/escape binary with a
three-way operational classification:

```text
inside the completion class
  = absorbed

outside the class but inside its operational closure
  = limit-underdetermined

outside the operational closure
  = finitely certified escape
```

The run is complete. It required exact symbolic proof and analytic controls,
not a local model or external hardware. No physical successor is activated.

## Scientific return

`HC-DU-069` banks:

1. a finite record excludes a completion class exactly when the realized
   history lies outside that class's closure in the topology generated by the
   admitted finite records and actions;
2. every false history has a finite exclusion certificate exactly when the
   completion class is closed;
3. at least one zero-error finite falsifier exists exactly when the class is
   not dense;
4. enlarging observer capability refines the operational topology and can
   shrink the limit-underdetermined region;
5. bounded eventually-periodic generators provide a control where every
   fixed bound is finitely refutable while the union over all finite bounds is
   dense and proper;
6. computable histories are dense and proper, so no finite prefix proves
   noncomputability of its infinite continuation; and
7. the same closure criterion applies to finite adaptive experiments against
   completed intervention-response trees.

The core mathematics is absorbed by topology, safety/liveness,
monitorability, computability, and identification-in-the-limit. Dynamic
Unity's return is the typed application to completion classes, records,
capabilities, and the disclosure/escape boundary.

## Priority disposition

Read-only review of the newest GU Formalization, Time as Finality, and
Temporal Issuance evidence found no physical Dynamic Unity reopener:

- GU's selector ambiguity is already typed by `HC-DU-033C/F`;
- TaF's record naturality is provisional and downstream of supplied stores;
- TaF owns the preferred-foliation branch, which remains off in DU; and
- TI's fixed-oracle results motivate the closure control but select no
  physical oracle restriction.

The next physical reopener is therefore narrower: supply a physically
selected, non-dense completion class and one realizable finite record fibre
that remains disjoint from its strongest legitimate enlargement.

## Portfolio transition

`CURRENT-RESEARCH.yaml` advances from revision 17 to 18.

- `CCR-OPERATIONAL-COMPLETION-CLOSURE` is completed with `HC-DU-069`.
- No scientific successor is selected.
- `DU-PAPER-013-HARDENING` remains separately prepared and unchanged.
- The successor class
  `physically-restricted-nondense-completion-class-with-finite-separator` is
  recorded but inactive.
- The previously completed infrared audit remains durable historical
  evidence; its live program block is replaced to preserve the cold-start
  budget.

## Durable files

- `explorations/operational-completion-closure-trichotomy-and-bounded-unbounded-squeeze-2026-07-28.md`
- `explorations/concept-register.md`
- `docs/quantum-foundations-orientation-surface.md`
- `COUNTER-ASSUMPTIVE-FINDINGS-REGISTER.md`
- `CURRENT-RESEARCH.yaml`
- `tests/du_agent_orientation_contract_probe.py`
- `tests/artifacts/du_agent_orientation_contract_result.json`
- this run plan and receipt

## Validation

- `python3 tests/du_agent_orientation_contract_probe.py --write-artifact` —
  **PASS**, 35 checks, 220 unique counter-assumptive rows, 5,922/6,000
  cold-start words
- `python3 tests/du_hypothesis_efficient_approach_registry_probe.py` —
  **PASS**, 10/10
- `python3 tests/du_near_term_swing_approach_atlas_probe.py` — **PASS**,
  16/16
- `python3 tests/du_capability_indexed_north_star_probe.py` — **PASS**,
  20/20
- changed-document local links — **PASS**; the new local theorem link
  resolves and the theorem's literature references are external primary-source
  links
- `git diff --check` — **PASS**
