---
title: "Action-relative record materialization — run receipt"
status: completed
doc_type: governed_run_receipt
created: 2026-07-30
completed_at: "2026-07-30 12:31:14 CDT"
run_id: RUN-20260730-122318-action-relative-materialization
work_id: MPA-05-ACTION-RELATIVE-RECORD-MATERIALIZATION
action_id: MPA-05-ACTION-RELATIVE-RECORD-MATERIALIZATION
program_id: CCR-MINIMAL-PHYSICAL-ANTECEDENT-TO-FINITE-REMAINDER
authority: "Joe direct chat: Go"
owner_repo: dynamic-unity
claim_id: HC-DU-163
---

# Action-relative record materialization

## Disposition

```text
ACTION_RELATIVE_QUOTIENT
+ OBSERVED_MEAN_QUOTIENT_ONLY
+ MATERIAL_REALIZATION_NONIDENTIFICATION
+ ACTION_CLASS_REFINEMENT
+ PARTIAL_PHYSICAL_TYPING
+ KNOWN_RESULT_ABSORPTION
+ NO_READY_SUCCESSOR
+ SWING 6 NOT ACTIVATED
```

Swing 5 is complete at scoped Grade 4.

## Formal positive

For a frozen finite action class \(A\), complete histories have response
signature

\[
\sigma_A(h)=\left(K_a(h)\right)_{a\in A}.
\]

Equality of signatures induces the unique coarsest set-level behavioral
quotient, up to relabeling. A record \(r\) is response-sufficient exactly
when \(\sigma_A\) factors through it, and it realizes the quotient exactly
only when

\[
\ker r=\ker\sigma_A.
\]

Adding admitted actions can only refine this quotient.

## Material boundary

For a physical history \(H\) and retained record \(R=r(H)\),

\[
\mathbb P(Y_a\in\cdot\mid R)
=
\mathbb E[K_a(H)\mid R].
\]

An observed predictive law is therefore the mean response law inside the
record fibre. Physical sufficiency additionally requires zero within-record
response-law heterogeneity. Behavioral minimality separately requires the
record not to preserve response-irrelevant distinctions.

The Peronnin source reports \(95\%\) aggregate agreement for two successive
readouts after heralding on the first expected outcome. It does not report
the complete joined conditional response kernels or a factorization/
minimality test for \(V_1,\beta_1,\) or \(z_1\).

## Exact finite twins

The sixteen-history control constructs:

- one homogeneous completion with repeat propensity \(19/20\); and
- one heterogeneous completion with equal-weight propensities \(1\) and
  \(9/10\).

They have identical candidate-record distributions, identical joined
one-step response laws at label/statistic/trace levels, and identical
\(19/20\) aggregate repeat probability. Their physical quotients have two
and four classes respectively.

The label is exact in the homogeneous completion and coarse in the
heterogeneous completion. Statistic and trace are overfine in the first and
incomparable in the second. The label-conditioned response-propensity
variance is \(0\) versus \(1/400\). An idealized identity archive is
sufficient but overfine in both.

This is an exact nonidentification control, not evidence that the apparatus
contains the hostile latent coordinate.

## Absorber result

Minimal sufficient statistics, Blackwell comparison, computational-mechanics
causal states, predictive-state representations, minimal automata,
probabilistic bisimulation/lumpability, QND measurement tomography, and
process tensors absorb the general mathematics.

Dynamic Unity banks only the typed source-pinned boundary:

```text
observed-history predictive quotient
  != complete physical-history behavioral quotient
  != physically formed archive realizing either quotient
```

## Repository transition

- `HC-DU-163` is banked in the durable exploration.
- `CONCEPT-DU-019` records the action-relative materialization correction.
- `NI-DU-206` preserves the observed-mean-to-physical-sufficiency trap.
- The quantum-foundations orientation surface carries the scoped correction.
- `CURRENT-RESEARCH.yaml` advances from revision 116 to 117.
- The coherent campaign is parked with `NO_READY_SUCCESSOR`.
- Swing 6 is not eligible and was not activated.
- No hardware, simulation, provider, prediction, paper, or external action
  was activated.

## Validation

- `python3 tests/du_action_relative_materialization_probe.py
  --write-artifact` — **PASS**; quotient, exact twins, conditional
  heterogeneity, archive relations, and action extension.
- `python3 tests/du_minimal_antecedent_campaign_probe.py --write-artifact` —
  **PASS**; all 17 seeds and 10 conditional swing cards remain preserved.
- `python3 tests/du_agent_orientation_contract_probe.py --write-artifact` —
  **PASS**; 37/37 governance checks, 319 unique counter-assumptive findings,
  72 resolving current references, 38 resolving stable-entrypoint links, no
  active/executable scientific action, and 5,853/6,000 cold-start words.
- Python compilation of all three probes — **PASS**.
- Direct PyYAML revision, parked-program, `HC-DU-163`, null-next-action, and
  no-ready-successor assertions — **PASS**.
- `git diff --check` — **PASS** before receipt finalization.

## Exact reopener

Reopen only with a source-pinned packet supplying joined per-attempt records
and later responses, a predeclared intervention/tomography family, complete
archive semantics, and separate response-factorization and behavioral-
minimality tests. This receipt does not authorize Swing 6 or any later
campaign card.

## Durable files

- `explorations/action-relative-behavioral-quotient-observed-mean-and-material-realization-boundary-2026-07-30.md`
- `explorations/concept-register.md`
- `docs/quantum-foundations-orientation-surface.md`
- `COUNTER-ASSUMPTIVE-FINDINGS-REGISTER.md`
- `CURRENT-RESEARCH.yaml`
- `tests/du_action_relative_materialization_probe.py`
- `tests/artifacts/du_action_relative_materialization_result.json`
- `tests/README.md`
- this plan and receipt
