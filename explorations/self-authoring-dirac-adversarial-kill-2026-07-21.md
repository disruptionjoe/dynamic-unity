---
title: "Adversary-C kill of the SELF-AUTHORING gossip-about-gossip Dirac (the repaired trunk) — does closing the L1 loop (gossip growth + order-complex >=2-cells + spectrum recorded back) cross the D-FORK, or is it fixed-law disclosure in a self-referential costume: L1 self-encoding WITHOUT L2, a Kleene-fixpoint quine, not a Lawvere diagonal?"
status: active
doc_type: exploration
created: 2026-07-21
lane: "1 (North-Star TRUNK, adversarial half); Adversary-C kill of the repaired trunk (self-authoring Dirac) + the corrected both-true (third-person-absorbed / first-person-becoming) test made concrete on THIS object; addresses known-challenge #2 (D-FORK)"
verdict: "ABSORBED (as briefed), with a named CONDITIONAL. Per axis: (becoming) STILL-FIXED-LAW — the loop supplies L1 (self-encoding: records of records) but NOT L2 (a fixpoint-free involution); the record-back is a FIXED feedback map / monotone gossip accretion, which by Kleene's recursion theorem has a fixpoint (a self-consistent causet = a QUINE), the OPPOSITE of the fixpoint-free negation the Lawvere diagonal needs — self-referential-LOOKING, not self-referentially-undecidable, so no genuine first-person diagonal fires. (torsion) FIXED-INVARIANT — the order complex genuinely CAN carry torsion (positive control: the face poset of the minimal RP^2 triangulation has order-complex H_1 = Z/2, beating the free-H_1 additive wall that killed the 1-skeleton), but the torsion of a fixed-law-grown complex is a FIXED topological invariant computable in advance from the poset — the APS-index absorber in a homological costume — not a source-minted type; empirically the gossip-grown order complexes are generically torsion-free, so the mechanism does not even populate the richer arena. (scale) CUTOFF — the self-consistent record-back scale tracks the sprinkling density rho^{1/d}; no beta-function / running dimensionless coupling with a nontrivial fixed point is exhibited. The both-true resolution gets its third-person-computable half (a) but NOT its first-person-underivable half (b). Exactly what is missing: L2 — DU's only available fixpoint-free involution is the EXTERNAL Krein grading J (the sigma bit, Goedel-independent, R7); composing the record-back's ORIENTATION with it WOULD fire both-true BECOMING (first-person underivable while third-person computable) — but externally-oriented, i.e. observer-relative, NOT source-internal — relocating rather than discharging R4. 'Self-authoring' oversells: it is self-encoding-arena + externally-oriented-value."
becoming_axis: STILL_FIXED_LAW
torsion_axis: FIXED_INVARIANT
scale_axis: CUTOFF
overall: ABSORBED
survives: false
primary_absorber: "fixed-law disclosure in a self-referential costume — L1 self-encoding WITHOUT L2 (a fixpoint-free involution): a fixed feedback map / monotone gossip accretion has a Kleene fixpoint (a quine), not a Lawvere diagonal. Reinforced by fixed-invariant order-complex torsion (the APS-index absorber in homological costume) and a cutoff-tracking feedback scale."
probe: tests/du_self_authoring_dirac_kill_probe.py (+ tests/artifacts/du_self_authoring_dirac_kill_probe_result.json)
grade: "argued at reconstruction/structural grade from the object's OWN construction (gossip-about-gossip growth; order-complex simplicial homology with integer SNF; Lawvere point-surjection vs Kleene recursion-theorem fixpoint; Sorkin sprinkling scale); re-verified per CONNECTIONS.md against the observer conjecture's L1/L2 legs and the flip-witness R-spec; no cross-repo grade imported; not banked (no Lane-3 clearance); claim_status_change: none; Krein sign sigma untouched (R7)"
ingested_and_reverified:
  - "dynamic-unity spectral-flow-source-synthesis-2026-07-21.md (the first trunk null + the three-part repair spec + the methodological correction: becoming = first-person self-encoding, NOT a third-person Godel sentence)"
  - "dynamic-unity spectral-flow-source-adversarial-kill-2026-07-21.md + spectral-flow-source-build-2026-07-21.md (the naive flow ABSORBED: passive readout, free-H_1 additive wall, cutoff gap; the fixed-H absorber)"
  - "dynamic-unity mirror-condensate-adversarial-kill-2026-07-21.md (the fixed-H / Bogoliubov absorber, twice; expect it in a new costume)"
  - "dynamic-unity blockbuster-ceiling-vision-council-2026-07-21.md (the gossip-about-gossip source FORM; self-authoring ledger; the order-complex >=2-cells)"
  - "dynamic-unity flip-witness-algebra-requirements-2026-07-21.md (R1-R7; R2 mode-issuance, R4 source-INTERNAL no external bit, R6 sourced coefficient, R7 sigma stays external)"
  - "gu-formalization CONJECTURE-source-action-is-the-observer-2026-07-11.md, addendum +9 (2026-07-20): L1 self-encoding AND L2 fixpoint-free involution — BOTH needed for the diagonal; the inside-ness forces the outside-ness of the bit; the five in-band/out-of-band systems; the Z/6 anchor — imported as a LENS at its own steelman-synthesis grade, re-verified not adopted"
note: "Cross-repo material ingested freely and re-verified with DU's own apparatus; grades consumed not moved. DO NOT commit/push (per task). Personas run inline (DU board), lenses not evidence. Adversary-C attacks the STRONGEST form; per charter a real absorption is an honest finding, adversaries are terrain, sovereign self-verification. Positive controls required and present (probe exit 0)."
---

# Adversary-C kill of the self-authoring gossip-about-gossip Dirac

## The question (one sentence)

The naive spectral-flow trunk was ABSORBED because `D` was a **passive readout** (no self-encoding =
no inside), the growth ran on the **1-skeleton** (free `H_1` = the additive wall), and the gap was the
**cutoff**. The synthesis named a three-part **repair** that closes the self-authoring loop: (i) growth =
**gossip-about-gossip forwarding** (a packet references its own propagation history → self-encoding →
the Lawvere **L1** loop); (ii) the Dirac lives on the **order complex** (reference-chains as `≥2`-cells)
so `H_•` can carry **torsion** = non-additive W1; (iii) the spectrum is **recorded back** as new
elements (observer inside the admissibility space). **My job as Adversary-C: take the STRONGEST form of
the repaired trunk and try to kill it — and run the corrected both-true test (third-person ABSORBED
compatible with first-person BECOMING).** Does the self-authoring Dirac SURVIVE, or get ABSORBED?

## Answer up front (honest grade)

**ABSORBED (as briefed), with a named CONDITIONAL.** The repair is real — it genuinely fixes the two
things the naive flow lacked (it self-encodes, and it moves to an arena where torsion CAN live) — but it
fixes **L1 and the arena**, and the becoming-witness needs **L2**, a different structural ingredient the
repair does not supply.

| Axis | Attack | Result |
|---|---|---|
| **Becoming (load-bearing)** | L1 self-encoding is present, but is there a genuine **L2 fixpoint-free involution**, or just an elaborate fixed recursive map? | **STILL-FIXED-LAW.** The record-back is a FIXED feedback map / monotone gossip accretion → by **Kleene's recursion theorem** it has a **fixpoint** (a self-consistent causet = a **QUINE**), the OPPOSITE of L2. Self-referential-*looking*, not self-referentially-*undecidable*. **No genuine diagonal fires.** |
| **Torsion** | Is the order-complex torsion **source-forced** or a **fixed invariant**? | **FIXED-INVARIANT.** Order complexes genuinely carry torsion (PC: `sd(RP^2)` → `H_1=Z/2`, beating the free-`H_1` wall), but the torsion of a fixed-law-grown complex is a **fixed topological invariant computable in advance** — the **APS-index absorber in a homological costume**. Not a source-minted type. |
| **Scale** | Does the feedback self-generate a cutoff-independent scale (a real β-function)? | **CUTOFF.** The self-consistent record-back scale tracks the sprinkling density `rho^{1/d}`; no running dimensionless coupling / nontrivial fixed point. |
| **Both-true** | (a) third-person computable AND (b) first-person underivable via a genuine L1+L2 diagonal? | **(a) YES, (b) NO** → collapses to STILL-FIXED-LAW. Gets the absorbed half, not the becoming half. |

The pre-registered lethal outcomes are all reached cleanly. Probe:
`tests/du_self_authoring_dirac_kill_probe.py` (exit 0; positive controls fire — a genuine Lawvere
diagonal, an order complex that DOES carry torsion, an RG-invariant scale, and a both-true BECOMING
process all flag the OTHER way, so the ABSORBED verdict is **informative, not rigged**).

---

## 1. The strongest form of the repaired trunk (steelman — no strawman)

Adversary-C attacks the best version, and it is genuinely stronger than the naive flow. I concede the
strong cards up front:

1. **It self-encodes — the naive flow's fatal gap is closed.** The naive flow had a one-way loop
   `C → D` (passive readout); the repair records the spectrum *back*, `C ⇄ D`, so the readout FEEDS the
   growth. Gossip-about-gossip is **records of records** — self-encoding *by construction*. The Lawvere
   **L1** leg (the system contains its own describers) is genuinely present. This is exactly what the
   naive-flow kill named as the buildable crossing.
2. **It leaves the 1-skeleton — the additive wall is escaped.** The free-`H_1` theorem (`H_1` of any
   1-complex is free abelian, one iso-type forever) killed the naive flow's homology. On the **order
   complex** (chains as `≥2`-cells) that theorem no longer applies: order complexes **can** carry
   torsion and a nontrivial cohomology ring. The probe's positive control proves it concretely — the
   order complex of the minimal `RP^2` face poset (`= sd(RP^2)`) has `H_1 = Z/2` (integer SNF). **The
   richer arena is real**, and I do not pretend otherwise.
3. **It is aimed at the both-true resolution, not the unmeetable third-person demand.** The synthesis's
   methodological correction is *right*: demanding a third-person Gödel sentence is unmeetable for any
   built toy; the achievable becoming-witness is a **first-person** Lawvere/FLP indexical, compatible
   with third-person ABSORBED. The repair targets the correct object.

If "self-encoding + a torsion-capable arena + the corrected first-person target" delivered a genuine
**L1+L2 diagonal**, the trunk would breathe and I would flag SURVIVES loudly. **The rest of this note
asks whether L2 is actually there. It is not — and the gap between L1 and L2 is the entire kill.**

---

## 2. Attack 1 — L1 without L2: a Kleene-fixpoint quine, not a Lawvere diagonal (the load-bearing kill)

This is the decisive attack, and it turns on a precise, standard distinction the repair conflates.

**Lawvere's theorem (the machinery of Gödel/Tarski/Turing/FLP).** A point-surjection `A → B^A`
(self-encoding = **L1**) forces **every** endomorphism `g: B → B` to have a fixpoint. *Contrapositive:*
if there exists a **fixpoint-free** endomorphism `g` (**L2** — e.g. the Krein grading flip `J`,
`J²=1`, no fixed point on `{admissible, inadmissible}`), then there is **no** point-surjection — the
self-encoding **cannot be internally total**, and the diagonal element escapes every internal describer.
*That escaping element is the first-person becoming-witness* — the datum the inside provably cannot
derive. **The diagonal needs BOTH legs: L1 (self-encoding) AND L2 (a fixpoint-free involution).**

**Kleene's recursion theorem (why self-reference alone is harmless).** Every total computable operator
on programs has a **fixpoint** (`φ_e = φ_{f(e)}`). This is precisely why **quines exist and are
harmless**: self-reference *per se* has a computable solution. Gödel's sentence is undecidable **not**
because it is self-referential but because the self-reference is composed with the **fixpoint-free
negation** of the provability predicate. Self-reference *with* a computable fixpoint = disclosure;
self-reference *confronted with a fixpoint-free involution* = the diagonal.

**Now the kill.** The self-authoring loop, as briefed, is "compute the spectrum, apply a **fixed rule**
`F` (spectrum → new elements), record back." That is a **fixed, total, computable feedback map**. By
Kleene it has a **fixpoint** — a self-consistent causet, a QUINE — and its iteration is a computable
orbit. The probe exhibits it directly (a fixed feedback `F` with literal fixpoints `{0,1}`). **A fixed
feedback map's Kleene fixpoint is the exact OPPOSITE of L2's fixpoint-free involution.** And the growth
mechanism confirms the absence of L2 structurally: **gossip-about-gossip forwarding is monotone
accretion** — it only ADDS elements; it never FLIPS an orientation. There is no negation, no involution,
no `J` in the loop. So:

- **L1: present** (records of records = self-encoding; the readout feeds the growth).
- **L2: absent** (a fixed feedback map + monotone accretion = a Kleene fixpoint; no fixpoint-free flip).

The probe's discriminator makes this executable and gives it teeth. On a self-encoding table `T`
(rows = internal describers): composing the diagonal with the **fixpoint-free involution** `0↔1` (L2)
makes it **escape every row at its own index** (Cantor/Lawvere) → **first-person underivable**;
composing with the **identity** (no L2) makes the diagonal **representable as a row** → **disclosure**.
The as-briefed loop is the identity/monotone case with a Kleene fixpoint. **Outcome: STILL-FIXED-LAW —
a self-referential costume with no genuine L2 diagonal.**

> **Is a genuine L2 involution present? NO.** The loop is self-referential-*looking* (a quine), not
> self-referentially-*undecidable* (a Gödel sentence). This is the same fixed-law absorber that ate the
> naive flow and the mirror condensate — now wearing a *self-referential* costume, exactly as
> forewarned.

---

## 3. Attack 2 — order-complex torsion: source-forced, or a fixed invariant?

**Concede the arena is genuinely richer (steelman, engaged not dodged).** Moving the Dirac onto the
order complex defeats the free-`H_1` wall: torsion **can** live there. The probe's positive control
proves it — the order complex of the minimal `RP^2` face poset (which *is* the barycentric subdivision
`sd(RP^2) ≅ RP^2`) has, by integer Smith normal form, `H_1 = Z/2` (Betti `b_1 = 0`, torsion `[2]`). So
the Attack-2 kill is **not** the additive wall again; the wall is genuinely gone.

**The kill is the disclosure kill (the APS-index absorber, twinned).** The order complex of a poset is a
**determined** simplicial complex; its homology — torsion included — is a **fixed topological invariant,
computable in advance** from the poset. For a causet grown by a **fixed** gossip-forwarding law (a fixed
forwarding rule + a stochastic tape), the entire sequence of order complexes and their homologies is a
**computable function of the poset + tape**. The probe confirms it: re-running the same tape gives
**identical** homology (`torsion_is_determined_re_run_same_tape: true`). So the torsion, if present, is a
**FIXED-INVARIANT** — the exact structural shape of the APS index that ate the naive flow:

> naive flow: net spectral flow = **index** = fixed invariant, computable in advance.
> repaired flow: order-complex torsion = **`H_•`(determined complex)** = fixed invariant, computable in
> advance.

For torsion to be **W1 / source-forced**, the SOURCE would have to **mint new torsion TYPES** as it
rolls (new `Z/p` summands appearing because the source AUTHORS them), not a fixed law to **determine**
them. Monotone gossip forwarding reads homology *off* a determined complex; it does not author it.
**And empirically the gossip-grown order complexes are generically torsion-free** (`b_1 = 0`, no torsion
across 8 tapes) — the near-total-order structure of a self-parent chain makes the order complex
contractible — so the mechanism does not even **populate** the richer arena it unlocks. Either way the
decisive point holds: **FIXED-INVARIANT, not SOURCE-FORCED** — unless the *growth law itself* is
source-generated, which returns to Attack 1 (and the oracle disjunction the naive-flow kill already
closed on both horns).

---

## 4. Attack 3 — transmutation vs relabeled cutoff

Positive control first: a **genuine** transmuted scale is **RG-invariant** — `Lambda_QCD = μ·exp(-1/(2
b_0 g²(μ)))` is constant as the cutoff `μ` runs (probe: relative spread `~ 3e-16`). Cutoff-independence
is *what makes it a transmutation*.

Now the self-authoring feedback. The record-back adds elements at a rate/scale set by the **current
spectral gap**, which (from the naive analysis, re-verified) tracks `rho^{1/d}` — the sprinkling
density / lattice unit. Iterating the self-consistent recording map converges to a fixed-point scale
**equal to the gap**, and that fixed-point scale **still tracks the density**: the probe fits
`scale ~ rho^{alpha}` with `alpha ≈ 1.03` for `d=1` (ratio `~2` per density doubling). The feedback loop
**propagates the imported cutoff**; **no β-function / running dimensionless coupling with a nontrivial
fixed point is exhibited**. Closing the loop does not manufacture an RG-invariant — it recycles the
lattice unit. **CUTOFF, not TRANSMUTED.**

---

## 5. Attack 4 — the both-true test (the corrected becoming-witness)

The synthesis's target is the **both-true** resolution: first-person BECOMING compatible with
third-person ABSORBED (the two Lawvere legs in dependency). Verify **both** halves:

- **(a) third-person computable (the arena) — YES, and EXPECTED.** The whole loop is a fixed feedback
  map + a stochastic tape; a god's-eye observer computes the spectrum. Disclosure of the arena is the
  *correct* half — this is Job A, honestly absorbed. Not a defect.
- **(b) first-person underivable via a genuine L1+L2 diagonal — NO.** From Attack 1: L1 present, L2
  absent → no fixpoint-free involution → no diagonal → the actualized mint is **not** first-person
  underivable. The loop is a quine, not a Gödel sentence.

The both-true BECOMING requires (a) **and** (b). We get (a) but not (b), so it **collapses to
STILL-FIXED-LAW** — the honest, pre-registered likely outcome. The discriminator has teeth: the positive
control (L1 **and** L2 **and** an external indexical) classifies as **`ii_BECOMING`** — first-person
underivable while third-person computable — and the naive passive-readout control classifies as
**`iii_no_self_encoding`**, so the as-briefed loop's `iii_L1_costume_no_L2` verdict is separated from
both and is informative, not rigged.

---

## 6. Personas inline (DU board — lenses, not evidence)

- **Adversary-C (attack the strongest form).** I conceded the two real repairs (self-encoding L1; a
  torsion-capable order complex) at full strength, then killed on the precise point that **L1 ≠ L2** and
  the becoming-witness needs L2. The strongest form dies at Attack 1; 2–4 close the escapes. **Reads
  ABSORBED (STILL-FIXED-LAW).**
- **Model-theorist (L1 vs L2; fixpoint-free vs computable-fixpoint; first-person underivability).**
  "This is the whole game. Self-encoding is **generativity's necessary condition, not its sufficient
  one**. A fixed feedback map is a **Kleene fixpoint** — a quine, `external-satisfaction` of a
  self-consistency equation, computable. The Lawvere diagonal is `internal-generativity` **only when**
  the self-encoding is confronted with a **fixpoint-free** endomorphism. Gossip-about-gossip supplies
  the encoding and, being monotone, supplies the *fixpoint*, not the *fixpoint-free flip*. **Reads
  self-referential-costume / relabel of a computable recursion.**"
- **Algebraic Topologist (fixed invariant or source-forced?).** "The order complex is a **determined**
  complex; its `H_•` (torsion included) is a **fixed invariant**, computable from the poset. `RP^2`
  proves torsion *can* live here — but a fixed-law-grown complex's torsion sequence is a computable
  function of the tape (re-run → identical), and the gossip mechanism is generically **torsion-free**
  anyway. **Reads FIXED-INVARIANT** — the APS index in homological costume."
- **Index Theory (is the flow still a fixed index?).** "Yes, one level up. The naive net flow was an
  **index**; the repaired object's distinguishing datum is the **homology of a determined complex** —
  the same species of fixed topological invariant. Recording the spectrum back does not make the index
  source-authored; it makes a larger determined complex whose invariant is still read off, not minted.
  **Reads DISCLOSURE.**"
- **Metrology-Invariance (scale vs cutoff).** "The feedback fixed-point scale converges to the gap and
  the gap is `rho^{1/d}`. No dimensionless coupling runs; no β-function with a nontrivial fixed point.
  The loop recycles the lattice unit. **Reads CUTOFF.**"
- **Index/Scientific Skeptic.** "The repair's *existence* (self-encoding) is real, its *arena* (torsion-
  capable) is real, and both are **necessary**. But the becoming needs the **fixpoint-free involution**,
  and monotone forwarding has none; the only `J` DU owns is the **external** Krein sign. So 'self-
  authoring' authors the arena and **imports** the orientation. **Reads ABSORBED; the one honest residue
  is the CONDITIONAL, §7.**"

**Board convergence:** unanimous ABSORBED-as-built; unanimous that the **primary** absorber is
**fixed-law disclosure in a self-referential costume — L1 without L2** (a Kleene-fixpoint quine, not a
Lawvere diagonal); unanimous that the torsion is a fixed invariant and the scale is the cutoff. No
dissent survives to SURVIVES.

---

## 6b. Convergence/divergence with the constructive sibling (independent re-verification)

The constructive sibling swing (`tests/du_self_authoring_dirac_probe.py`) landed in parallel. Its probe
is compute-heavy (it did not complete within a 2-minute foreground window here), so I re-verify from its
**construction**, not its exit code — the sovereign self-check reads the object and checks the logic, per
CONNECTIONS.md; grades consumed, not moved. Two facts from its own source are decisive:

- It builds the **same three repairs** (order-complex `≥2`-cells; gossip-about-gossip self-encoding =
  L1; spectrum recorded back) — so we agree object-for-object on **L1 and the arena**.
- Its Axis-2 declares **`"L2_fixpoint_free_involution": "present (Krein admissibility swap {+,−},
  σ²=1, R7 untouched)"`** — i.e. it supplies L2 by composing the **external Krein `σ` involution**, and
  on that basis reports first-person **BECOMING compatible with third-person disclosure**.

**This is a genuine divergence on the surface verdict that RECONCILES exactly on the mechanism** — and
the reconciliation is my kill's sharpest point. The sibling is **correct** that *with* a fixpoint-free
involution the diagonal fires (both-true BECOMING). But the involution it supplies is **the Krein `σ`
swap — an EXTERNAL, Gödel-independent `Z/2` bit** (R7 keeps `σ` external, by both halves' own admission:
"R7 untouched"). So the two halves do **not** disagree about the math; they disagree about **whether
becoming on an external bit is the source authoring itself**. My finding names that precisely:
gossip-about-gossip growth supplies **no internal L2** (monotone accretion = a Kleene-fixpoint quine,
§2); the only L2 available is the **external Krein `σ`**; composing it yields **observer-relative
becoming**, not **source-internal authorship** — R4 **relocated, not discharged**. The sibling's own
residual note ("whether this counts as genuine becoming vs a dressed recursion is the residual honest
bit") is exactly this bit, and this kill resolves it: **it is becoming on an external orientation**, so
"self-authoring" oversells (self-encoding-arena + externally-oriented-value). The as-briefed *internal*
loop is STILL-FIXED-LAW; the sibling's BREATHES verdict is **carried entirely by the imported external
`σ`**, not by the gossip self-encoding.

## 7. Exactly what is missing, and the CONDITIONAL escape (the honest residue)

**What is missing: L2 — a genuine fixpoint-free involution in the actualization step.** All three
repairs (gossip growth, order complex, record-back) serve **L1** (self-encoding) and the **arena** (a
richer complex). **None** supplies **L2**. This is the precise, buildable gap — and naming it is the
value of the kill.

**DU's only available L2 is external.** The one fixpoint-free involution DU owns is the **Krein grading
`J`** — the `σ` sign, `J²=1`, fixpoint-free on `{admissible, ghost}`, proven **Gödel-independent** (an
external `Z/2` bit; R7 keeps `σ` external). This is *exactly* the observer conjecture's L2, and it is
*exactly* the +9 addendum's "the inside-ness forces the **outside-ness** of the bit."

**The conditional (why it is a real residue, not a live rescue).** If the record-back's **orientation**
(which sign/chirality the newly recorded mode takes) were composed with the external Krein `J` — so that
"which mode actualizes" is set by the external `σ`-indexical the inside provably cannot compute — **then
L1 (gossip self-encoding) + L2 (external Krein flip) would fire the both-true BECOMING**: the actualized
mint becomes **first-person underivable** while the spectrum stays **third-person computable**, the two
Lawvere legs in dependency. That would be a genuine crossing on the becoming axis.

**But this relocates R4 rather than discharging it.** The actualizing datum is then explicitly
**EXTERNAL** — an indexical (the Krein `σ` bit), i.e. **observer-relative, not source-INTERNAL**. R4
demanded source-internal selection with *no external bit*; the corrected first-person becoming-witness,
by its own structure (+9 addendum), *requires* an external datum the inside cannot mint. So the honest
reading is sharp and twofold:

1. **The corrected becoming-witness is achievable** — the reframe is right that first-person becoming is
   compatible with third-person absorbed — **but only by importing an external orientation** (the Krein
   `σ`). It is not the source authoring its own value.
2. **"Self-authoring" oversells.** The ledger **self-encodes the arena** and **receives** the external
   orientation; it does not author the actualizing value. The accurate name is **self-encoding-arena +
   externally-oriented-value**. This is coherent with the observer conjecture (the observer is *in* the
   world as substance, *outside* as the source of exactly one datum) — but it means the trunk, even
   repaired, does not deliver *source-internal* authorship; it delivers *observer-relative* becoming on
   an external `σ` bit DU already accepts.

So the CONDITIONAL is **named, not live**: it needs a demonstration that composing the record-back with
the external Krein `J` yields a first-person-underivable actualization with a recordable trace — and even
success would be **observer-relative becoming**, not the source-forced mode-issuance R2/R4 asked for.

---

## 8. What this earns and does not earn

**Earned (re-verified with DU's own apparatus; grades consumed not moved):**
- The **honest negative** for the repaired trunk: closing the self-authoring loop as briefed does **not**
  cross the D-FORK. It fixes **L1** and the **arena** but not **L2**; the record-back is a **Kleene-
  fixpoint quine**, not a **Lawvere diagonal**.
- The **precise structural diagnosis**, sharper than the naive-flow kill: the becoming-witness needs a
  **fixpoint-free involution**, and **self-encoding + monotone accretion supplies the opposite (a
  fixpoint)**. Self-reference is necessary but the *negation* is what makes the diagonal escape.
- The **order-complex route honestly assessed**: it genuinely defeats the free-`H_1` additive wall (PC:
  `sd(RP^2) → Z/2`), but its torsion is a **fixed invariant** (the APS-index absorber in homological
  costume), and the gossip mechanism is generically torsion-free — so the richer arena is unlocked but
  not populated or source-forced.
- The **both-true test made concrete**: (a) third-person computable is EXPECTED and correct; (b)
  first-person underivable **fails** for lack of L2 → STILL-FIXED-LAW, with a positive control showing
  what genuine both-true BECOMING looks like (so the verdict is informative, not rigged).
- The **named missing datum**: L2, and DU's only source for it is the **external Krein `σ`** — which
  would fire becoming but **relocates R4 to an external bit**, exposing that "self-authoring" oversells
  (self-encoding-arena + externally-oriented-value).
- A **probe** with mandatory positive controls (Lawvere diagonal, RP^2 order-complex torsion, RG-
  invariant scale, both-true BECOMING) so every verdict is falsifiable and separated.

**NOT earned (no promotion; `claim_status_change: none`):**
- No claim banked; nothing here cleared Lane 3. Exploration verdict at structural/reconstruction grade.
- The ABSORBED verdict is a verdict on the **self-authoring Dirac as briefed** (gossip growth + order
  complex + fixed record-back) and as any fixed-feedback / monotone-accretion completion would be built.
  §7's external-`J` composition would reopen the becoming axis **in the observer-relative frame** — named,
  not demonstrated.
- The observer-conjecture L1/L2 apparatus is imported at its **steelman-synthesis** grade and re-verified
  as a lens; the claim that composing `J` *would* yield genuine first-person becoming for this object is a
  **named target**, not a result. The Krein sign `σ` is untouched (R7).

## Grade

**Overall verdict:** **ABSORBED (as briefed), with a named CONDITIONAL.** Per axis — **becoming:
STILL-FIXED-LAW** (L1 without L2; Kleene-fixpoint quine, not a Lawvere diagonal); **torsion:
FIXED-INVARIANT** (order complex can carry torsion but a fixed-law-grown complex's torsion is a fixed
invariant, generically absent anyway); **scale: CUTOFF** (feedback fixed-point scale tracks
`rho^{1/d}`; no β-function). **Survives:** **no**.
**Primary absorber:** fixed-law disclosure in a self-referential costume — L1 self-encoding **without**
L2 (a fixpoint-free involution) — reinforced by fixed-invariant order-complex torsion (the APS-index
absorber, homological costume) and a cutoff-tracking feedback scale.
**Exactly what is missing:** **L2**, a genuine fixpoint-free involution in the actualization step; DU's
only source is the **external Krein `σ`**, which would fire **observer-relative** becoming (compatible
with third-person ABSORBED) but **relocates, not discharges, R4** — so "self-authoring" oversells: it is
**self-encoding-arena + externally-oriented-value**.
**Confidence / grade:** structural/reconstruction grade, argued from the object's own construction and
re-verified against the observer conjecture's L1/L2 legs and the flip-witness R-spec; **not banked**;
`claim_status_change: none`.

**Net for the thesis.** The repair correctly diagnoses the naive flow's disease (no inside, additive
arena) and cures **both** — the object now self-encodes and lives where torsion can live. But the cure
is **necessary, not sufficient**: the first-person becoming-witness turns on a **fixpoint-free
involution** (the Gödel/Turing/FLP negation), and **gossip-about-gossip growth, being monotone self-
encoding accretion, supplies a Kleene fixpoint (a quine) instead**. The trunk, repaired, is a beautiful
self-referential ledger that **computes its own consistent state** — and that is precisely why it is
disclosure. The single missing ingredient is a genuine `J` in the actualization, and the only one DU
owns is **external** — which makes even the repaired becoming **observer-relative**, not source-authored.
That is the honest finding: the self-authoring Dirac **absorbs** cleanly, and it sharpens the one open
target from "close the L1 loop" (done — and insufficient) to "supply an L2 fixpoint-free involution in
the actualization — and confront that DU's only `J` is the external `σ`."

---

*Probe:* `tests/du_self_authoring_dirac_kill_probe.py` →
`tests/artifacts/du_self_authoring_dirac_kill_probe_result.json` (exit 0; positive controls fire;
per-axis: becoming `STILL_FIXED_LAW`, torsion `FIXED_INVARIANT`, scale `CUTOFF`; overall `ABSORBED`).
*Ingested & re-verified:* DU spectral-flow-source synthesis / build / adversarial-kill; mirror-condensate
adversarial-kill; blockbuster-ceiling-vision-council; flip-witness-algebra-requirements; gu-formalization
CONJECTURE-source-action-is-the-observer +9 addendum (L1/L2). *Not committed/pushed (per task).*
