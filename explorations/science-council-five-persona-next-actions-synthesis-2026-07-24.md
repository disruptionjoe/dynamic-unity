---
title: "Science Council five-persona next-actions synthesis"
status: completed_council_synthesis
doc_type: council_synthesis
created: 2026-07-24
directed_by: "Joe direct chat, 2026-07-24"
contract: explorations/science-council-five-persona-next-actions-contract-2026-07-24.md
predecessor: explorations/physical-influence-selector-wave-synthesis-2026-07-24.md
divergent_index: explorations/science-council-five-persona-divergent-wishlist-index-2026-07-24.md
lanes: "1 / 2 / 3 / 4 / A"
verdict: "PHY-02 CENTRAL VERDICT UPHELD / FOUR SCOPES NARROWED / NAIVE COUNT-RUNNING KILLED / NEXT PROGRAM STAGE-GATED"
banked: false
seeded: false
---

# Science Council five-persona next-actions synthesis

## Executive decision

Five independent Science Council persona agents re-audited
`SWING-DU-PHY-02`, contested one finding each, produced sequenced roadmaps,
hypothesis and stop ledgers, and preserved an unconstrained divergent wish
list.

The central physical disposition survives:

> **Live formalization components exist, but no concentration functional,
> physical observable identity, unit-bearing nonzero scale, or `Lambda` value
> has been identified.**

The council work changes the surrounding research state in five useful ways:

1. The generic replication signature and terminal normalized residue do not,
   by themselves, strengthen the complete `CONCEPT-DU-001` invariant.
2. `TRANSIENT-ONLY` is scoped to fixed-target closed Bianconi relaxation;
   externally driven open dynamics can sustain raw dissipation, but imports its
   cadence.
3. Response, embedding, and units are a conjunction at the
   physical-identification claim gate, not an indivisible gate on building the
   pieces sequentially.
4. `HC-DU-011 + HC-DU-022` remains the right next **program**, but is not one
   build-ready ticket.
5. The first build must test full path covariance. Statewise permutation
   invariance alone can hide a birth-order clock.

The immediate next product is therefore:

> **`HC-DU-011A`: a full causal-growth admission passport plus an exact
> small-unlabeled-causet covariance harness.**

No claim is banked or seeded.

## The five independent dispositions

| Persona | Contested swing | Result | Unique consequence |
|---|---|---|---|
| Orthodox professor | Ordinary quadratic normalization/relaxation null | `6/6`; reproduces the point residue and exact `(1,-1/2,-1/2,0,-1)` replication signature | Narrow the PHY-02 concept increment to live, concept-compatible components; retain Gini and residues as diagnostics |
| Heterodox professor | Rotating anisotropic open Bianconi drive | `10/10`; both completions sustain nonzero periodic `D` with work–dissipation balance | Narrow `TRANSIENT-ONLY` to fixed-target closed relaxation; keep the scale leg open because the drive cadence is imported |
| Commercial scientist | Dependency and units audit of `HC-DU-011 + HC-DU-022` | Analytic audit | Preserve the program priority but split it into stage-gated products; require a generated hierarchy relative to one declared microscopic unit |
| Philosopher of science | Separability witness for response `R`, embedding `E`, and units `U` | Analytic variance response orders the frozen incomparable pair before a units map exists | Treat `R ∧ E ∧ U` as a promotion conjunction, while allowing separately graded intermediate research |
| Wild-frontier scientist | Fisher/CSG/count-scale bridge with exact path-covariance kill | `12/12`; constructive skeleton works, but direct cardinality running gives path probabilities `0.118190, 0.118190, 0.078793` for one unlabeled causet | Kill naïve `p(N)`/`g(N)` running; strengthen “label invariant” to full natural-labeling path covariance |

The council did not vote. Root accepts each correction because its derivation or
probe is independently checkable and its scope is compatible with the others.

## What remains unchanged

- The Euclidean and affine Bianconi profiles exactly decompose their own
  completion-specific action dissipation on `D>0`.
- Their fixed-target closed relaxations drive raw `D` to zero and leave the
  exact stationary profile undefined.
- The record score-energy profile exactly decomposes observed residual energy,
  not Fisher information.
- The participation/Shannon versus Gini split does not select a functional.
- Normalized shape alone cannot restore common magnitude, units, or causal
  provenance.
- The old incomparable pair still lacks a physical ordering in the completed
  PHY-02 wave.
- No tested construction identifies `Lambda`, its coefficient, a dimensional
  scale, or a cosmological observable.
- Standalone proxy, endpoint-map, coefficient, and `rho` proliferation remains
  stopped.

## Corrections and newly identified opportunities

### 1. Factor the concept grade

The orthodox null shows that the two visually striking signatures in PHY-02
are generic:

```text
point-like normalized residue while raw activity vanishes
replication signature (D,lambda_2,lambda_H,G,max p)
                    ~ (K,K^-1/2,K^-1/2,K^0,K^-1)
```

They occur in an ordinary quadratic relaxation with no higher-order influence
mechanism, response law, units map, or cosmological identity. PHY-02 therefore
constructed necessary concept-compatible components; it did not independently
test the full concept invariant.

The lossless current grade is:

```text
LIVE_INFLUENCE_OBJECT_EXISTENCE = CONSTRUCTIVELY_REALIZED
DU_MECHANISM_LINK               = CONDITIONAL / COMPLETION-DEPENDENT
CONCEPT_RELEVANCE               = STRUCTURAL_SUPPORT
FUNCTIONAL_SELECTOR             = OPEN
PHYSICAL_OBSERVABLE_IDENTITY    = OPEN
DIMENSIONAL_SCALE               = UNASSESSED
LAMBDA_VALUE                    = OPEN
```

Earlier structural support remains; the object result is not erased.

### 2. Preserve open-system dissipation without pretending it is a scale

For a rotating anisotropic target, the heterodox construction satisfies

```text
dS/dn = -D_X + P_drive.
```

At drive rate `omega=0.4` per event:

```text
Euclidean    <D>=0.1716510  <P_drive>=0.1716488
affine-SPD   <D>=0.1768233  <P_drive>=0.1768200.
```

The tail is periodic and `D>0`; the two completions remain distinct. At slow
rates, `D` approaches the expected `O(omega^2)` response.

This is a real counterexample to an unqualified “Bianconi dissipation is always
transient” statement. It is not self-organization or dimensional
transmutation: anisotropy and cadence are imported. Its best use is an exact
work ledger for testing whether a future native growth law genuinely drives
the geometric state.

Carry three separate objects forward:

```text
D        raw activity
p_i      normalized allocation
a_i=Dp_i raw modal activity.
```

### 3. Stage the influence reopener

Let:

```text
R = independently motivated response law
E = matched physical embedding of a held-out disagreement pair
U = unit-bearing observable map.
```

The philosopher's variance-response witness shows that `R ∧ E` can, in
principle, select a functional before `U` exists. Conversely, a raw carrier and
`U` can test a scale without selecting a functional.

The corrected rule is:

- partial stages may be built, tested, killed, and recorded at their honest
  grades;
- `R ∧ E` is required for a physical selector result;
- `R ∧ E ∧ U` is required before candidate physical identification;
- robustness, rivals, and independent hardening are still required before any
  bank review.

`NOT_EVALUABLE` should always name the exact question and missing objects. It
is a design receipt, not a synonym for false.

### 4. Correct dimensional-transmutation language

Dimensionless order data can generate a dimensionless hierarchy or crossover,
but not physical units ex nihilo. A successful program must declare one
microscopic unit such as `ell_atom` and generate a regulator-stable large
ratio:

```text
ell_* = ell_atom F(order couplings and invariants).
```

The real naturalness burden is that `F` be generated, robust under admissible
blocking/regulator changes, and not an inserted `H0`, `N0`, window, reset, or
target epoch.

Accordingly, `HC-DU-022` seeks:

> **an RG-stable memory hierarchy relative to a declared microscopic unit,**

not an absolute unit from dimensionless order alone.

### 5. Strengthen causal-growth covariance

The wild-frontier construction joins:

- transitive-percolation births;
- a transition-law Fisher/KL metric;
- a coordinate-covariant natural mobility;
- an algebraic invariant `N_*=N exp(-g)`; and
- normalized expected Fisher contributions of physical down-set outcomes.

It also supplies the decisive kill. With constant `p`, three natural labelings
of the same three-event causet have identical probability `0.143883`. With
`g(N)=-log(16)+log N`, they receive:

```text
0.118190
0.118190
0.078793.
```

The final unlabeled causet remembers its birth order. The exact formalization

```text
FORMALIZATION-WF-01:
direct cardinality-running transitive-percolation logit
```

is therefore killed.

This establishes a new admission rule:

> Statewise relabeling equivariance is necessary but insufficient. A candidate
> must give equal total path weight to every natural labeling of the same
> physical causet, or explicitly make the differing update records physical
> state.

The same finite model exposes a new conditional opportunity: causal closure is
a data-processing channel that redistributes and can reduce expected Fisher
response. Its retained-Fisher fraction may become a mechanism-native response
diagnostic inside an admissible growth law. It is not a new free-standing
proxy or `Lambda`.

## Integrated next-actions roadmap

### Stage 0 — freeze a factored epistemic and object passport

**Output.** Separate fields for state, transition law, covariance,
mechanism-link, response, embedding, raw carrier, units, scale, selector,
value, and robustness.

**Stop.** Once the passport prevents existence-to-identity and
statewise-to-path-covariance conflations, stop refining taxonomy.

### Stage 1 — build `HC-DU-011A`, the full covariance harness

**Output.**

- finite unlabeled causet state;
- admissible one-element extensions;
- Markov normalization;
- statewise isomorphism equivariance;
- equal path weight across every natural labeling;
- spectator/Bell-causality behavior;
- past immutability; and
- a typed distinction between gauge bookkeeping and physical coupling records.

Enumerate the smallest causets exhaustively and preserve the first
counterexample, not just a pass count.

**Immediate controls.**

- constant-`p` transitive percolation;
- the killed direct `p(N)`/`g(N)` schedule;
- a metric-seeded or target-scheduled law; and
- fixed-history replay.

**Stop.** Any birth-label-dependent path weight kills that exact law before
geometry or cosmology.

### Stage 2 — admit only two covariant feedback/scale routes

1. **Covariant-landmark renormalization.** Start from a fixed path-covariant
   growth law and transform effective couplings only at order-defined
   landmarks, such as posts, with blocking-universality controls.
2. **Physical update records.** Add minimal causal records whose content
   changes subsequent transitions, so differing update histories are different
   physical orders rather than gauges.

**Output.** Explicit law, source/resource ledger, feedback-off ablation,
regulator sequence, and generated dimensionless hierarchy relative to one
declared atom unit.

**Stop.** No third family until these are either survivors or fail for
different localized reasons. Kill schedules keyed directly to birth count,
future data, desired dimension, or cosmological epoch.

### Stage 3 — derive response and mobility from each survivor

**Output.**

```text
q_y(C)       physical transition probabilities
F_ab(C)      Fisher/KL or other derived response metric
I_y          expected outcome contributions
K_response   retarded response
K_noise      positive covariance/noise
D,p,a_i      work/dissipation telemetry where defined.
```

Compare Fisher-natural, Euclidean, and affine-SPD mobilities only on a common
typed state. Preserve the empirical record score-energy profile separately
from expected transition Fisher information; their mismatch may diagnose
nonequilibrium or misspecification.

**Stop.** Kill a mobility selected for a desired concentration, a response
that is only the chosen functional restated, or a feedback result matched by a
fixed-law replay.

### Stage 4 — demand a geometry basin and generated hierarchy

**Output.** Multi-size, multi-seed, neighboring-law results for:

- Myrheim–Meyer dimension;
- longest-chain scaling;
- width/layer and interval-abundance observables;
- one held-out geometry estimator;
- transfer/response-operator gap;
- blocking/regulator stability; and
- increment covariance/correlation class.

**Stop.** A tuned crossing at one `N`, a cutoff-tracking gap, a target-used
geometry estimator, or a correlation class that destroys the claimed
half-power.

### Stage 5 — execute selector and units legs

**Output.**

1. derive one held-out response without naming participation, Shannon, or Gini;
2. construct matched physical histories realizing a frozen disagreement pair;
3. predict the response ordering before evaluation;
4. map the surviving raw response into the generated scale's units; and
5. run common-rescaling, time-reparameterization, exact-duplication, and
   calibration-removal controls.

**Stop.** If the response is insensitive, embedding-dependent, or
completion-reversed, keep the selector open. If the scale comes from `H0`,
`N0`, a present-age window, or a fitted conversion, kill the scale leg.

### Stage 6 — only then build cosmological completion

**Output.** Stress-consistent retarded response plus positive noise, finite-`k`
stability, common-past spectrum, early-history behavior, sign persistence,
de Sitter limit, and one held-out prediction against constant `Lambda`,
everpresent-`Lambda`, RR/nonlocal, and fixed-history rivals.

No likelihood run is warranted before Stages 1–5 survive.

## Hypothesis and finding ledger

| ID | Statement | Current disposition | Decisive next test | Forbidden overclaim |
|---|---|---|---|---|
| `COUNCIL-F1` | Identical replication forces `(1,-1/2,-1/2,0,-1)` for the declared objects | `DERIVED / GENERIC NORMALIZATION RESULT` | New definition or non-identical generated growth only | Evidence for a DU mechanism or `Lambda` |
| `COUNCIL-F2` | A slow normalized residue can occur while raw activity vanishes | `CONSTRUCTIVELY REALIZED IN ORDINARY NULL` | Predict actual mobility-Hessian residues ex ante | Persistent stationary influence |
| `COUNCIL-F3` | Open anisotropic Bianconi driving can sustain `D>0` | `CONSTRUCTIVELY REALIZED / DRIVE-IMPORTED` | Native causet driver and analytic stability | Generated scale or self-organization |
| `COUNCIL-F4` | `R`, `E`, and `U` are stage-separable | `ANALYTICALLY SUPPORTED` | Actual successor dependency audit | Any stage is already physically realized |
| `COUNCIL-F5` | Direct cardinality-running transitive percolation is path covariant | `FORMALIZATION-FALSIFIED` | Reopen only with physical update records or a different covariant law | Statewise invariance repairs it |
| `COUNCIL-H1` | A fixed CSG law can have a covariant post-to-post effective flow | `CONJECTURE / OPEN` | Small-causet exact transformation and repeated blocking | Generated hierarchy already exists |
| `COUNCIL-H2` | Physical coupling records can close the return arrow | `CONJECTURE / OPEN` | Explicit resource-bearing record law with path covariance and intervention difference | Feedback equals source ownership |
| `COUNCIL-H3` | Causal closure's retained Fisher response can diagnose order dynamics | `CONDITIONAL CONSTRUCTION` | Admissible growth law plus held-out response | New proxy, selector, scale, or `Lambda` |
| `COUNCIL-H4` | Transition information geometry can select a Bianconi mobility | `STRUCTURAL CONJECTURE` | Derive common SPD state and predict the completion before trajectories | Affine mobility is presently selected |
| `COUNCIL-H5` | A generated transfer-operator gap can break the scale-free tracker failure | `CONJECTURE / OPEN` | Regulator-stable gap, geometry basin, early/late controls | Finite matrix gap is dimensional transmutation |
| `COUNCIL-C1` | PHY-02 constructed live influence components but identified no selector or scale | `POSSIBLE SCOPED CLAIM / NOT BANKED` | Independent implementation and claim audit | No selector or scale can exist |

## Stop ledger

Stop now:

- additional normalized influence proxies, endpoint maps, `rho` variants, and
  coefficient sweeps;
- further identical-block or iid Gaussian sample-size sweeps;
- more fixed-target Bianconi relaxation on the same fixture;
- arbitrary moving-target drives after the one scope counterexample;
- averaging Euclidean and affine completions;
- any transition coupling scheduled directly by birth count;
- calling statewise permutation invariance discrete general covariance;
- passive gaps reported only in cutoff units;
- geometry tuning at one cardinality;
- generic scale-free memory kernels, selected stochastic signs, offsets, and
  present-age windows;
- cosmological calibration or likelihood before the causal-growth and
  generated-scale stages survive; and
- monolithic implementation of all frontier obligations at once.

These are scoped stops. Each persona memo preserves exact reopeners.

## Underused outcomes retained

The current work supplies more than a failed `Lambda` route:

- Gini and participation/Shannon prefactors can diagnose a future raw carrier
  law or effective dimension.
- Terminal residues and approach rates can diagnose slow-subspace degeneracy
  and mobility.
- Completion disagreement can become a held-out constitutive response test.
- `D`, `p`, and `a_i=Dp_i` form a work/allocation telemetry tuple.
- Exact duplication diagnoses lost causal provenance as well as lost scale.
- Expected transition Fisher information and empirical score energy can be
  compared as a model-misspecification or nonequilibrium alarm.
- `NOT_EVALUABLE` can be stored as
  `(question, missing objects, cheapest resolving test)`.
- Full path covariance can be expressed as a local holonomy/cocycle condition,
  offering a possible analytic alternative to exhaustive enumeration.

None is promoted to a physical identity.

## Divergent-custody rule

All persona-specific wish-list items remain lossless in their source memos and
are indexed in
`science-council-five-persona-divergent-wishlist-index-2026-07-24.md`.
Clustering or future prioritization may route them but must not erase their
individual provenance.

Every item remains:

> **`UNRANKED / UNCOMMITTED / NOT_A_CLAIM`.**

## Reproduction

New bounded probes:

```bash
.venv/bin/python tests/du_orthodox_normalization_null_probe.py
.venv/bin/python tests/du_heterodox_driven_bianconi_probe.py
.venv/bin/python tests/du_wild_frontier_fisher_csg_scale_bridge_probe.py
```

Results:

```text
orthodox null          6/6
heterodox open drive  10/10
wild-frontier bridge  12/12
```

The check counts establish execution only. The commercial and philosopher
swings are analytic. No council count or agreement is scientific evidence.
