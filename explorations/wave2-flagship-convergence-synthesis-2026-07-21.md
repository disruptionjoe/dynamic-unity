---
title: "Wave-2 flagship convergence synthesis — four swings reduce to one priced unlock (source-forced mode-issuance), and the static GU substrate structurally forbids it"
status: active_research
doc_type: synthesis
created: 2026-07-21
lane: "cross-lane synthesis over Lane 1.1 (condensate build + kill) and Lane 2.1/1 (Bet #1 measurement, Bet #2 low-entropy origin)"
verdict: "CONVERGENT. As built, DU's N=e^{4p} roll is FTS/disclosure: the mirror-sector condensate — the wave's convergent candidate for the source-forced count — is ABSORBED (fixed-H/Bogoliubov), confirmed object-for-object by the independent build (PARTIAL, both win-bars fail) and kill (ABSORBED) halves. Genuine becoming, the native DE-amplitude VALUE, measurement's Born+single-outcome legs, and the low-entropy SELECTION are one unlock, now priced to a hard spec."
grade: "synthesis over four honestly-graded swings; no claim banked or moved (claim_status_change: none across all four); the operative-C / Krein-sign bit (W211 Gödel-independent) untouched"
inputs:
  - explorations/mirror-condensate-algebra-growth-build-2026-07-21.md  # PARTIAL — one-shot genesis, both win-bars fail
  - explorations/mirror-condensate-adversarial-kill-2026-07-21.md      # ABSORBED — fixed-H/Bogoliubov
  - explorations/bet1-measurement-via-records-finality-2026-07-21.md   # SPLIT — 2 relocations, Born + single-outcome fail
  - explorations/bet2-low-entropy-origin-bottom-of-roll-2026-07-21.md  # PARTIAL — 3-row co-location, selection-forcing = the D-FORK win
---

# Wave-2 flagship convergence synthesis

Four pre-registered swings ran in parallel against the object the first wave converged on:
a **source-forced, non-additive, record-ALGEBRA-growing count** — the single object that was
claimed to (a) flip the D-FORK from disclosure to genuine becoming, (b) supply the non-additive
count that sources the DE-amplitude VALUE, (c) ground the measurement basis. Two swings went at
the mirror-sector condensate (a constructive build and an adversarial kill); one each went at the
two new Lane-1 bets (measurement via records/finality; low-entropy past = the bottom of the roll).

They converge. This is the synthesis; the four source docs carry the detail and the probes.

## 1. The convergent verdict — as built, the roll is FTS/disclosure

The mirror-sector condensate was the wave's best candidate for the source-forced count. The two
halves, run **independently**, agree object-for-object:

- **Build (PARTIAL).** The `ker(Γ)` BCS condensate is real: gapped spectrum `±√(ξ²+Δ²)`, a
  self-consistent gap `Δ*=0.62`, `E_cond<0` (condensed = true vacuum), and the exact
  `Sp(32,32;H)→Sp(32)×Sp(32)` isotropy jump matching W224 to the integer. Its one genuine escape
  from *pure* disclosure: the symmetric phase has the good-stable set `F` **empty**, so
  condensation is an admissibility-predicate **genesis** (`F: empty→nonempty`), and the branch is
  not action-computable — both distinguish it from vanilla SSB. But it fails **both** win-bars, for
  computed reasons: (1) **not productive** — the genesis is monotone/absorbing (compact-image
  absorption), so it fires **at most once**, count `0→1`, caps at 1 → bounded → a fixed `A_∞`
  absorbs the roll → FTS; (2) **not source-forced** — the selector is the *one Krein sign W211
  proved Gödel-independent of GU*, external not B5-generated.
- **Kill (ABSORBED).** The **fixed-H / Bogoliubov absorber** eats it. A Bogoliubov/BdG map is a
  (Krein-)unitary automorphism of a **fixed** CAR algebra over the 96 null pairs of the fixed
  `Cl(9,5)=M(64,H)` — the generator set is invariant, so a fixed `A_∞` factors every `A_n`. The
  condensed vacuum being *unitarily inequivalent* is a **state/representation** change, not algebra
  growth (W1 not delivered). The "new predicate" (W2) is the **pre-existing Cartan involution `P`**
  (W219 wrote it down pre-dynamics) — the condensate *satisfies* an old predicate, it does not
  *author* a new one (relabel). Source-forcing dies on an exhaustive disjunction: either a posited
  fixed action (`fixed_source`) or the unbuilt action as a completed oracle (`completed_history`) —
  both horns kill it.

Independent build + independent kill, converging on ABSORBED, is the strongest honest form of the
result: even the swing trying to *construct* the witness, graded honestly, finds it absorbed.

## 2. The one unlock — every swing reduces to the same missing object

| Swing | Grade | What it delivered | What it did NOT deliver | The gate |
|---|---|---|---|---|
| Condensate (build+kill) | PARTIAL / ABSORBED | one-shot admissibility genesis; precise localization | productive, source-forced algebra growth | fixed-H absorber |
| Bet #1 measurement | SPLIT | pointer-basis + tensor-split reframe (relocation grade) | Born (μ is Born-blind) + single-outcome | the D-FORK becoming-win |
| Bet #2 low-entropy | PARTIAL | 3-row co-location at N_min; common root = finality/irrevocable accretion | forces only the arrow *direction*, not the *selection* | the D-FORK becoming-win |

The structural/discrete legs **reframe**; the continuous/quantitative legs **do not come
natively** — the same fingerprint as the type wall. And the missing legs are not four separate
problems: Bet #2 states it exactly — *"the bet's selection win and the D-FORK win are one win."*
Genuine becoming, the DE-amplitude VALUE, measurement's Born + single-outcome, and the low-entropy
selection are **one unlock**.

## 3. The unlock, priced to a hard spec (the wave's most valuable output)

Both condensate halves converge on the exact needed datum. The flip-witness is no longer "build
the condensate" (done, absorbed) — it is:

> **A B5 source action whose flow ISSUES NEW PHYSICAL MODE CONTENT — grows `ker(Γ)` (new null
> pairs) productively as `p` rolls — not access-revealed from a fixed CAR algebra, with a
> recordable trace and no completed oracle.**

This single object would flip the D-FORK to genuine becoming, source the non-additive DE-amplitude
VALUE, ground measurement's Born + single-outcome legs, and force the low-entropy selection — one
build, the whole flagship.

**Two honest headwinds (named, so this is a target not a hope):**
1. **SSB/Bogoliubov is mode-*reshuffling* on a fixed algebra — it IS the fixed-H trap, not an
   escape from it.** Survival needs mode-*issuance*, a strictly stronger claim than any
   condensation/symmetry-breaking move.
2. **The static GU substrate structurally forbids it.** `ker(Γ)` is a fixed 96 null pairs of the
   fixed `Cl(9,5)=M(64,H)`; growing the mode space is exactly what the static substrate cannot do.

## 4. The sharpest GU/DU boundary result

Headwind 2 is a genuine finding, and the clearest statement yet of *why DU cannot come from GU
alone*: **the productive mode-issuance the whole flagship needs is precisely what GU's fixed
Clifford substrate forbids.** The dark-energy VALUE, genuine becoming, and the hard measurement
legs all require the mode space to *grow*; GU's is fixed by construction. Dynamics is not an
optional enrichment of the static geometry here — it is the only place the required growth can
live. This is the load-bearing motivation for the program, now earned rather than asserted.

## 5. What is banked at honest grade (survivors)

- **A localization at reconstruction/structural grade:** the mirror-sector condensate is
  FTS/disclosure — both win-bars fail — converged by independent build and kill. (No claim moved.)
- **The flip-witness sharpened to a falsifiable spec:** productive `ker(Γ)` mode-issuance, no
  oracle, recordable trace. The Lane-1.1 target.
- **The "one unlock" consilience:** D-FORK win = DE-VALUE win = measurement-hard-legs win =
  low-entropy-selection win.
- **The GU/DU boundary result:** the static GU substrate structurally forbids productive
  mode-issuance (§4).
- **Bet #1's two relocations** (record-finality pointer-basis; record-relational tensor-split) at
  relocation grade — better than σ's word-match, at parity with quantum Darwinism plus one genuine
  DU delta, carried with the quantum-mereology (Carroll–Singh) circularity flag.
- **Bet #2's 3-row co-location** at N_min with the finality/irrevocable-accretion common root, at
  PARTIAL (the singularity/curvature facet rides W151 + confronts the live T223 no-go).

## 6. The empirical seam (candidate, carried with its blocker)

A discriminator against unitary QM lives *not* in the vanilla double-slit (where DU = QM by
construction; delayed-choice needs no retrocausality — no-signaling keeps the marginal a blob) but
in the **reversible-measurement / few-copy-redundancy** regime: tune the number of which-path
environmental copies `R` and measure interference visibility `V(R)` and recoverability. Standard
decoherence → smooth crossover, always in-principle recoverable at unitary cost. A DU finality that
is a *sharp irreversible-accretion threshold* → a knee at `R_c` and un-recoverability beyond the
unitary cost. Closest existing formalism to the "the record is already fixed in the global causal
set" intuition: the two-state-vector / retrocausal-consistent reading (operationally visible in
weak-measurement). **Conditional and not live:** it is a deviation from unitary QM, DU-as-built does
not predict it (μ is Born-blind; the count is one-shot), and it fires only if the §3 productive
count is built. Value: it gives the flip-witness its first *empirical* target — a measurable knee
in `V(R)`-recoverability — the natural Lane-4 prediction-with-blocker.

## 7. Adversaries

Observer-gradient falsifier — beaten across the wave (homogeneous temporal boundary). Block-universe
absorber — survives on the structural claims only; every "genuine becoming" gloss is *not* earned
as built. D-FORK — **not beaten; it is the load-bearing cap** on the whole flagship, and the wave
localizes exactly what beating it requires (§3). Fixed-H/Bogoliubov — the primary absorber; it wins
the win-question against the current condensate.

## Boundary

Synthesis over four in-repo swings, each honestly graded with a probe; no claim banked (no Lane-3
clearance), no grade moved, `claim_status_change: none`. Cross-repo objects (W173/W211/W216/W219/
W224, TI CompletionClass, TaF arrow-as-theorem) were ingested and re-verified per CONNECTIONS.md,
never adopted on a sibling's say-so. The operative-C / Krein-sign bit stays external and untouched.
