---
title: "Bet #2 — are the low-entropy past, the initial singularity, and the arrow ONE object (the bottom of the roll, N_min)? Testing whether N_min FORCES the low-entropy SELECTION or only re-describes the DIRECTION σ already gives. Verdict: PARTIAL. The three rows genuinely CO-LOCATE at one locus (N_min), with a real common root — finality / irrevocable accretion — so the collapse is a real structural economy of DESCRIPTION. But the deep test fails as built: whether N_min FORCES the low-entropy SELECTION (sub-maximality — the Penrose 1/10^10^123 'why so special', the thing σ could not reach) is decided ENTIRELY by whether the accessible phase space GROWS from ~0 at N_min (Gödelian/issuance → the Past Hypothesis DISSOLVES) or is FIXED-large from the start (FTS/disclosure → the Past Hypothesis STANDS). DU's own D-FORK resolution already found the roll is FTS/disclosure AS BUILT — so as built N_min re-describes the direction's origin and does NOT force the selection; the selection-forcing is gated on the SAME source-forced-algebra-growth witness as the D-FORK win. The singularity/curvature facet is PARTIAL and worse than merely exploratory — it rides W151 (HALF-NATIVE) and confronts the LIVE T223 no-go (the uniform finite finality-colimit ensemble does not concentrate on manifoldlike causal sets). Beats the observer-gradient adversary; survives the block-universe absorber only on the STRUCTURAL claim (not the 'becoming' gloss); is capped by the D-FORK as built."
status: active_research
doc_type: exploration
lane: "1 (candidate bet #2, from Lane-2.1 sweep); addresses the σ direction-vs-selection fence and known-challenges #1/#2/#5/#6"
created: 2026-07-21
directed_by: "Joe direct chat via coordinator, 2026-07-21 (pre-registered Lane-1 swing, new Bet #2: test whether the low-entropy past, the initial singularity, and the arrow are ONE object — the bottom of the roll N_min; establish entropy_min ⟺ N_min ⟺ origin is forced not assumed; test whether N_min FORCES the low-entropy SELECTION, not just the direction; flag the singularity facet PARTIAL honestly; must beat the observer-gradient + block-universe absorbers). DO NOT commit/push."
grade: "PARTIAL — three rows co-locate at one locus (real economy; common root = finality); the low-entropy SELECTION is NOT forced by N_min as built (it is decided by the phase-space-growth fork, and DU-as-built is FTS/disclosure = the fixed column, where N_min only re-describes the direction's origin); singularity facet PARTIAL and T223-blocked. Not REFRAMES (no clean forcing of the selection); not STILL-INHERITED (genuine description-collapse + a real, gated route to the selection)."
verdict: "PARTIAL. FORCED: N_min ⟺ roll-origin ⟺ S_ext-min (direction / absolute-entropy leg; trivial). NOT FORCED AS BUILT: the low-entropy SELECTION (sub-maximality — the Penrose content σ could not reach). REACHED only in the Gödelian/growing-phase-space regime DU has not built, gated on the same witness as the D-FORK win."
probe: "tests/du_low_entropy_origin_bottom_of_roll_probe.py (foreground; exit 0; forced-leg + selection-fork with teeth + positive control + observer-gradient control). Artifact: tests/artifacts/du_low_entropy_origin_bottom_of_roll_probe_result.json"
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
inputs:
  - dynamic-unity/explorations/dynamic-tension-reframe-sweep-2026-07-21.md (Bet #2 detail; the σ direction-only fence)
  - dynamic-unity/explorations/d-fork-regime-resolution-2026-07-21.md (DU's own finding: the roll is FTS/disclosure as built — LOAD-BEARING here)
  - dynamic-unity/explorations/bianconi-entropy-amplitude-route-2026-07-21.md (extensive entropy S ~ N; fluctuation ~ √N; record-change ≠ finality without μ)
  - dynamic-unity/explorations/sibling-repo-harvest-2026-07-21.md (the standing adversaries)
  - time-as-finality/open-problems/arrow-of-time-as-constructor-theorem.md (TaF's OWN direction-vs-selection split; "low-entropy Big Bang = constraint on accessible records, NOT the source of direction"; conditional grade)
  - time-as-finality/explorations/records-as-rows-spacetime-from-attention-2026-07-10.md (time = record accumulation; the T223 manifoldlikeness no-go)
  - gu-formalization/explorations/W151-gr-and-c-emergence-from-records-2026-07-14.md (emergent GR/c from records; HALF-NATIVE; monotone-record arrow)
  - gu-formalization/explorations/council-dynamic-unity-assessment-2026-07-21.md (N = e^{4p} common variable; σ = w1(L_time) time-orientation; DE magnitude 1/√N = PURE IMPORT)
  - gu-formalization/explorations/W155-ten-divergent-personas-tachyon-2026-07-14.md (roll = record-growth is PLAUSIBLE / conjecture)
posture: >-
  Open exploration; derivation is NOT the only standard; honesty bites at the banking gate; the
  adversaries are terrain to beat, not gates. Sovereign self-verification (CONNECTIONS.md): every
  cross-repo grade below — the σ type wall, the arrow-as-constructor conditional, W151 HALF-NATIVE,
  the T223 no-go, the Sorkin 1/√N import — is CONSUMED as a hypothesis re-verified here, never
  adopted on a sibling's say-so. DU's own D-FORK verdict is a DU finding, re-checked from its
  construction, not imported. Nothing banked; this faces Lane 3 before any count.
self_check: >-
  The one place this document leans hardest is DU's own D-FORK resolution (FTS/disclosure as
  built). That is re-verified here from the construction — a fixed B5 action generating a monotone
  4-volume of one atom-type, grading vacuum unbuilt (W224) — not taken on authority; if that
  construction changes (the §7 flip-witness is built), the selection-forking column flips with it.
  The probe carries its own positive control so the FTS verdict is informative, not rigged.
---

# Bet #2 — the low-entropy past, the initial singularity, and the arrow as one object (N_min)

## Bottom line up front

**GRADE: PARTIAL.** Decomposed against the three questions the swing pre-registered:

1. **Is `entropy_min ⟺ N_min ⟺ the roll's origin` FORCED or ASSUMED?**
   **FORCED — but only on the leg that does not matter.** `N_min ⟺ roll-origin` is forced cleanly
   (an accretion count is monotone and bottoms out at its infimum; DU's own construction has "the
   4-volume only grows"). `S_ext-min ⟺ N_min` is forced for the **extensive / absolute** record
   entropy `S_ext ~ N` (Bianconi's `σ·Tr ln G` is a sum of `N` local log-volume terms, so it is
   extensive and monotone in `N`). **But the Past Hypothesis is not about absolute entropy.** It is
   about **sub-maximality** — the state being far below the *maximum available* entropy at that
   scale (Penrose's smoothness / low gravitational entropy, the `1/10^{10^{123}}` fine-tuning). And
   `N_min` forcing low *absolute* entropy is trivial (a small system has little entropy); it does
   **not** force sub-maximality. So the forced leg is the trivial one.

2. **Does `N_min` FORCE the low-entropy SELECTION (the thing σ could not reach), or only re-describe
   the DIRECTION σ already gives? → It only re-describes the direction, AS BUILT.** The selection
   is decided by exactly one variable — **does the accessible phase space (hence `S_max`) grow from
   `~0` at `N_min` (records/d.o.f. accrete: Gödelian/issuance) or is it fixed-large from the start
   (FTS/disclosure)?** In the growing column the Past Hypothesis **dissolves** (no room for a
   fine-tuned corner in a phase space that does not exist yet); in the fixed column it **stands**
   (the origin is an improbable corner and `N_min` merely says *where* that corner sits). **DU's own
   D-FORK resolution already found the roll is FTS / fixed-H / disclosure AS BUILT.** That is the
   fixed column. So **as built, `N_min` does not force the selection** — it re-describes the
   direction's origin — and the selection-forcing is **gated on the same source-forced-algebra-growth
   witness** the D-FORK win requires.

3. **The singularity/curvature facet — PARTIAL, honestly, and worse than merely exploratory.** It
   rides W151 (emergent GR/c from records, self-graded **HALF-NATIVE**, "NOT evidence") and confronts
   the **live T223 no-go**: the uniform finite finality-colimit ensemble does **not** concentrate on
   manifoldlike causal sets. So the record substrate does not even yield a manifoldlike spacetime in
   the smooth regime, let alone reframe the strong-field singularity. PARTIAL, blocked by a named
   in-house negative.

**Does it collapse the three ledger rows?** **Yes, as a co-location of three DESCRIPTIONS at one
LOCUS** (`N_min` = low-N end = low-absolute-entropy end = geometric origin), with a genuine common
root — **finality / irrevocable accretion** (records don't un-happen, so the count bottoms out at
zero and grows one way). That is a real structural economy. It is **not** a derivation of the hard
selection content, and the "these are one object because the origin *forces* all three" reading does
not close as built.

**Does it beat the adversaries?** **Observer-gradient falsifier: beaten** (a spatially homogeneous
temporal boundary predicts no observer-centred spatial gradient). **Block-universe absorber:
survived on the STRUCTURAL claim only** — the co-location is a fact about the block's structure, true
in B-theory; the "`N_min` = origin of genuine BECOMING" gloss is **not** survived (DU's D-FORK file
found the roll is disclosure as built). **D-FORK: not beaten** — it is the load-bearing cap on the
whole selection-forcing. **record-change ≠ finality: the actual arrow (genuine irreversibility) needs
μ, unbuilt** — `N`-counting gives monotone growth (direction), not finality.

Net: **PARTIAL** — genuinely more than a re-description (the three-row co-location is real, and the
tiny-phase-space *dissolution* is a real route to the selection), genuinely less than a REFRAME (the
route is gated on an unbuilt Gödelian witness, and DU-as-built sits on the wrong side of the fork).

---

## 1. The exact bet, and where the honesty bites

The claim: the past hypothesis is **not** a separately-posited boundary condition — in the
record-roll `N = e^{4p}`, entropy is minimal exactly where the record-count is minimal, so the
low-entropy start, the Big-Bang singularity, and the arrow are three faces of one object, the
**bottom of the roll `N_min`** (arrow = the roll's monotone growth priced by μ).

The σ-council's sharp fence (re-verified from `council-dynamic-unity-assessment`,
`council-trit-resolution`): **σ is the time-orientation `Z/2` `= w1(L_time)`**; by the proven type
wall it "owns only signs" and **cannot touch a magnitude, a dynamical basis, a count, or a vacuum
selection**. So σ fixes the arrow's **direction** (a sign) and leaves the low-entropy **selection**
(which special state, and how special) untouched. The bet is only interesting if `N_min` reaches
that untouched selection. Re-describing the direction's origin is not enough — σ already had the
direction.

**The anti-toy blade (pre-registered).** "The roll starts low so entropy is low" is **circular**
re-description unless the roll's origin is *independently* forced to be low-entropy **in the
Past-Hypothesis sense**. And the Past-Hypothesis sense is **not** low absolute entropy — every
small/young system trivially has little total entropy — it is **sub-maximality**: `S(N_min) ≪
S_max(N_min)`, the state far below the maximum it *could* have had (Penrose: the early universe was
smooth = low *gravitational* entropy while its matter was already thermal = high entropy; the
`1/10^{10^{123}}` improbability is computed against the max, not against zero). The whole test is
whether `N_min` forces **sub-maximality**, or only low absolute entropy (which is trivial) and the
*location* of the corner (which is re-description).

---

## 2. Question 1 — the forced leg, and why it is the trivial one

Two sub-claims, both forced, neither reaching the selection:

- **`N_min ⟺ roll-origin`: forced, cleanly.** `N = e^{4p} ∈ [1,∞)` is an accreted 4-volume /
  record count. Records are *irrevocable* (TaF/TI finality: a measured record does not un-measure —
  W151 §"monotone growth"; TI's `Finalize` is not `Lose`-able), so the count only accretes and has
  an infimum. The origin of an accretion process is *definitionally* its minimum. This traces to
  **finality**, not to σ — a genuine non-σ object. (Probe: `N` monotone in `p`, `argmin N` = origin.)

- **`S_ext-min ⟺ N_min`: forced for the extensive entropy.** The natural record entropy is
  extensive: Bianconi's vacuum term `σ·Tr ln G = Σ_i ln g_i` is a sum of `N` independent local
  contributions (re-verified in `bianconi-entropy-amplitude-route`), so `S_ext ~ N`, monotone in
  `N`, and `argmin S_ext` = `argmin N` = the origin. (Probe: `S_ext` monotone, `argmin` coincides.)

**Why this is the trivial leg.** Both facts are about *absolute* entropy. They say the young
universe had *little total* entropy because it had *few records* — which is not in dispute and is
not the Past Hypothesis. The Past Hypothesis puzzle is the **sub-maximality** (why so smooth /
special given how much entropy it *could* have had). The forced leg does not touch it. This is also
exactly TaF's own reading: `records-as-rows` makes "time = record accumulation, the row index IS
time" and the arrow "monotone by construction" — but that is the **direction**, and TaF grades it
"exploration-tier … no claim."

---

## 3. Question 2 — the selection, and the one fork that decides it

Here is the crux, and it resolves sharply. Whether `N_min` forces **sub-maximality** at the origin
depends on **one** thing: `S_max(N_min)`, the maximum entropy *available* when only `N_min` records
have accreted. And `S_max(N_min)` is fixed by whether the accessible phase space (the count of
degrees of freedom) **grows with the records** or is **present from the start**:

| | **FIXED phase space (FTS / disclosure)** | **GROWING phase space (Gödelian / issuance)** |
|---|---|---|
| d.o.f. at count `N` | `D` = large constant (all present; records disclose the corner) | `D(N) ~ N` (d.o.f. accrete with records) |
| `S_max(N_min)` | large | `~ 0` |
| sub-maximality `R(N_min)=S/S_max` | `~ 0` (far below max) | `~ O(1)` (generic) |
| initial gap `S_max−S` (Penrose cost) | **huge** (`~10^{5}` nats in the probe) | **`~0`** |
| the Past Hypothesis | **STANDS** — origin is an improbable fine-tuned corner | **DISSOLVES** — no room for fine-tuning in a phase space that doesn't exist yet |
| what `N_min` does | says *where* the corner sits = **re-describes the direction's origin** | **reaches the selection** (the arrow arises because the gap *opens* as d.o.f. accrete) |

The probe (`du_low_entropy_origin_bottom_of_roll_probe.py`, exit 0) exhibits both lethal outcomes
with teeth: FIXED column → origin `R ≈ 1.4e-6`, selection cost `≈ 6.9e5` nats (improbable corner,
PH stands); GROWING column → origin `R ≈ 0.72`, cost `≈ 0.39` nats (generic, PH dissolved). The
GROWING column is the **positive/sensitivity control**: the test *can* return "selection reached,"
so an FTS verdict is informative, not rigged.

**The steelman for the bet (the defense attorney).** The strongest form is the growing column: the
Penrose improbability is computed against a *fixed large* phase space of `~10^{90}` d.o.f. — but in
the records picture that phase space **did not exist at the origin**; it is *itself a product of the
roll*. At `N_min` the phase space is minimal, so the initial state is not an improbable corner — it
is the generic small-`N` state, and the arrow is the *opening* of the entropy gap as d.o.f. accrete.
This genuinely **dissolves** the Past Hypothesis and **reaches past the direction to the selection**.
It is the real intellectual content of the bet, and it is more than σ can do.

**Why it does not close as built (the kill the defense must beat).** The dissolution is available
**only** in the growing column, and **DU's own D-FORK resolution already placed the roll in the
fixed column, as built** (`d-fork-regime-resolution-2026-07-21.md`, re-verified here from the
construction, not imported): the source is a **fixed** B5 action, `N` is a monotone 4-volume of a
**single atom-type** (`|Θ_eff|=1`), and the object that would grow the observable algebra — the
mirror-sector condensate grading vacuum — is **unbuilt (W224)**. In that regime the phase space is
effectively fixed (access-expansion, Bogoliubov on a fixed field algebra, *not* source-forced
d.o.f. growth), so `S_max(N_min)` is large, the origin *is* an improbable corner, and **`N_min`
re-describes the direction's origin without forcing the selection.** The selection-forcing is
**co-extensive with the Gödelian regime** — it is gated on the *identical* source-forced-algebra-
growth witness that the D-FORK win requires (D-FORK §7). One unbuilt object gates both.

**Independent corroboration from the finality repo itself.** TaF's `arrow-of-time-as-constructor-
theorem.md` reaches the *same* split from the other side and resolves it the *same* way: finality
gives the direction (T18, "conditional"), and "**The low-entropy Big Bang then becomes a constraint
on which records are accessible, not the source of time's direction**." I.e. even the repo whose
primitive *is* finality keeps the low-entropy **selection** as a *separate accessibility constraint*,
not something the finality/roll structure forces — grade "a conditional constructor target, not
active physical-arrow evidence." That is exactly this document's verdict, arrived at independently.

**So: does `N_min` force the low-entropy selection?** It forces low *absolute* entropy trivially; it
*dissolves* the fine-tuning **only** in the Gödelian/growing regime; **as built (FTS) it does not
force the selection — it re-describes the direction's origin.** More than σ on the *unification/
co-location* axis (σ is a bare sign; `N_min` co-locates three descriptions at one geometric locus);
**not** more than σ on the *selection* axis as built (both leave sub-maximality untouched; `N_min`'s
route to it is gated). Net on Q2: **re-description as built, with a real but gated route to a genuine
forcing.**

---

## 4. Question 3 — the singularity / curvature facet (PARTIAL, T223-blocked)

The claim that the initial **singularity** is a third face of `N_min` needs the metric/curvature to
be a coarse-grained record structure, so that the singularity is "where the record density
terminates," not a fundamental breakdown. That rides **W151** (emergent GR/c from records), whose own
verdict is **HALF-NATIVE**: the causal/**conformal** leg (c-emergence via Malament + the W131 gimmel
null cone) is GU-native/theorem-backed, but the **dynamics** leg (the Einstein equation) is a generic
Jacobson/Padmanabhan import that matches only on the **sign** (both routes attractive) and **not the
magnitude**, with a genuine **conflict** in the `R²` sector; grade "exploration / conditional …
NOT evidence; novelty = RELABEL + one structural pin, no GU-specific number."

Worse, the record-substrate route confronts a **live in-house negative**, the **T223 no-go**: the
uniform finite finality-colimit ensemble does **not** concentrate on manifoldlike causal sets
(`records-as-rows` §"the decisive n=8 no-go T223"; the GU six-axis file: "the reframe inherits it").
So the records do not yield even a *manifoldlike* smooth spacetime in the generic regime — the
strong-field singular regime is far beyond what is built. **Verdict on this facet: PARTIAL, and the
honest flag is not merely "exploratory" but "confronts a named negative (T223) it does not evade."**

---

## 5. The three-row collapse — what it is and isn't

**What it is (real).** Three *descriptions* co-locate at one *locus*, `N_min`:
- **low-entropy past** = the low-`S_ext` end (forced, §2, absolute-entropy sense);
- **initial singularity** = the low-`N` / minimal-4-volume end (PARTIAL, §4, W151/T223);
- **arrow of time** = the direction of monotone growth away from `N_min` (direction; W151's "the
  volume element IS the tick"; genuine *irreversibility* needs μ — §6).

And the co-location has a genuine **common root**: **finality / irrevocable accretion.** Records
don't un-happen ⇒ the count bottoms out at zero and grows one way ⇒ all three descriptions attach to
the same bottom. That is a real economy: three *posits* become three *readings of one locus with one
root*. This is the honest win of the bet, and it survives the block-universe absorber (below).

**What it isn't (the ceiling).** It is a collapse of **descriptions into a locus**, not a **forcing
of the selection content**. "These are one object because the origin *forces* all three" overreaches:
the low-entropy row's *hard* content (sub-maximality) is forced only in the unbuilt Gödelian regime
(§3); the singularity row is W151/T223-gated (§4); the arrow row's *direction* is native but its
*finality* (genuine irreversibility) needs the unbuilt μ (§6). So the rows share a **locus and a
root**, not a **derivation**.

---

## 6. The adversaries (terrain, carried as pre-registered kills)

- **Observer-gradient falsifier (TaF) — BEATEN (not tripped).** The `N_min` claim is a spatially
  homogeneous **temporal** boundary (the low-`N` end of the roll). It carries no observer-centred
  **spatial** cosmological gradient, so it does not trip the excluded-gradient falsifier. (Probe
  control asserts the spatial invariance explicitly; same clean pass as the Bianconi route.)

- **Block-universe absorber (TaF) — SURVIVED on the structural claim, NOT on the becoming gloss.**
  The three-row *co-location* is a fact about the **structure** of the block (the low-`N` boundary is
  the low-entropy boundary is the geometric origin) and is true in B-theory — the low-entropy-past
  puzzle exists *even* in a block universe, so the structural collapse is meaningful without
  becoming. But "`N_min` = the origin of genuine **becoming**" is exactly what the absorber forbids
  promoting for free — and DU's D-FORK file already found the roll is FTS/**disclosure** as built,
  i.e. the "becoming" gloss is a bookkeeping artifact until the §7 witness is built. **Survived
  structurally; the becoming reading is not earned.**

- **The D-FORK / Gödelian gate (TI) — NOT BEATEN; the load-bearing cap.** This is the same fork that
  decides §3. As built, DU is FTS/disclosure, so both (a) the "becoming" gloss and (b) the
  selection-forcing dissolution fail together. They are gated on **one** unbuilt object (source-forced
  record-*algebra* growth via the compactifying condensate). **The bet's selection win and the D-FORK
  win are the same win.**

- **record-change ≠ finality (continuity-ledger) — the arrow's DIRECTION is native, its FINALITY is
  not.** An `N`-increment (laying a 4-volume atom) is monotone growth (direction), but not
  automatically a *crossing*. Genuine irreversibility — the actual thermodynamic arrow — needs the
  reversal-cost measure **μ** (Landauer + computational-irreducibility), which is **unbuilt**
  (`bianconi` §4: the deterministic flow is reversible; only the stochastic completion produces
  entropy). So "arrow = the roll's monotone growth priced by μ" is: monotone growth ✓ (native), μ
  pricing ✗ (unbuilt). The arrow row collapses into `N_min` as a **direction**, not yet as a **priced
  irreversibility**.

---

## 7. Personas inline (DU Dynamic-Physics board; lenses, not evidence — computation disposes)

- **Cosmologist (past hypothesis, initial conditions, de Sitter).** The bet lives or dies on
  Penrose's phase-space question, and Penrose computes against a **fixed** large phase space. The
  records picture's only escape is the **growing** phase space — which is a substantive, currently
  *unbuilt* physical commitment, not a bookkeeping choice. Note the built vacuum is the record-count
  **de Sitter rolling attractor** with `Λ ~ 1/√N` a **pure import** (`council-dynamic-unity-
  assessment`), and the everpresent `1/√N` law is proven **mutually exclusive** with pinning the
  crossing epoch — so the cosmological amplitude here is not native either. Reads: PARTIAL; the
  dissolution is real but rides an unbuilt growing-d.o.f. commitment.

- **Symbolic Dynamics (irreversibility / the arrow).** A fixed action gives a subshift on a
  one-symbol alphabet ("volume atom") — finite type, *literally* the FTS object (D-FORK §6). The
  monotone `N`-growth is a direction; a genuine irreversible arrow needs a non-sofic/productive
  symbol system (the Gödelian witness) *and* the μ measure. Direction native; finality absent.
  Reads: direction ✓, selection/finality ✗.

- **Dynamical Systems (the roll's origin as an attractor boundary / N_min).** `N_min` is the
  boundary of a 1-D gradient flow on a fixed potential `V(p)`. A boundary of a fixed-potential flow
  is a *starting point*, not a *selection principle* — it says where the trajectory begins, not why
  that beginning is sub-maximal. The "entropy gap opens as the phase space accretes" story is the
  only one that makes the boundary a selection, and it needs the accretion of *d.o.f.*, not just of
  *count*. Reads: co-location real, selection gated on d.o.f.-accretion.

- **Thermodynamics (low-entropy past, entropy-record coupling).** The decisive distinction:
  `S_ext ~ N` couples entropy to the count (extensive, forced), but the Past Hypothesis is about
  `S/S_max` (sub-maximality). The CMB shows the early **matter** was already thermal (max-entropy);
  the low-entropy-ness is entirely **gravitational** (Weyl smoothness) — and that sector is exactly
  the W151/T223-gated one. So the extensive coupling reaches the *absolute* entropy, not the
  *gravitational sub-maximality* that is the actual puzzle. Reads: forced on absolute, gated on the
  gravitational selection.

- **Mathematical Physicist.** The load-bearing structural fact is that **one** variable —
  `S_max(N_min)`, i.e. phase-space growth — decides the whole selection question, and it is
  *identical* to the D-FORK's FTS-vs-Gödelian bit. That is a clean reduction: the bet is not
  independently gradeable from the D-FORK; it *inherits* the D-FORK's verdict. Given DU-as-built is
  FTS, the honest grade is PARTIAL with the selection-forcing deferred to the same unbuilt witness.
  No double-counting: this bet and the D-FORK win are one.

**Anti-crank / Adversary-C (mandatory, an amplitude/selection result is on the table).** The
strongest form — "`N_min` FORCES the low-entropy selection, collapsing three rows into a derived
one" — is **false as built**, for a structural reason not a computational gap: the forcing is
co-extensive with the growing-phase-space (Gödelian) regime, and DU's own construction is FTS. Do
**not** bank the co-location as a forcing; the honest, bankable content is (i) a real **description-
collapse** at one locus with root = finality, and (ii) a real but **gated** dissolution route. Clean
PARTIAL, correctly graded.

---

## 8. Grade, and what would move it

**GRADE: PARTIAL.** Not REFRAMES (no clean forcing of the selection as built — `N_min` re-describes
the direction's origin, and the dissolution route is gated on an unbuilt Gödelian witness). Not
STILL-INHERITED (there is a genuine, non-trivial description-collapse of three rows into one locus
with a real common root — finality — and a real, if gated, route to the selection via the
tiny-initial-phase-space dissolution). It is a **strong PARTIAL** whose sharpest finding is a
reduction: **the low-entropy-selection forcing IS the D-FORK win** — same unbuilt witness gates both.

**What would move PARTIAL → REFRAMES (the forcing conditions, named not pursued):**
1. **Build the growing phase space (the D-FORK §7 witness).** Show the roll **source-forces**
   record-*algebra* growth (`D(N)` genuinely grows from `~0` at `N_min`), surviving the fixed-law /
   Bogoliubov / stochastic-CSG absorbers. Then `S_max(N_min) ~ 0`, the Penrose corner has no room to
   exist, and the selection is **forced by dissolution** — and the D-FORK flips to Gödelian in the
   same move. This is the single highest-leverage target, and it is *hard* for the reason D-FORK §5
   gives (a fixed action generically cannot grow its observable algebra).
2. **Build μ and price the arrow.** Turn the native *direction* (monotone `N`) into a native
   *finality* (genuine irreversibility) via the reversal-cost measure μ, so the arrow row collapses
   into `N_min` as a priced crossing, not just a direction.
3. **Clear T223 for the curvature facet.** A finality-native *generator* whose large-scale limit is
   manifoldlike/Lorentzian would let the singularity facet move off PARTIAL; until then it inherits
   the T223 no-go.

Absent (1), the collapse is a description-economy, not a forcing; the low-entropy selection stays
where σ left it — untouched — reached only in a regime DU has not built.

---

## 9. Boundary

Exploration/proposal tier. **Two new files** written (this document + `tests/du_low_entropy_origin_
bottom_of_roll_probe.py`, foreground, exit 0, stdlib-only, with a positive control and an
observer-gradient control so the FTS verdict is informative not rigged). All cross-repo material —
the σ type wall, the arrow-as-constructor conditional, W151 HALF-NATIVE, the T223 no-go, the Sorkin
`1/√N` import, TaF's direction-vs-selection resolution — was **ingested and re-verified here** under
DU's sovereign self-check (CONNECTIONS.md), consumed as source, never adopted on a sibling's say-so;
DU's own D-FORK verdict was re-checked from the construction, not imported. No sibling file, and no
DU governance/canon file (README, AGENTS, LANES, CONNECTIONS), was edited. **Nothing banked:** this
is a candidate-bet verdict carrying its pre-registered kills (observer-gradient — beaten;
block-universe — structural-only; D-FORK — the cap; record-change ≠ finality — μ unbuilt), not a
passed result; it faces Lane 3 before any count. No claim, canon, or public-posture change. **No
commit, no push** (per the directing instruction). Joe alone publishes; nothing routes externally.
Promotion of this bet into Lane 1 is a lane move that stays Joe-gated.

```
Probe: python tests/du_low_entropy_origin_bottom_of_roll_probe.py  ->  exit 0
       (forced-leg holds; selection-fork discriminator has teeth; positive control fires;
        observer-gradient not tripped; DU-as-built = FTS => selection NOT forced as built)
```
