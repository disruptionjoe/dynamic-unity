---
title: "Next five-swing scaffold: preferred foliation as a fixed conditional assumption"
status: position_4_complete__position_5_executable
doc_type: inline_science_council_persona_triple_diamond_five_swing_scaffold
created: 2026-07-27
authority: "Joe direct chat: prepare the next five-swing series under a preferred-frame assumption"
sequence_id: N5-PF
active_position: N5-PF-P5
completed_positions:
  - N5-PF-P1
  - N5-PF-P2
  - N5-PF-P3
  - N5-PF-P4
deferred_positions: []
run_plan: "../runs/2026-07-27-preferred-foliation-next-five-preparation.md"
assumption_status: "HELD CONDITIONAL PREMISE / NO EVIDENTIARY GRADE"
claim_status_change: none
paper_state_change: none
prediction_state_change: none
hardware_state_change: none
---

# Next five-swing scaffold: preferred foliation as a fixed conditional assumption

## Executive decision

Hold an **exactly compensated preferred foliation** for the complete series,
but do not make the series a Lorentz-violation campaign.

Use this five-position shape:

```text
P1  re-rank the whole DU portfolio and map assumption dependencies
P2  prove the selected primary's operational quotient or return its first counterexample
P3  test whether the relevant physical antecedent selects the complete accessible interface
P4  attack the strongest surviving regional-finality or excess-content candidate
P5  collide the result across preferred-leaf and causal-partial-order arenas
```

`N5-PF-P1` selected conservative certification and physical feedback as the
primary while returning the foliation `INERT` for that front. `N5-PF-P2`
proved the scoped deterministic, stochastic-kernel, and quantum-process
necessity package. `N5-PF-P3` proves the response-class selector theorem:
microscopic boundary identity is unnecessary, but selection is relative to a
frozen future action family and can fail after capability growth. The reused
gauge and metastable specimens select restricted response classes and fail
when orientation/path or route/reset-lineage capabilities are added. Their
current antecedents do not endogenously select the complete class. The
foliation remains `INERT`. `N5-PF-P4` now closes the three regional-excess
candidates: consensus does not derive a physical finality rate, broad
mergeability does not derive complementarity, and an unselected finite
threshold does not carry a universal exponent. Its important correction is a
fourth rate class—genuinely new Lorentz-covariant dynamics—which remains
conditional in `PRED-DU-001/002`, not derived here. Only `N5-PF-P5` is now
executable.

The present evidence makes one provisional branch especially
information-dense:

```text
hidden global leaf
    != formed record
    != observer access
    != regional agreement
    != public action finality
    != empirical excess.
```

The branch asks whether privileged simultaneity reduces any North-Star
dependency, or merely adds a hidden completion whose observer-accessible
shadow already factors through causal partial order.

No scientific claim, hypothesis grade, concept promotion, paper priority,
prediction, model, hardware path, provider action, or publication state
changes through this preparation.

## 1. Frozen assumption contract

### Version held

The complete series holds:

```text
EXACTLY_COMPENSATED_PREFERRED_FOLIATION
```

Formally, let

\[
\mathfrak F=\{\Sigma_\tau\}_{\tau\in\mathbb R}
\]

be a preferred foliation with privileged simultaneity parameter \(\tau\).
Finite-speed propagation and all observer-accessible dynamics are exactly
compensated so that every admitted experiment agrees with the corresponding
Lorentz-covariant/SR description.

This version has no novel empirical prediction by construction.

### Included

- a continuous privileged simultaneity structure;
- a global update parameter;
- a coherent global time orientation on the admitted region;
- finite-speed physical propagation; and
- an allowed hidden global completion or biography.

### Not included

- a discrete clock, lattice, cellular automaton, or minimum time step;
- Lorentz-violation leakage;
- a preferred spatial direction;
- an observer token;
- a system--environment boundary;
- a detector or boundary orientation;
- a record write, archive, provenance rule, or reset;
- an observer access route;
- common knowledge, Byzantine safety, or public finality; or
- evidence that the assumption is true.

The first mailbox proposal's partial-order architecture and the held foliation
are independent axes. Causal set theory remains the standing counterexample to
any inference from discreteness to preferred frame.

### Time-orientation receipt

The assumption closes one previously implicit arena premise: the admitted
region is time-oriented. That makes “future action class” meaningful.

It does **not** identify the material gauge-boundary orientation. A global
choice of future cone and a local outward normal plus charge convention have
different object types, loci, and provenance. Neither selects the other.

### No-switch rule

The assumption will not switch inside this sequence. If a proposed result
requires an observer-accessible foliation dependence, return:

```text
LEAKING_BRANCH_REQUIRED
```

and park it for a separately authorized series. Do not quietly relax exact
compensation.

## 2. Matched hostile control

Every relevant swing carries a causal-partial-order control.

Let \(M_{\mathfrak F}\) be the admitted foliated histories and let

\[
\pi_{\mathfrak F}:M_{\mathfrak F}\longrightarrow M_{\prec}
\]

forget privileged simultaneity while preserving the operational
happens-before structure, complete local intervention statistics, formed
records, observer access, capability, and resource ledger.

The control must match:

1. local event and intervention identities;
2. multi-time process statistics;
3. causal reach and propagation speed;
4. record instruments and archives;
5. observer access routes;
6. admitted action/capability families;
7. finality/fault semantics; and
8. resources and horizon.

Classify every result:

```text
FOLIATION_INERT
FOLIATION_NARROWS_HIDDEN_COMPLETIONS
FOLIATION_AIDS_REPRESENTATION
FOLIATION_AIDS_RECONSTRUCTION
FOLIATION_SELECTS_INTERFACE
FOLIATION_HAS_OPERATIONAL_EXCESS
INCOMPLETE_COMPARISON
```

The last substantive class contradicts exact compensation unless the
comparison exposed an unmatched premise.

## 3. What the current repository already decides

The series begins after `HC-DU-054`, not from a blank page.

| current receipt | consequence under the held assumption |
|---|---|
| `HC-DU-033F/G/H` | Selection remains antecedent-relative. A global clock or time-oriented flow does not select a material clock, write, archive, decoder, observer, or access route unless those outputs are constant on the antecedent fibres. |
| `HC-DU-035D/E` | Public finality is selective-future center screening and public-shadow descent relative to a frozen capability envelope. A global leaf does not replace those conditions. |
| `HC-DU-039C/D` | Complete public behavior and one total phase do not identify hidden source structure. Adding a hidden global time label does not improve source attribution unless a source-selective sensitivity changes. |
| `HC-DU-041` | Interface selection, strict compression, held-out transfer, and finality are separate receipts. |
| `HC-DU-047..051` | Propagation, signatures, gossip, metastability, quorums, and cryptographic hardening change knowledge, reliability, or action safety only after source-sensitive information exists in the complete admitted input. |
| `HC-DU-052..054` | Causal closure, noninjective transfer, and accessible-interface selection remain distinct. The transfer theorem is absorbed, and archive relocation keeps `H-CCR-17` dormant. |

The exact-compensation assumption does not by itself break the
accessible-versus-hidden archive relocation twin. It therefore does not
automatically reopen `H-CCR-17`.

## 4. The five-person Science Council

These are inline lenses, not evidence.

| council member | strongest contribution | preparation consequence |
|---|---|---|
| Orthodox professor | Exactly compensated LET changes ontology or completion bookkeeping, not admitted predictions. The first scientific job is to identify which existing DU results actually depend on rejecting a global frame. | Make Position 1 an assumption-dependency audit and require a partial-order control. |
| Heterodox professor | A privileged leaf may still simplify the source side: actual configurations, global history, and update order can be stated without ambiguity. Its value could be explanatory even when empirically hidden. | Allow a scoped forward-representation or completion-narrowing return without calling it reconstruction or evidence. |
| Wild-frontier scientist | The interesting mismatch is a globally ordered hidden process with locally formed, regionally hardened, capability-relative facts. A leaf may coexist with plural public realities. | Test global order against archive/access and regional-finality fibres rather than searching immediately for Lorentz leakage. |
| Commercial scientist | The assumption is valuable if it cheaply closes dead branches or exposes one tractable theorem. It is not valuable if it launches another bespoke simulator or literature tour. | Put the whole-portfolio value-of-information gate first and admit no hardware or large model. |
| Philosopher of science | Existence of a hidden biography, reconstructability of that biography, and explanatory priority are three different claims. Exact empirical equivalence cannot decide ontology. | Preserve operational duality and structural explanation as honest outcomes; pre-register the retreat cost of every stronger claim. |

### Council convergence

The council does not recommend testing whether Lorentz Ether Theory is
respectable or viable. That is held.

It recommends testing:

> Does the extra global structure reduce the minimum physical premises needed
> for certified causal reconstruction, interface selection, or regional
> finality? If it does not, can that inertness be stated as a useful
> branch-invariant theorem?

## 5. The twelve Q-0059 persona lenses

| lens | what it uniquely notices now | preparation consequence |
|---|---|---|
| Engine architect | A server tick is not the same as cache validity, collision authority, visibility, or client possession of state. | Audit what the foliation invalidates or authorizes; do not infer access from update order. |
| Relational quantum foundationalist | A hidden global completion may coexist with relationally indexed facts. Observer-relative records need not deny the completion. | Seek a branch-invariant observer/access formulation rather than making relationality an anti-foliation axiom. |
| Emergence physicist | Global microscopic time does not select the coarse variables, stable phases, or critical behavior that become classical. | Require a physical coarse-graining/formation mechanism before using universality or threshold language. |
| Glitch-hunter | Exact compensation moves the search from ordinary Michelson--Morley tests to seams where hidden order might be mistaken for record order or finality. | Fuzz archive relocation, delayed access, spacelike ordering, capability enlargement, and regional merge order. |
| Complexity theorist | FLP and randomized-consensus bounds depend on synchrony, failure, task, and adversary assumptions—not merely on whether a global clock exists. | Reconstruct every lower bound in its native model before importing a finality-rate claim. |
| Type theorist | Foliation, causal order, record, certificate, public fact, and action capability are different types linked by partial maps or quotients. | Give every swing an explicit typed-arrow and kernel ledger. |
| Enactivist | An observer is a physical coupling and action role, not a coordinate label carried by the global leaf. | Require source--observer interaction and access rather than a named node. |
| Predictive-processing scientist | Global physical order and an agent's internal temporal model may agree operationally while remaining different levels. | Prevent a cognitive timing model from being promoted to substrate evidence. |
| Capability and access theorist | A hidden fact matters to an observer only through an admitted action or access edge. | Index all reconstruction and finality claims by action class, risk, cost, and horizon. |
| Metaphor historian | “Server,” “tick,” “gossip,” and “ledger” are disposable vocabulary unless a statement survives their removal. | Apply the deletion test to every theorem and prediction candidate. |
| Phenomenologist | Lived temporal order may track record availability and breakdown rather than the hidden simultaneity structure. | Keep experienced/operational time separate from the posited leaf parameter. |
| Madhyamaka philosopher | Privileging a foliation does not force primitive observer nodes or intrinsic facts. | Test which relata are selected by dynamics and which are imposed by description. |

### Divergent warnings preserved

1. A preferred leaf can narrow the ontology while remaining invisible to every
   observer-accessible target.
2. A global state can exist while no observer has access to it.
3. A global time orientation can exist while detector orientation remains
   unselected.
4. A total update parameter can exist while knowledge remains only partially
   ordered.
5. A synchronous hidden dynamics can present an asynchronous interface to
   bounded observers.
6. A regional certificate can be action-safe without reconstructing the
   global biography.
7. A finality rate can be a standard dynamical timescale rather than a new
   law.
8. A measurable foliation-dependent rate would leave the exactly compensated
   branch.
9. Noncommutativity and record nonmergeability need not be extensionally
   identical.
10. Critical scaling can arise inside standard open-system physics and would
    not establish consensus architecture.
11. Bohmian or LET compatibility can supply a serious specimen without
    selecting that ontology.
12. A clean no-excess result can still remove a hidden assumption from the
    North-Star charter.

## 6. Preparatory conditional conjectures

These are sequence targets, not ledger claims.

### `PF-C1` — Operational foliation quotient

If every admitted intervention, record, observer-access channel, capability,
and resource-accounted response is exactly compensated, then the complete
observer-accessible process factors through the foliation-forgetting quotient
\(\pi_{\mathfrak F}\).

**Cheapest kill:** two foliated histories with the same complete
partial-order operational process but different admitted response
distributions.

**Retreat cost:** if the difference is real, the series cannot remain exactly
compensated; if no difference exists, the foliation earns at most hidden
completion or forward-representation value.

### `PF-C2` — Foliation does not select an accessible record interface

A preferred foliation selects a complete accessible formed interface only if
that interface is constant across all implementation variants sharing the
same held antecedent, including archive-routing and observer-access variants.

**Cheapest kill:** one physically justified theorem making source binding,
write, archive route, decoder, access, action envelope, and resource horizon
unique from the foliated antecedent.

**Retreat cost:** an archive-relocation pair leaves the foliation as a
permissive background rather than a record selector.

### `PF-C3` — Global order does not imply regional public finality

Finite-speed propagation plus one privileged simultaneity structure does not
by itself yield common knowledge, incompatible-certificate exclusion,
evidence availability, or capability-safe regional handoff.

**Cheapest kill:** derive those properties from foliation and propagation
alone without adding fault, authentication, access, archive, or action
premises.

**Retreat cost:** regional finality remains a separately formed and
capability-indexed layer.

### `PF-C4` — Finality-rate quadrilemma

A proposed finality rate distinct from causal reach is either:

1. a standard dynamical/communication/decision timescale;
2. an observable preferred-foliation leak; or
3. operationally unidentifiable; or
4. genuinely new observer-accessible Lorentz-covariant dynamics.

The fourth class is a correction earned by `HC-DU-057`. Relativistic
objective-collapse models show the class is logically available, but DU has
not selected its generator or coefficient. `PRED-DU-001/002` already carry
the conditional branch.

**Retreat cost:** the mailbox finality-rate proposal moves to the leaking
branch, is absorbed by standard dynamics, or remains a conjecture without a
selected coefficient. A covariant-new-dynamics claim stays conditional until
its generator, coefficient, controls, and held-out excess are derived.

### `PF-C5` — Branch-invariant observer indexing

The Certified Causal Reality North Star can keep observer indexing
operational and methodological under both preferred- and
nonpreferred-foliation completions by indexing only physical records, access,
interventions, actions, and resources.

**Cheapest kill:** a central DU theorem whose statement or truth value
necessarily changes solely because a hidden preferred foliation exists while
all admitted operational data remain matched.

**Retreat cost:** the charter must declare the foliation dependency for that
specific theorem rather than treating observer indexing as unconditional
ontology.

## 7. Triple Diamond 1 — choose the problem worth solving

### Divergence

The assumption could be used in at least seven different ways:

1. search for Lorentz-violation leakage;
2. build a Bohmian or LET record host;
3. reconstruct a global history from local records;
4. ask whether a global history selects observer interfaces;
5. derive regional finality from a global update order;
6. use the foliation only as a forward explanatory representation; or
7. leave it inert while another DU front remains more important.

### Convergence

The leaking search is outside the chosen assumption. Another record host is
blocked by `HC-DU-054` unless the foliation breaks archive relocation.
Reconstruction and finality questions remain valuable only after a
whole-program comparison.

Therefore Position 1 must answer:

> Across the complete current Dynamic Unity portfolio, which next bounded
> question has the highest expected North-Star decision value when a preferred
> foliation is held but not allowed to count as evidence?

### Required assumption-dependency labels

For every candidate front:

```text
INERT
REMOVES_A_PREMISE
NARROWS_COMPLETION_CLASS
CHANGES_RECONSTRUCTION_TARGET
CHANGES_INTERFACE_SELECTION
CHANGES_EMPIRICAL_CONTENT
CONTRADICTS_EXACT_COMPENSATION
```

## 8. Triple Diamond 2 — choose the efficient orchestration

| orchestration | advantage | failure mode | disposition |
|---|---|---|---|
| Five Lorentz-violation tests | Directly empirical | Violates the exactly compensated branch and lets the assumption select the work | reject |
| Five Bohmian/LET constructions | Concrete hidden-state models | Repeats host proliferation and supplies beables, foliation, equilibrium, apparatus, and access together | reject |
| Five consensus analogies | Explores regional finality | Repeats completed `HC-DU-047..051` and risks theorem transfer by metaphor | reject |
| One assumption-only formal treatise | Clean | Could prove only what exact compensation defines | use as one position, not the series |
| Portfolio reset, quotient theorem, interface test, hostile excess audit, cross-arena closeout | Alternates global selection, formal proof, physical attribution, and falsification | Requires later positions to remain conditional | selected |

### Selected principle

Use **portfolio-first staged concentration with a mandatory hostile
architecture control**:

```text
whole-program value
    -> exact factorization
    -> physical antecedent attribution
    -> risky candidate audit
    -> branch-invariant synthesis or clean non-unification.
```

## 9. Position 1 — whole-program reset and foliation-dependency map

### Identity

```text
N5-PF-P1
Whole-Program Priority Reset and Preferred-Foliation Dependency Map
```

This was the sole executable position at preparation and is now complete.

### Decision

Which current Dynamic Unity front most efficiently advances purpose and the
North Star under the held assumption, and what does the assumption actually
change for that front?

### Best approach

Reuse the complete 17-front compiler from the previous portfolio reset and
add, without changing its old grades:

1. exact held antecedent;
2. foliation-dependency label;
3. partial-order matched control;
4. minimum new premise;
5. strongest absorber;
6. cheapest exact positive or kill;
7. local pre-hardware learning;
8. expected North-Star decision value;
9. novelty only after the assumption is deleted from the statement; and
10. explicit stop/reopen condition.

At minimum compare:

- `H-CCR-17` accessible-interface selection;
- conservative certification and physical feedback;
- `H-CCR-11` global-clock quotient or leakage;
- regional finality/descent;
- physical record formation;
- proper-time/source attribution;
- QFT/AQFT localization;
- CFS/law-filtered reconstruction;
- quantum-strength/GPT selection;
- causal growth and source-action branches;
- the three mailbox discriminator candidates;
- paper-independent local theorem opportunities; and
- a deliberate stop or return to a different DU seam.

### Required return

```text
ASSUMPTION_VERSION_EXACTLY_COMPENSATED
PRIMARY_<NAMED_FRONT>
RESERVE_<NAMED_FRONT>
STOP_<NAMED_EXHAUSTED_FRONT>
FOLIATION_ROLE_<DEPENDENCY_LABEL>
```

### Stop

Do not rank by affinity with the assumption, persona enthusiasm, conceptual
drama, or paper count. A front without a finite local learning path cannot
win.

## 10. Position 2 — provisional operational quotient or selected-primary spine

### Conditional identity

```text
N5-PF-P2
Operational Foliation Quotient or Selected-Primary Formal Spine
```

Position 1 has now activated the selected-primary branch and retyped this
position as the Conservative Certification and Physical Feedback Formal
Spine. The provisional quotient design below remains the reserve/control;
the executable contract is recorded in the completion receipt at the end of
this document.

### Provisional decision

Does the observer-accessible process factor through the
foliation-forgetting quotient, or does the held global structure reduce a
North-Star ambiguity not captured by causal partial order?

If Position 1 chooses another primary, retain the role of this swing—prove
the winner's sharpest necessary-and-sufficient condition or smallest exact
counterexample—and record the foliation as inert where appropriate.

### Best approach

1. Define foliated histories, partial-order histories, admissible
   interventions, records, observer/access maps, action families, and
   resources.
2. Freeze exact compensation as equality of complete operational process
   statistics, not only terminal probabilities.
3. Construct the forgetting map and test factorization/naturality.
4. Separate hidden-completion narrowing from observer-target reconstruction.
5. Include one LET/Bohm-compatible positive representation and one
   foliation-free causal-order control without choosing either ontology.
6. Return the first target, if any, that fails to descend.
7. Collide the statement with ordinary quotient/gauge, empirical-equivalence,
   hidden-variable, and observability results before novelty language.

### Required return

```text
EXACT_OPERATIONAL_FOLIATION_QUOTIENT
HIDDEN_COMPLETION_NARROWING_ONLY
SCOPED_RECONSTRUCTION_ADVANTAGE
FIRST_NON_DESCENDING_TARGET
LEAKING_BRANCH_REQUIRED
INCOMPLETE_CONTRACT
```

### Stop

Do not call a quotient theorem novel physics, and do not call an
unobservable hidden label an observer-accessible reconstruction.

## 11. Position 3 — complete accessible-interface selection test

### Conditional identity

```text
N5-PF-P3
Preferred-Leaf Interface Selection or Archive-Relocation Obstruction
```

Activate only after Position 2.

### Provisional decision

Does the foliated physical antecedent select:

```text
source binding
    -> write
    -> occurrence provenance
    -> archive route
    -> orientation
    -> observer access
    -> action envelope
    -> resource horizon
```

or does it merely coexist with several accessible and hidden implementations?

### Best approach

1. Apply the `HC-DU-033F/054` antecedent-fibre test rather than constructing
   another fitted host.
2. Hold global leaf history, Lorentz-compensated observable dynamics, source,
   and local reduced behavior fixed.
3. Vary archive routing, decoder access, and observer boundary without
   changing the held antecedent.
4. Use one serious physical realization only if the general fibre argument
   leaves a real ambiguity: Bohmian conditional records, an FLRW/ADM
   matter-clock arena, or a locally coupled AQFT probe are candidates, not
   preselected winners.
5. Test whether a further physically motivated premise closes the first
   implementation fibre without target coding.
6. Report any positive as point selection, orbit selection, plural selection,
   or conditional formation.

### Required return

```text
FOLIATION_SELECTS_COMPLETE_INTERFACE
FOLIATION_SELECTS_PARTIAL_ANTECEDENT_ONLY
ARCHIVE_RELOCATION_OBSTRUCTION
ACCESS_UNSELECTED
ORBIT_OR_PLURAL_INTERFACE
TARGET_CODED_REPAIR
INCOMPLETE_CONTRACT
```

### Stop

Do not credit global chronology for a record that was supplied through a
pointer, archive, decoder, or observer cut.

### Completion receipt — `HC-DU-056`

Position 3 returns:

```text
RESPONSE_CLASS_SELECTOR_IFF_ANTECEDENT_FIBRE_CONSTANCY
RAW_MICROSCOPIC_IDENTITY_NOT_REQUIRED
PARTIAL_OR_ORBIT_BOUNDARY_SELECTION
FEEDBACK_BOUNDARY_RELOCATION_OBSTRUCTION
CAPABILITY_DEPENDENT_FIRST_LEAK
RESET_REQUIRES_COMPLETE_FUTURE_READABLE_MEMORY
CURRENT_COMPLETE_INTERFACE_NOT_ENDOGENOUSLY_SELECTED
MATHEMATICAL_CORE_FULLY_ABSORBED
FOLIATION_INERT
```

For a complete response signature \(\Sigma_{\mathcal A}\), the selected
response class exists exactly when

\[
\ker A\subseteq\ker(\Sigma_{\mathcal A}\circ B).
\]

Capability enlargement refines response-equivalence. The material `Z3`
specimen has exact class counts \(1\to2\to4\) under source-only, fixed-pointer,
and intermediate-path testers; the closed host has the same counts under
endpoint, occurrence, and next-cycle-memory testers. Orientation/path and
route/reset-lineage close the finite classes only when physically frozen;
response signatures themselves are action-coded repairs, not selected
physics. Future-invisible microscopic tags remain legitimately plural.

The exact regression passes `21/21`. No new host, simulation, hardware,
physical law, ontology, prediction, paper, or endogenous complete interface
was produced. Position 4 is activated only as a hostile excess-content audit.

## 12. Position 4 — regional finality and candidate-excess hostile audit

### Conditional identity

```text
N5-PF-P4
Regional Finality and Excess-Content Hostile Audit
```

Activate only after Position 3, and deepen at most one candidate.

### Decision

Does any currently proposed regional/consensus formulation make a
meaning-preserving, risky statement beyond standard physics under the held
assumption?

### Candidate set

Audit the three proposed mailbox discriminators independently:

1. a finality-rate bound distinct from \(c\);
2. complementarity derived from record mergeability; and
3. universal critical exponents for decoherence/finality thresholds.

Do not presume any survives.

### Best approach

For each candidate:

1. delete the computer-science vocabulary and state the physical proposition;
2. reconstruct the native theorem assumptions;
3. define the physical adapter and matched standard null;
4. state the pre-registered kill and retreat cost;
5. run the cheapest exact counterexample or literature-level absorber;
6. ask whether preferred foliation is inert, enabling, or contradicts exact
   compensation; and
7. deepen only the candidate with nonzero residual and a local
   decision-changing path.

The finality-rate audit must distinguish hidden synchrony, participant-known
synchrony, bounded delay, partial synchrony, asynchrony, faults, randomness,
and task definition. A global clock alone does not establish an FLP model or
its lower bounds.

The mergeability audit must distinguish sharp commuting observables,
jointly measurable unsharp observables, physical record interfaces, and
resource/access compatibility.

The critical-exponent audit must distinguish ordinary open-system phase
transitions and universality from a new regional-finality dynamics with
selected coefficients.

### Required return per candidate

```text
RELABEL_ONLY
KNOWN_THEOREM_SPECIALIZATION
SCOPED_CONJECTURE_WITH_NONZERO_EXCESS
EXACT_COUNTEREXAMPLE
LEAKING_BRANCH_REQUIRED
SEARCH_INCOMPLETE
```

### Stop

Do not build a consensus or decoherence simulator unless a minimal local model
passes `LMLG-01` and can decide a residual not already decidable by theorem,
counterexample, or existing literature.

### Completion receipt — `HC-DU-057`

Position 4 returns:

```text
CONSENSUS_DERIVED_FINALITY_RATE_EXACT_COUNTEREXAMPLE
RATE_TRILEMMA_CORRECTED_TO_FOUR_WAY_CLASSIFICATION
BROAD_MERGEABILITY_COMPLEMENTARITY_EXACT_COUNTEREXAMPLE
SHARP_PVM_CASE_KNOWN_THEOREM_SPECIALIZATION
UNIVERSAL_FINALITY_EXPONENT_EXACT_COUNTEREXAMPLE
NO_CURRENT_DU_SPECIFIC_EXCESS
FOLIATION_INERT
```

One hidden global tick permits different ordinary formation times and
arbitrary protocol waits. FLP and partial-synchrony theorems do not select a
physical generator, delay bound, participant knowledge, error contract, or
coefficient. The exact \(p=1/2\) versus \(p=3/4\) control gives safe formation
in three versus two steps at error \(1/8\) while causal reach is fixed.

The prepared rate trilemma was incomplete. New observer-accessible
Lorentz-covariant dynamics is a fourth logical class distinct from standard
timescales, preferred-frame leakage, and inaccessible structure. DU does not
derive this branch; `PRED-DU-001/002` already carry it conditionally.

For mergeability, noncommuting unsharp \(X/Z\) effects at \(\eta=1/2\) admit
an exact positive joint POVM, while already formed provenance-bearing
classical records merge by set union. The sharp-PVM equivalence remains known
mathematics. For criticality, a monotone score power changes the apparent
exponent before an order parameter is physically selected, and a finite
analytic crossover can become arbitrarily sharp without a critical
singularity.

The exact regression passes `20/20`. No candidate warrants a new model, and
no physics, prediction, concept, paper, ontology, hardware, or provider state
is promoted. Position 5 is activated.

## 13. Position 5 — cross-arena branch-invariant synthesis

### Conditional identity

```text
N5-PF-P5
Preferred-Leaf / Partial-Order Cross-Arena Adjudication
```

Activate only after Position 4.

### Decision

What, if anything, did the preferred foliation change in Dynamic Unity's
North-Star architecture after complete process, interface, access, finality,
and resource matching?

### Arenas

Choose three from Position 1, with these roles:

1. **Global-leaf positive representation:** LET/Bohm/FLRW/ADM or another
   serious model in which the held structure is explicit.
2. **Causal-partial-order control:** same local operational process without
   privileged simultaneity.
3. **Physical record arena:** material gauge boundary, localized AQFT probe,
   optical-clock process, or another independently selected physical
   instrument.

Add a distributed ground-truth fixture only when it tests the same typed
statement rather than supplying vocabulary.

### Best approach

Apply one unchanged ledger:

- physical completion and source;
- primitive causal/operational order;
- foliation and time orientation;
- formed record and provenance;
- observer/access interface;
- regional finality/fault contract;
- action/capability envelope;
- resource horizon;
- held-out target;
- quotient or first witness; and
- strongest absorber.

Search for:

1. one branch-invariant theorem;
2. one exact place where preferred foliation removes a premise;
3. one exact non-unification;
4. one separately parked leaking prediction; or
5. a clean no-excess result.

### Final returns

```text
BRANCH_INVARIANT_CERTIFIED_CAUSAL_REALITY_KERNEL
SCOPED_FOLIATION_ASSISTED_RECONSTRUCTION
SCOPED_FOLIATION_INTERFACE_SELECTION
EXACT_FOLIATION_PARTIAL_ORDER_NON_UNIFICATION
NO_EXCESS_CONTENT
LEAKING_BRANCH_REQUIRED
NORTH_STAR_PORTFOLIO_RESET
```

### Charter check

Explicitly decide whether observer indexing remains:

```text
OPERATIONAL_AND_METHODOLOGICAL
CONDITIONAL_ON_REJECTING_PREFERRED_FOLIATION
THEOREM_SPECIFIC_DEPENDENCY
```

Do not promote an ontological answer from empirical equivalence.

## 14. Triple Diamond 3 — assurance and optimization

### Shared obligations

1. exactly compensated version declared in Position 1 and never silently
   switched;
2. assumption kept ungraded;
3. discreteness kept independent;
4. global time orientation separated from local apparatus orientation;
5. global leaf separated from observer access and public record;
6. complete multi-time process matched, not only endpoint statistics;
7. partial-order hostile control carried wherever meaningful;
8. law-only, record-assisted, and interface-selection receipts separated;
9. action/capability and resource envelopes frozen;
10. native theorem assumptions reconstructed before analogy;
11. strongest absorber, kill, and retreat cost pre-registered;
12. local proof/counterexample before simulation;
13. no hardware before irreducible local exhaustion;
14. no ontology from operational equivalence; and
15. adverse results shorten the sequence rather than trigger host
    proliferation.

### Branch-safe adaptation

| Position-1 or later return | Next action |
|---|---|
| A non-foliation front clearly wins | Retype Positions 2--5 around that primary; retain exact foliation as an explicit inert or premise-changing column. |
| Foliation dependency is the highest-value front | Run the provisional Positions 2--5 as written. |
| Foliation removes only the time-orientation premise | Bank that scoped dependency and pursue the selected primary; do not spend four positions re-proving it. |
| Operational quotient is exact | Position 3 tests interface selection; if also adverse, Position 4 may become the candidate-excess closeout. |
| A target fails to descend | Check unmatched process, access, and resource premises; a genuine residual forces `LEAKING_BRANCH_REQUIRED`. |
| Archive relocation survives | Do not reopen `H-CCR-17`; move to regional-finality/excess audit or the Position-1 reserve. |
| One mailbox candidate survives cheap kills | Deepen only that candidate locally in Position 4. |
| All candidates are absorbed | Treat the clean null as the result and use Position 5 to state the branch-invariant kernel. |
| First meaningful learning requires hardware | Park the branch and use the local reserve. |

### Local-computation and hardware rule

All positions must earn their first decision-changing result through direct
proof, exact counterexample, bounded symbolic calculation, or source-pinned
collision on the current computer.

No hardware belongs in the default series. If a later position leaves one
irreducible empirical discriminator, record one awareness note. Do not build
an adapter or contact a provider without separate authorization.

## 15. Execution discipline

1. Run one position at a time.
2. `N5-PF-P1/2/3/4` are complete; only `N5-PF-P5` is executable now.
3. Do not reopen Position 4 without a newly selected physical generator,
   instrument, or order parameter.
4. Position 1 must begin by repeating
   `ASSUMPTION_VERSION_EXACTLY_COMPENSATED`.
5. If the version ever changes, close this sequence and state why; do not
   mutate it in place.
6. Use the five-person Science Council and twelve persona lenses inline.
7. Preserve divergent output before convergence; persona agreement is never
   evidence.
8. Use the near-term approach atlas after Position 1 selects a primary.
9. Update this scaffold after each receipt without rewriting prior branches.
10. Stop early if the primary is absorbed and the reserve has higher expected
    learning value.

## 16. Unconstrained wish list

These are `UNRANKED / UNCOMMITTED / NOT_A_CLAIM`:

- characterize the category of foliated global histories and its
  operational quotient functor;
- test whether the quotient has a canonical section or only many hidden
  completions;
- distinguish continuous preferred time from a discrete update rule and
  quantify exactly which conclusions require each;
- ask whether Bohmian equivariance supplies any target-independent record
  selection beyond a global configuration history;
- derive the minimum extra premise turning a global leaf into an
  observer-accessible archive;
- compare global leaf order, Lamport order, distributed knowledge, and common
  knowledge in one typed model;
- characterize when participant-known delay bounds, rather than hidden
  synchrony, change finality/liveness;
- test whether public-shadow descent is natural under changes of foliation;
- examine non-time-orientable or topology-changing arenas only as a separate
  challenge to the held assumption;
- distinguish FLRW/CMB convenience frames from a fundamental preferred
  foliation in reconstruction statements;
- determine whether a hidden global biography can reduce description length
  or proof complexity without changing empirical content;
- ask whether exact compensation can be expressed as an observational
  bisimulation between preferred-leaf and covariant process models;
- look for a no-go stating that no exactly compensated hidden structure can
  improve observer-accessible source attribution without an independently
  accessible sensitivity;
- preserve the corrected four-way finality-rate classification and ask which
  physical premise, if any, selects its covariant-new-dynamics branch;
- classify mergeable sharp records, compatible unsharp records, and
  noncommuting capability in one operator-algebraic interface;
- audit existing open-system critical phenomena before positing new
  consensus universality;
- preserve hidden-shard or inaccessible-sector ideas only when they carry a
  covariant conserved coupling and a no-refit held-out consequence; and
- ask whether the North Star itself admits a foliation-independent universal
  property over observer/access functors.

## Bottom line

The preferred foliation is being used as a controlled conditional lens, not
as a destination.

The five-position series is designed to learn one of three things:

1. the hidden global structure genuinely removes a North-Star premise;
2. it improves only forward representation or hidden completion bookkeeping;
   or
3. it is operationally inert, while records, access, regional finality, and
   capability remain the real work.

Position 1 has now earned the next branch by beating the rest of the Dynamic
Unity portfolio on expected knowledge value.

## Position-1 completion receipt

The complete receipt is:

- [whole-program priority reset and dependency map](preferred-foliation-whole-program-priority-reset-and-dependency-map-2026-07-27.md);
- [run receipt](../runs/2026-07-27-preferred-foliation-position-1-portfolio-reset.md).

Position 1 returned:

```text
ASSUMPTION_VERSION_EXACTLY_COMPENSATED
PRIMARY_CONSERVATIVE_CERTIFICATION_AND_PHYSICAL_FEEDBACK
RESERVE_OPERATIONAL_FOLIATION_QUOTIENT_AND_ACCESSIBILITY_BOUNDARY
STOP_EXACTLY_COMPENSATED_FOLIATION_EXISTENCE_TESTING_AND_UNCONDITIONED_HOST_BUILDING
FOLIATION_ROLE_INERT
```

The source audit confirmed that exact compensation has no confirmation route
inside the branch, while narrowing several supplied claims: current bounds
constrain declared leakage/access mechanisms rather than hidden existence in
general; gravity-sector sensitivities are heterogeneous and are not evidence
of a gravitational leak; preferred foliation alone is not the logical escape
from Wigner-friend no-go results; and causal invariance, consensus timestamps,
and the CMB rest frame are constructive or material controls rather than
evidence for a fundamental tick.

### Position 2 contract — completed

```text
N5-PF-P2
Conservative Certification and Physical Feedback Formal Spine
```

This retypes the provisional Position-2 role without deleting it. Freeze the
response-system state \(X\), formed record, complete certificate/verifier
apparatus \(K\), semantic certificate verdict \(C\), policy, complete physical
boundary input \(B\), its declared control quotient \(U\), response, reset,
and resources. Prove or refute:

\[
Y\perp C\mid X,B
\qquad\text{and}\qquad
Y\perp B\mid X,U.
\]

Do so in deterministic, stochastic-kernel, and quantum-process forms, then attack
hidden memory, route, hysteresis, occurrence identity, and modified dynamics.
Carry a matched preferred-leaf versus causal-partial-order control.

Maximum credit is a scoped factorization/necessity theorem, exact omitted-state
witness, or branch-invariant formation/certification non-unification. The
likely mathematical core is standard causal factorization and data
processing; novelty language must wait for the collision. No simulation or
hardware is needed.

## Position-2 completion receipt

The complete result is:

- [conservative certification, physical-boundary factorization, and
  control-quotient sufficiency](conservative-certification-physical-boundary-factorization-and-control-quotient-2026-07-27.md);
- [run receipt](../runs/2026-07-27-conservative-certification-physical-feedback.md);
- [exact regression](../tests/du_conservative_certification_feedback_probe.py);
  and
- [deterministic artifact](../tests/artifacts/du_conservative_certification_feedback_result.json).

Position 2 returned:

```text
CONSERVATIVE_CERTIFICATION_THEOREM
PHYSICAL_BOUNDARY_FACTORIZATION
DECLARED_CONTROL_SUFFICIENCY_IFF_VALID_POSTPROCESSING
CAPABILITY_WITHOUT_SOURCE_CHANGE
EXACT_EXTRA_BOUNDARY_STATE_WITNESSES
NO_COUNTEREXAMPLE_TO_CONSERVATIVITY_AFTER_COMPLETE_MATCH
MATHEMATICAL_CORE_FULLY_ABSORBED
FOLIATION_INERT
```

Once complete pre-response history and physical boundary input are fixed, a
semantic relabel cannot change the response without an omitted variable,
occurrence mismatch, or modified dynamics. A declared control label remains a
substantive quotient: deterministic fibre inclusion, a valid stochastic
postprocessing, or quantum process-link kernel containment is required. Exact
route/memory, positivity, and quantum-phase witnesses show how that quotient
can fail. Certification can nevertheless enlarge safe action capability
through ordinary physical policy and feedback while the source law remains
unchanged.

The stochastic collision also corrects one overstatement: marginal channel
factorization \(M=QT\) is a postprocessing result, not conditional
independence in an already existing joint experiment unless the joint kernel
itself factors.

The mathematical core is fully absorbed by quotient factorization,
comparison/postprocessing of experiments, and process-tensor/quantum-comb
linearity. No force, new physics, ontology, grade-5 remainder, prediction,
paper, or hardware result was earned.

### Position-3 and Position-4 completion; executable Position 5

```text
N5-PF-P3 COMPLETE AS HC-DU-056
N5-PF-P4 COMPLETE AS HC-DU-057
N5-PF-P5 PREFERRED-LEAF / PARTIAL-ORDER ADJUDICATION NEXT
```

For Position 3, an independently motivated physical antecedent \(A\) selects
the complete boundary response class exactly when:

\[
\ker A\subseteq\ker(\Sigma_{\mathcal A}\circ B).
\]

The current specimens satisfy that inclusion only for restricted action
families. Orientation/path and route/reset-lineage capabilities expose exact
first leaks. Physically freezing those fields closes the declared finite
classes without demanding future-invisible microscopic identity, but the
current antecedents do not derive the fields.

Position 4 then closes all three declared candidate families without a new
host. Consensus does not derive a physical finality rate; broad record
mergeability does not derive complementarity; and an unselected finite
threshold does not carry a universal critical exponent. Its corrected
four-way rate classification preserves new Lorentz-covariant dynamics as a
logical branch, but DU does not derive it and `PRED-DU-001/002` already carry
it conditionally.

The preferred foliation remains inert. Position 5 must now state what survives
unchanged across preferred-leaf and causal-partial-order arenas after complete
process, interface, access, finality, action, and resource matching.
