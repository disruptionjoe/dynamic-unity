---
title: "Science Council orthodox-professor primitive-record-rate wave"
status: completed_persona_swing
doc_type: council_persona_result
created: 2026-07-24
contract: explorations/science-council-primitive-record-rate-wave-contract-2026-07-24.md
persona: orthodox_professor
verdict: "A-ONLY ECHO UNDERDETERMINES THE CLOCK ARCHITECTURE / G-FULL DELAY-ONLY FAILS SPLIT-RECORD, REUNION, AND MOVING-LAB TESTS / G-GATED REMAINS LIVE WITH ADDED DYNAMICS"
banked: false
seeded: false
---

# Science Council orthodox-professor primitive-record-rate wave

## Executive result

The global-clock premise is worth taking literally. The cleanest orthodox
result is not that a clock is forbidden. It is that three materially different
claims had been running together:

1. there is a universal local rule by which each observer makes records, with
   no global synchronization (`L`);
2. there is one actual global update index and every observer makes exactly
   one physical record on every update (`G-full`); and
3. there is one actual global update index, but it supplies only update
   opportunities while motion, fields, or path-dependent dynamics govern
   which opportunities become physical observer records (`G-gated`).

An exact `1+1` benchmark produces a sharper result than either an immediate
rejection or an immediate endorsement:

> **The send-return echo seen by observer A is exactly the same in the
> reciprocal local-clock construction and in `G-full`. A's echo alone can
> therefore hide the global clock. The record made by reflector B, a
> same-relative-rapidity boost comparison, a reunion, and a moving-laboratory
> round trip separate them.**

For the exact `beta=3/5` fixture, A records `60 -> 240` in both branches. The
local-clock target has B reflect at `120`, giving reciprocal factors `2,2`.
`G-full` has B reflect at global record `150`, giving `5/2,8/5`. Exact
travel-time correction makes that `150` equal A's radar time, so latency
creates no residual clock difference. At reunion, the local relativistic
target gives `100` versus `80` records; `G-full` gives `100` versus `100`.

The minimum `G-gated` Lorentz-ether-style completion admits B's physical
records at `1/gamma=4/5` per global update and contracts moving rods by the
same factor. It repairs all finite fixtures used here. That is a genuine live
global-clock construction, but the repair comes from additional clock-and-rod
dynamics, not from message delay.

The executable probe passes `19/19` exact rational checks. Its result is:

```text
G-FULL DELAY-ONLY OVERTURNED AT THE CONVENTIONAL LORENTZ BENCHMARK
L-SR CONDITIONALLY RECONSTRUCTS THE KINEMATICS
G-GATED LORENTZ-ETHER RESCUE REMAINS LIVE BUT ADDS DYNAMICS
```

This is a formalization-level result. It does not reject global clocks in
general. In particular, Arrighi, Facchini, and Forets provide a positive
`1+1` antecedent in which a specifically designed Clock quantum walk and Clock
QCA are exactly discrete-Lorentz covariant. The next high-value question is
therefore whether the necessary gating/encoding can be generated by one
admitted rule and whether its global update index is physical, hidden, or
gauge.

No claim is banked or seeded.

## 1. Reconstruct the accumulated learning

### Complete source audit

I read the complete mandatory corpus before freezing the proposition. I did
not read any same-wave persona memo or use any same-wave probe to select or
design this swing.

| Corpus group | Sources audited | Result carried into this swing |
|---|---|---|
| Current wave, authority, and integrated state | The complete dual primitive-record-clock contract; `AGENTS.md`; `README.md`; all of `LANES.yaml` rev. 12; `explorations/concept-register.md`; the complete hardening/reuse map; the conditional-and-abductive research contract; `tests/README.md` | Conditional constructions, abductive comparisons, and nulls are legitimate non-ordinal warrants; no selector, unit map, generated scale, or `Lambda` identity may be inferred |
| First five-persona wave | Its contract, synthesis, divergent index, all five persona memos, all three probe sources, and all three artifacts | Normalized influence is buildable but not selected; the quadratic null reproduces the residue/scaling signature; external drive can sustain Bianconi activity only with imported cadence; `R`, `E`, and `U` are staged; direct birth-cardinality running fails full path covariance |
| Four Condorcet receipts | Complete vote and pairwise records | `COM-D1`, `WF-D1`, `HET-D6`, and `WF-D12` were allocation winners, not scientific results |
| Successor five-persona wave | Its contract, synthesis, divergent index, all five persona memos, all five probe sources, and all five artifacts | The physical-sector CSG post map is exact; the factorial half-power is conditional; other tails give other exponents; post recurrence and geometry remain open; fixed-history replay has a typed scope; raw Fisher retention is hidden-fibre nonidentifying |
| Preserved divergent corpus | Both indexes, covering all 102 prior unranked paths with provenance | The new premise could be checked against intervention-first influence, fixed-history replay, duplication-aware records, finite-`N` metrology, and work per issued record rather than treated as an isolated idea |
| Primary antecedents | The four linked arXiv records/papers for Knuth–Bahrenyi, Arrighi–Facchini–Forets, Bombelli–Henson–Sorkin, and Saravani–Aslanbeigi; Malament's theorem scope as delimited in the contract and bibliographic record | There are known conditional routes from observer-chain quantification to Lorentz structure, a known exact `1+1` discrete-covariance construction, and known distinctions among causal/conformal structure, volume information, statistical Lorentz invariance, regular lattices, and metric scale |

The executable artifacts were inspected semantically, not treated as
scientific warrant because they ran. The first-wave results remain in force:

- completion-specific influence decompositions exist on their declared
  carriers;
- normalization and replication scaling are not mechanism discriminators;
- an externally driven completion can sustain activity, but the source,
  cadence, anisotropy, and physical observable remain imported;
- response, embedding, and units are different dependencies;
- a direct law `p(N)` or `g(N)` indexed by birth cardinality is killed in the
  tested form because natural labelings of one causet get different path
  weights; and
- no existing result supplies dimensional `c`, a generated scale, gravity, or
  `Lambda`.

The successor results also remain in force:

- for a post with invariant past size `P`, the physical CSG sector
  `(T_1,T_2,...)` has the exact binomial map
  `T_n(P)=sum_k binom(P,k)t_(n+k)`;
- the map composes projectively and has known fixed families;
- `t_eff~sqrt(u/P)` belongs to the selected factorial tail and is not forced
  by post conditioning;
- other tail classes overturn a universal half-power;
- recurrence/generativity, held-out geometry, response identity, units, and
  cosmology remain open;
- a complete fixed effective law can replay one post era exactly, so such a
  replay cannot identify whether that law was fundamental or inherited from
  invariant prehistory; and
- raw-to-physical Fisher retention is not an order-only observable until its
  hidden microhistory fibre is physically fixed.

### Established, conditional, narrowed, and killed

**Established within prior declared models**

- exact decomposition and null identities;
- exact natural-labeling counterexamples to direct cardinality scheduling;
- the exact CSG post transformation and its projective algebra;
- tail-family dependence of the post asymptotic; and
- hidden-fibre nonidentifiability of the raw Fisher-retention ratio.

**Conditionally constructed**

- sustained driven Bianconi activity;
- CSG-like order/Fisher candidates;
- a factorial post hierarchy;
- replay and intervention controls; and
- various record/provenance and geometry passports.

**Narrowed**

- post work now starts at recurrence plus held-out geometry, not reinvention
  of the post map;
- information geometry requires a declared physical statistical experiment;
- Bianconi work requires a native source and selected mobility; and
- record claims require a distinction between physical history and
  bookkeeping fibre.

**Killed locally**

- terminal residue or a half-power by itself as a DU discriminator;
- direct birth-label/cardinality scheduling in the tested transitive-
  percolation formalization;
- a universal post half-power;
- raw Fisher retention as an order-only selector;
- a fixed-history replay as a universal origin detector; and
- any present dimensional-scale or `Lambda` promotion.

### Non-obvious cross-output relationships

The first relationship joins `COM-D9` and `PHIL-D4`. A's complete send-return
echo is a fixed-output replay: both `L-SR` and `G-full` reproduce it exactly.
It cannot identify the architecture that generated it. Adding B's physical
reflection record and the reunion comparison is the intervention-first move:
observe a variable on which the two hidden mechanisms disagree rather than
post-process the same terminal trace.

The second relationship joins `COM-D4`, `HET-D13`, and `HET-S2-D8`. The global
clock forces a distinction that prior work needed but had not operationalized:

```text
global update opportunity
    != durable physical observer record
    != copied/duplicated record
    != causal message carrying a record
```

`G-full` identifies the first two. `G-gated` separates them. That separation
can acquire a provenance ledger and possibly a resource cost per admitted
record. It is not merely vocabulary because the two typings make different
reunion and boost predictions.

The third relationship is with the path-covariance kill. A fundamental global
`K` is not automatically an illicit birth label. If `K` is a physical
foliation, dependence on it is a substantive preferred-frame theory and must
be tested empirically. If `K` is gauge, physical predictions must quotient it
out. The earlier `p(N)` counterexample applies when supposedly gauge
natural-label paths receive different physical weights; it does not prove
that no theory may have a physical clock.

## 2. Audit the prior prioritization

### Most strengthened

The new premise most strengthens the philosopher-successor correction to
`COM-D9`, together with `PHIL-D4`: replay is a typed control, not a universal
absorber, and mechanism-sensitive interventions are required. The exact
echo provides a new, unusually clean instance. A-only data are identical;
the returned B record and reunion data discriminate.

It also strengthens the commercial preference for compact decision packets
over open-ended construction. The `19/19` tournament is a small passport with
three branches, exact observables, five null classes, falsifiers, and explicit
stops.

### Most weakened

The new premise weakens the successor synthesis only as an **exclusive
allocation conclusion**: post generativity followed by held-out geometry is
no longer the only immediate high-information route. The record-clock
question admits a much cheaper exact discriminator and now has a positive
discrete-covariance antecedent.

It does not weaken the exact post map, tail-family result, or post roadmap.
Those results concern invariant history-to-law transduction; this swing
concerns observer records, message exchange, and elapsed-time comparison.
They should remain separate until an admitted DU construction maps one into
the other.

### Previously low-ranked path newly live

`HET-D13`, work per issued record, becomes newly live after a scope change.
The first object should not yet be cosmological work. It should be a finite
ledger that distinguishes:

- one global update opportunity;
- one admitted durable clock/detector record;
- one rejected or gated opportunity; and
- one transmitted copy of an existing record.

If the resource ledger cannot distinguish those events, `G-gated` risks
becoming pure relabeling. If it can, the ledger may help select which notion
of “record” is physically admissible. `COM-D4` is the necessary provenance
partner, and `ORTH-D8` remains the later dimensional-calibration partner.

## 3. Type and stress-test both premise branches

### Operational definitions

| Term | Definition used here | What is not granted |
|---|---|---|
| Observer | A persistent subsystem or worldline process that can store an ordered durable counter and send/receive a causal message | Fundamental observer ontology |
| Physical record | An irreversible counter/detector increment locally available to that observer and reportable in a later message | Every substrate update; every mathematical state transition; a coordinate timestamp |
| Global update | Branch-G substrate index `K` ordering complete update slices | That ordinary observers directly read `K`; that `K` is gauge; that its foliation is hidden |
| Record equivalence | Equal increments use the same local transition rule and are calibrated at co-location against the same reference process | Equality of rate per an unobserved external time |
| Causal precedence | The partial order generated by physically possible influence/message edges | A total order or simultaneity surface |
| Logical-clock order | A counter ordering consistent with causal precedence, such as local record order plus message updates | Proper time, metric duration, or physical global synchronization |
| Order used in the probe | Each observer has a local total record order; message edges create a causal partial order; Branch G additionally has total order by `K` | A metric or simultaneity relation from order alone |
| Local cadence | One record per admitted local transition opportunity | A rate before a denominator is specified |
| Branch-L denominator | The observer's own local transition opportunity/record step; there is no cross-observer phase or global tick | Proper time as a derived theorem |
| `G-full` denominator | One physical record per global `K` tick: `Delta n_i=Delta K` for every observer | Lorentz clock behavior |
| `G-gated` denominator | One global update opportunity per `K`, with physical admission fraction `a_i(K,path,field)`; the finite rescue uses `a=1/gamma(v)` | A derivation or unique law for `a` |
| Message | A physical carrier with send, reflection/receipt, and return events; it may carry B's local record value | Instantaneous comparison or common synchronization |
| Simultaneity convention | A rule assigning a distant event a coordinate time; the displayed radar midpoint uses Einstein synchronization | A remotely read physical clock value |
| One-way latency | Coordinate-time difference from send to receipt under a chosen synchronization | Synchronization-independent observable by itself |
| Round-trip latency | A's local elapsed records from send through reflection to return | The split of that interval into two unique one-way times |
| Radar time | `(n_s+n_r)/2` under Einstein midpoint synchronization | B's physical record; unique simultaneity |
| Radar distance | `(n_r-n_s)/2` in declared record-tick units | Independently calibrated spatial distance |
| Independent spatial distance | A rod/metric standard fixed separately from the same round-trip timing convention; the moving-lab fixture declares rest length `12` | A derived DU length or dimensional unit |
| Proper-time target | Path-dependent elapsed count displayed by an ideal relativistic clock between shared endpoints | Something derived from “one local record per local tick” alone |
| Coordinate time | A chosen event-coordinate assignment; `K` is the preferred coordinate in the tested G models | Direct observability in all branches |
| Limiting slope | The dimensionless signal slope set to `1` in the declared coordinates | The dimensional value of physical `c` |
| Observer memory/detector record | The durable local state that stores an increment and can later be carried in a message | A transient scheduler state or an unrecorded update opportunity |

Disconnected observers can instantiate the same local law or be driven by the
same global updater, but they cannot empirically compare phase, offset, or
rate until a causal exchange or reunion. Branch G may still posit a fact about
their common `K`; the posit is simply nonidentifying for disconnected
observers unless some later observable retains it.

### Exact clause instantiation

| Clause | Instantiation in this swing | Status |
|---|---|---|
| `RR-S1` | Each admitted observer has ordered, durable, reportable physical record events | Assumed |
| `RR-L1` | Identical local transition rules add one record per local opportunity; the denominator is local and carries no global phase | Assumed |
| `RR-L2` | Branch L has no synchronization until messages define an operational comparison | Assumed |
| `RR-G1` | One actual fundamental `K` updates all sites. In `G-full` its foliation leaks through split records and moving-lab metrology. In `G-gated` it is physical but hidden on the tested finite observables by added dynamics | Assumed, then tested for observability |
| `RR-G2` | `G-full` has `dn_i/dK=1`; `G-gated` has update opportunities at every `K` but physical admission `dn_i/dK=a_i` | Assumed split |
| `RR-G3` | Message propagation creates send/receipt offsets. The contested stronger claim is that these offsets alone also account for relativistic elapsed-time differences | Propagation part derived; stronger part tested and overturned for `G-full` |
| `RR-G4` | The tested `K` defines physical Euclidean slices. A gauge-clock alternative is not represented by `G-full` and remains open | Assumed for tested formalization |
| `RR-S2` | Comparison occurs only through causal messages carrying local counts or through shared reunion endpoints | Assumed |
| `RR-S3` | Raw latency must survive exact delay subtraction and a zero-delay reunion before it may count as clock accumulation | Test burden enforced |
| `RR-S4` | Counts, velocities, and the limiting slope are dimensionless. The moving-rod length is independently declared but no physical unit is calibrated | Assumed |
| `RR-S5` | The same rule can apply to disconnected observers, but no relative phase is observable without later contact | Assumed |
| `RR-S6` | All three branches are conditional candidates; none is a present DU derivation | Assumed research status |

### Strongest Branch-L and Branch-G versions

The strongest Branch L used here is not bare cadence. It is `L-SR`:

- identical locally calibrated physical counters;
- no global synchronization;
- a reciprocal limiting-signal exchange law;
- multiplicative exchange factors; and
- the conventional special-relativistic reunion target.

From those additions, Bondi-style `k` calculus reconstructs rapidity,
`beta`, and `gamma`. The reciprocity and signal cone do real work. Local
cadence alone does not derive them.

The strongest Branch G is not `G-full`. It is a family:

- `G-full`: an actual preferred `K`, Euclidean slices, signal slope `+/-1`,
  and exactly one physical record per observer per `K`;
- `G-gated`: the same actual preferred `K`, but physical clocks and rods have
  motion/path-dependent dynamics; and
- a more general encoded global scheduler, exemplified by the particular
  Clock QW/QCA antecedent, where exact discrete covariance is a property of
  evolution plus encodings and parameter transforms.

`G-gated` and encoded-QCA routes must not be killed by a failure of
`G-full`. Conversely, they do not get the result for free: their encoding,
gating, rod response, observer semantics, and held-out covariance must be
derived or independently motivated.

### Primary antecedent audit

[Knuth and Bahrenyi](https://arxiv.org/abs/1005.4172) begin with a causal
event order, then select two coordinated observer chains, assign valuations,
and project events onto those chains. Their pair quantification yields a
Minkowski scalar, Lorentz transformations, and an invariant maximum speed.
For this wave, the important correction is that the event postulate does not
act alone: chain coordination, valuation, and the projection scheme supply
the operational structure that bare local cadence lacks.

[Arrighi, Facchini, and Forets](https://arxiv.org/abs/1404.4499) formalize
discrete Lorentz transforms in `1+1` dimensions by replacing lattice points
with lightlike patches and requiring evolution to commute with isometric
encodings and transformed parameters. Their Dirac quantum walk is only
first-order covariant in the tested formulation, whereas their specifically
constructed Clock quantum walk and Clock QCA are exactly discrete-Lorentz
covariant. This is decisive positive prior art against “a synchronous
fundamental update is automatically impossible.” It is not yet a proof that
the update index is an observable global clock or that its clock degrees of
freedom are the durable observer records meant here.

[Bombelli, Henson, and Sorkin](https://arxiv.org/abs/gr-qc/0605006) show that
an intrinsic measurable construction from a Poisson sprinkling cannot select
a spacetime direction. That is an ensemble/intrinsic-construction result, not
a result for a regular globally updated lattice.
[Saravani and Aslanbeigi](https://arxiv.org/abs/1403.6429) distinguish
causal/conformal information, statistical number-volume information, and
regular-lattice frame effects, with special qualified `1+1` constructions
that should not be generalized to higher dimensions.
[Malament](https://doi.org/10.1063/1.523436) supplies a theorem-level route
from causal structure to topology, differentiable structure, and conformal
structure under its spacetime assumptions; it does not supply conformal
scale or the observer-record calibration.

## 4. Select one contested proposition

I froze the following proposition after the complete corpus and revised
dual-branch contract were read, and before inspecting any same-wave output:

> **`ORTH-RR-P1`. `G-full`, with every observer adding exactly one physical
> record per global tick and every apparent inter-observer time difference
> caused solely by finite message propagation, can reproduce reciprocal
> Lorentz exchange, boost invariance of the complete record comparison, and
> unequal reunion elapsed times without an additional motion-dependent record
> mechanism.**

This became newly testable because:

1. the user's premise identifies a concrete cadence denominator, `K`;
2. the prior replay/intervention work says which extra observable can expose
   two mechanisms with one common terminal trace;
3. the contract requires the reflector's record and reunion count, not only
   A's round trip;
4. the known Clock-QCA antecedent prevents a failure from being inflated into
   a no-global-clock claim; and
5. the `G-full`/`G-gated` split identifies exactly which added mechanism a
   rescue would need.

Evidence that would change the disposition would be:

- an exact `G-full` construction retaining `dn_i/dK=1` that gives different
  physical counts to reunited observers sharing the same initial and final
  events without changing the meaning of record;
- a complete boost transformation under which B's `G-full` split record is
  invariant while `K` remains physical and no clock gating or encoding is
  introduced; or
- a demonstration that the target physical clocks do not count durable
  records at all, in which case the proposition's ontology—not its
  arithmetic—would have to be replaced.

The Arrighi Clock QW/QCA can change the broader Branch-G outlook but does not
by itself uphold this exact `G-full` proposition because its encoding
degrees of freedom have not been identified with one durable record per
global update for every observer.

## 5. Take one bounded evidence-producing swing

### Exact construction

The probe
`tests/du_record_rate_orthodox_clock_tournament.py` uses exact rational
arithmetic at:

```text
(beta, gamma, k) =
    (5/13, 13/12, 3/2),
    (3/5,   5/4, 2),
    (8/17, 17/15, 5/3).
```

For `L-SR`, reciprocal exchange gives:

```text
k^2 = (1+beta)/(1-beta)
beta = (k^2-1)/(k^2+1)
gamma = (k+k^-1)/2
m_B = k n_s
n_r = k m_B = k^2 n_s.
```

For `G-full`, put A and B on preferred slices with A at rest, B at
`x=beta K`, and signal slope `+/-1`. Then:

```text
m_B = n_s/(1-beta)
n_r = n_s(1+beta)/(1-beta)
p_G = m_B/n_s = 1/(1-beta)
q_G = n_r/m_B = 1+beta.
```

The A-only product is identical:

```text
p_G q_G = n_r/n_s = k^2.
```

The split is not reciprocal. For `G-gated`, admitting physical B records at
`1/gamma` per `K` gives:

```text
m_B^physical = n_s/[gamma(1-beta)] = k n_s
p_G-gated = q_G-gated = k.
```

That exact equality is a constructive rescue and an explicit price: global
update opportunities are no longer identical to physical records.

### Common fixture at `beta=3/5`

| Quantity | `L-SR` | `G-full` | `G-gated` |
|---|---:|---:|---:|
| A send record `n_s` | 60 | 60 | 60 |
| B reflection record `m_B` | 120 | 150 | 120 |
| A return record `n_r` | 240 | 240 | 240 |
| Outward factor | 2 | `5/2` | 2 |
| Inward factor | 2 | `8/5` | 2 |
| A-only product | 4 | 4 | 4 |
| A radar time | 150 | 150 | 150 |
| A radar distance | 90 | 90 | 90 |
| Reunion A records over `Delta K=100` | 100 | 100 | 100 |
| Reunion B records | 80 | 100 | 80 |
| Moving-lab two-way slope in its own rod/record units | target 1 | `16/25` | 1 |
| Added mechanism beyond propagation | reciprocal local-clock law | none | clock admission plus rod contraction |

The table exposes the key underdetermination. If B returns no local count, A's
entire record is identical in all three columns. If B returns its count,
`G-full` is separated at one reflection event.

### Rapidity and composition

Multiplicative local exchange factors compose:

```text
k_1=2
k_2=3/2
k_12=3
beta(k_12)=4/5
             =(beta_1+beta_2)/(1+beta_1 beta_2).
```

This conditionally reconstructs Einstein velocity composition. It is not a
derivation from cadence because reciprocity, multiplication of exchange
factors, and the limiting signal law were admitted.

### Executable result

The probe passes `19/19` checks and writes a complete conditional-candidate
artifact. Check completion means that assumptions, choices, comparators,
nulls, falsifiers, stops, and exact calculations are legible. It is not a
scientific endorsement.

The `G-gated` rescue is exact as a kinematic **rate law**. The selected
`beta=3/5` echo and reunion have rational `gamma=5/4`, so their finite counts
can be implemented in periodic five-update blocks. An arbitrary boost can
make `1/gamma` irrational. A literal discrete implementation would then need
an oscillator phase, deterministic discrepancy bound, or stochastic
admission law. None is supplied here. That missing microimplementation is one
reason the rescue is constructive rather than derived.

## 6. Run synchronization/preferred-frame and delay-only nulls

### Delay-only null

For the common echo:

```text
t_radar = (60+240)/2 = 150
rho_radar = (240-60)/2 = 90.
```

`G-full` gives B's record `m_B=150`. Exact travel-time correction therefore
erases the entire apparent separated-clock difference:

```text
t_radar - m_B = 0.
```

`L-SR` gives B's physical record `120`; the ratio `150/120=5/4=gamma`
survives the delay correction. This is conditional kinematic recovery, not a
derivation of why the local clock follows proper time.

### Synchronization/reslicing null

Reassigning the reflection coordinate within the same round trip with
`epsilon={1/4,1/2,3/4}` gives:

| `epsilon` | Outward one-way slope | Inward one-way slope | Round-trip slope |
|---:|---:|---:|---:|
| `1/4` | 2 | `2/3` | 1 |
| `1/2` | 1 | 1 | 1 |
| `3/4` | `2/3` | 2 | 1 |

The physical A and B counts do not change. One-way speed and simultaneity are
therefore conventional in this fixture; the round trip and carried records
are the appropriate observables.

### Preferred-frame/boost null

The first draft of the benchmark considered two pairs with the same
Galilean velocity difference. That is not the right Lorentz-invariance
control, so it was rejected before the memo was written.

The corrected exact pairs are:

```text
(u,v) = (0,3/5)
(u',v') = (1/5,5/7).
```

They are related by a common Lorentz boost and both have Einstein-relative
`beta_rel=3/5`. For arbitrary preferred-frame velocities:

```text
p_G = (1-u)/(1-v)
q_G = (1+v)/(1+u).
```

The result is:

| Branch and frame | Outward factor | Inward factor | A-only product |
|---|---:|---:|---:|
| `G-full`, `(0,3/5)` | `5/2` | `8/5` | 4 |
| `G-full`, `(1/5,5/7)` | `14/5` | `10/7` | 4 |
| `G-gated`, `(0,3/5)` | 2 | 2 | 4 |
| `G-gated`, `(1/5,5/7)` | 2 | 2 | 4 |

This is more informative than an A-only preferred-frame test:

- A's product is invariant and hides the frame even in `G-full`;
- B's split record reveals the preferred frame in `G-full`; and
- velocity-dependent physical-record gating hides it on this finite fixture.

The last point is constructive, not universal. It has not established
higher-dimensional covariance, dynamics, gravity, or the absence of
dispersion.

### Reunion null

A and B share `E_0`, follow distinct histories, and share `E_1`. The
piecewise-inertial target uses an instantaneous turnaround and total preferred
duration `Delta K=100` at `|beta|=3/5`.

```text
L-SR target:  Delta n_A=100, Delta n_B=100/gamma=80
G-full:       Delta n_A=100, Delta n_B=100
G-gated:      Delta n_A=100, Delta n_B=80.
```

There is no signal delay at either common endpoint. `G-full` cannot turn the
equal counts into unequal elapsed time without changing its exact one-record-
per-`K` rule. `G-gated` repairs the result by leaving twenty global update
opportunities without B physical records.

### Independently declared moving-laboratory distance

Give the moving laboratory a rest-calibrated rod length `L=12`.
`G-full` holds that preferred-frame separation fixed and measures:

```text
Delta K_roundtrip = 2L/(1-beta^2) = 75/2
c_2way_measured = 2L/Delta K_roundtrip = 16/25.
```

The measured two-way slope depends on absolute velocity. In the minimum
`G-gated` completion:

```text
L_preferred = L/gamma = 48/5
Delta K_roundtrip = 30
Delta n_physical = Delta K_roundtrip/gamma = 24
c_2way_measured = 2L/Delta n_physical = 1.
```

The invariant measured slope is restored by the joint clock-and-rod law, not
by redefining radar distance. The number `1` remains dimensionless because no
independent physical unit map was supplied.

## 7. Reconcile the result by grade and type

### Proposition disposition

`ORTH-RR-P1` is **OVERTURNED FOR THE DECLARED `G-full` FORMALIZATION**.

It is not a no-go theorem for:

- a hidden Lorentz-ether clock with dynamical clock/rod response;
- an exactly covariant encoded QCA;
- a gauge update index;
- a stochastic globally scheduled causal process whose physical records are
  not scheduler ticks; or
- an order-plus-volume reconstruction under additional assumptions.

### Grade separation

| Grade | Earned result | Unresolved burden |
|---|---|---|
| Operational definition | Send, B reflection record, return, radar midpoint, radar distance, split exchange factors, reunion counts, and moving-lab round trip are distinct | Which DU subsystem is an observer and which transition is a durable record |
| Conditional construction | Exact `L-SR`, `G-full`, and finite `G-gated` comparators | Selection of any branch by an admitted DU law |
| Kinematic recovery | `L-SR` and `G-gated` reproduce the declared finite Lorentz targets; `G-full` reproduces only A's echo product | Continuum, higher-dimensional, non-inertial, dispersive, and gravitational recovery |
| Dynamical selection | None | Derive the reciprocal signal law or the G-gating/rod law, including a discrete admission mechanism for irrational rates, without inserting `gamma`, a preferred velocity fit, or the target transform |
| Dimensional calibration | None; all record and slope quantities are dimensionless | Independent length/time standard, density, scale, and unit map |
| Physical prediction | A formal discriminator: if B's durable record and reunion clock behave Lorentzian, delay-only `G-full` is insufficient | Actual experimental identification, DU mapping, gravity, and cosmology |

### Warrant separation

- **Derived within assumptions:** all rational echo, boost, reslicing,
  reunion, and moving-light-clock identities.
- **Conditionally entailed:** reciprocal `k` calculus reconstructs
  `beta`, `gamma`, and Einstein composition once the reciprocal signal law is
  admitted.
- **Constructively realized:** `G-gated` repairs every declared finite
  fixture using `1/gamma` clock admission and `1/gamma` rod contraction.
- **Comparative abduction:** the B-record/reunion packet favors `L-SR` or a
  dynamically completed G branch over delay-only `G-full` if the conventional
  Lorentz targets are the data.
- **Established antecedent, different construction:** exact `1+1`
  discrete-Lorentz covariance for the particular Clock QW/QCA.
- **No warrant:** a DU selector, dimensional `c`, gravitational dynamics,
  observer ontology, generated cosmological scale, or `Lambda`.

## 8. Write the next-actions roadmap and ledgers

### Dependency-ordered roadmap

| Order | Action | Concrete output | Falsifier or stop |
|---:|---|---|---|
| 1 | Put physical observer/record semantics onto the exact Clock QW/QCA antecedent | A small circuit/event benchmark reporting global update index, encoded observer clock, B reflection record, reunion record, and the exact covariance map | Stop calling it an instance of the user's premise if `K` is only a simulation coordinate or if “record” is not one durable observer increment; reject the candidate mapping if held-out split/reunion covariance fails |
| 2 | Attempt a genuine `G-gated` microdynamics without inserting `gamma` | One local update rule, fixed before the target sweep, that jointly produces discrete clock admission with a discrepancy law, rod response, and signal propagation over rational and irrational-`gamma` boosts | Stop if velocity, `gamma`, rod contraction, or the desired Lorentz transform is explicitly supplied; kill the formalization if one held-out boost or reunion fails |
| 3 | Type substrate opportunities, durable records, copies, and messages | A duplication-aware finite state machine plus resource/provenance ledger, linked to `COM-D4` and `HET-D13` | Stop if retyping changes no observable or resource balance; reject a work claim if “work per record” is bookkeeping |
| 4 | Run a multi-observer covariance packet | Three-observer echo triangle, rapidity-composition closure, common-boost test, reunion loop, and asynchronous-message perturbation | Stop the candidate if closure depends on absolute frame, arbitrary synchronization, or fitted latency |
| 5 | Move surviving constructions beyond `1+1` | A `2+1`/`3+1` orientation and dispersion tournament comparing a regular global scheduler, any encoded QCA candidate, and a statistically Lorentz-invariant causal ensemble | Do not proceed to gravity if orientation, lattice, or high-energy dispersion exposes a preferred frame incompatible with the target |
| 6 | Audit order-plus-number metrology | An assumption passport separating causal/conformal reconstruction, number-volume statistics, density, manifoldlikeness, observer calibration, and physical units | Stop if order alone is said to fix scale, or if density/units are fitted after seeing the target |
| 7 | Only then attempt the DU bridge | A typed map from an admitted GU/DU event or source transition to observer, update opportunity, physical record, message, and response | Stop at structural analogy if the map has no intervention, held-out observable, or path-covariance receipt; do not infer `Lambda` |

The post-generativity and held-out-geometry program should continue as a
separate line. This record-clock packet earns one bounded allocation because
it is cheap and discriminating; it does not supersede the post results.

### Hypothesis and possible-claim ledger

| ID | Statement | Warrant/status | Evidence that would upgrade or overturn it |
|---|---|---|---|
| `ORTH-RR-H1` | A-only send-return echoes underdetermine `L-SR` versus `G-full` | `DERIVED / EXACT FINITE` | Overturn by adding an A-only observable not algebraically fixed by the same signal path |
| `ORTH-RR-H2` | The reflector's physical record separates the two branches on the common fixture | `DERIVED / EXACT FINITE` | Overturn by an exact `G-full` transformation preserving one record per `K` and the split factors |
| `ORTH-RR-H3` | `G-full` gives equal counts at reunion | `DEDUCTIVE FROM DEFINITION` | It changes only if shared endpoints, physical record, or one-per-`K` is changed; that is a new formalization |
| `ORTH-RR-H4` | Delay alone cannot reproduce path-dependent reunion aging | `DEDUCTIVE FOR G-full` | A counterexample with zero endpoint delay and no gating/path-dependent record law |
| `ORTH-RR-H5` | A global scheduler can be hidden on finite SR observables by added clock-and-rod dynamics | `CONSTRUCTIVELY_REALIZED / CONDITIONAL` | Upgrade by deriving the dynamics; overturn the implementation with a held-out boost, orientation, dispersion, or acceleration test |
| `ORTH-RR-H6` | Exact discrete Lorentz covariance is compatible with at least one specifically encoded synchronous `1+1` Clock QW/QCA | `ESTABLISHED ANTECEDENT` | The open issue is semantic and physical: map scheduler, observer, and durable record |
| `ORTH-RR-H7` | Coordinated observer chains and valuation can conditionally reconstruct Lorentz structure from causal order | `ESTABLISHED ANTECEDENT / ASSUMPTION-SENSITIVE` | Audit which coordination/additivity/projection axioms are independently motivated in DU |
| `ORTH-RR-H8` | A DU law may generate the G-gating factor rather than import it | `CONJECTURE / OPEN` | One fixed microscopic rule predicting held-out record ratios and rod response |
| `ORTH-RR-H9` | Opportunity-versus-record typing may connect provenance and resource closure | `COMPARATIVE ABDUCTION / OPEN` | A finite intervention in which the two typings have different durable outcomes or work ledgers |
| `ORTH-RR-H10` | Causal order plus event number may recover conformal plus statistical volume information | `ESTABLISHED ANTECEDENT UNDER SPACETIME/SAMPLING ASSUMPTIONS / DU MAP OPEN` | A declared manifoldlike ensemble, density, calibration, and error law |
| `ORTH-RR-H11` | Any present record-rate result identifies dimensional `c`, gravity, or `Lambda` | `NO WARRANT` | Requires independent metrology, dynamics, observable identity, and held-out physical evidence |

### Stop and reopener ledger

| Route | Current disposition | Exact reopener |
|---|---|---|
| `G-full` delay-only as a recovery of conventional Lorentz clocks | `STOPPED FOR THIS FORMALIZATION` | A zero-delay reunion and split-record construction that retains exactly one physical record per global `K` |
| All global-clock theories | `NOT STOPPED` | Not applicable; `G-gated` and exact Clock-QCA antecedents remain live |
| A-only radar echo as architecture selector | `STOPPED` | An additional A-local observable not fixed by the same round-trip path |
| Radar `c=1` as independent prediction | `STOPPED` | Independent distance/metric calibration plus nontrivial invariance or selected cone dynamics |
| Message offset as reunion time dilation | `STOPPED` | A residual difference at shared endpoints after exact latency removal |
| Bare local cadence as a derivation of proper time | `STOPPED` | Independently derive reciprocity, signal cone, clock response, and path law |
| `G-gated` as explanation rather than fit | `OPEN BUT UNPROMOTED` | Derive admission and rod dynamics from one rule fixed before held-out tests |
| Regular globally updated lattice as Lorentz invariant by assertion | `STOPPED` | Correct boost/orientation/dispersion tests or an exact covariance theorem with physical observable mapping |
| Direct `p(N)`/`g(N)` birth-cardinality running | `REMAINS KILLED IN TESTED FORM` | A full-history path-covariant reformulation, not a renamed global clock |
| Dimensional `c`, gravity, or `Lambda` from the record clock | `STOPPED` | Independent units, gravitational dynamics, response identity, and observational map |

### Newly visible or previously misused outcomes

1. **The A-only echo is more deceptive than expected.** It not only matches
   across `L-SR` and `G-full`; its product is invariant under the corrected
   same-relative-rapidity boost. The preferred frame is exposed by the split
   record, not the product.
2. **A returned local count is a minimal intervention-first observable.**
   Message content, not merely message timing, distinguishes architectures.
3. **Exact delay correction is constructive information.** In `G-full`,
   B's record equals A's radar time; the model explains propagation but leaves
   no relativistic clock residue.
4. **Reunion prices the rescue.** A global scheduler survives only by
   separating update opportunities from physical records or adding another
   path-dependent mechanism.
5. **Clock gating and record ontology are the same dependency viewed from two
   sides.** If gated-out ticks are still called physical records, the reunion
   repair disappears.
6. **A global `K` and an arbitrary natural labeling are not interchangeable.**
   The first is a possible physical structure; the second is gauge in CSG.
   The correct question is observability and covariance, not prohibition by
   vocabulary.
7. **The exact Clock QW/QCA antecedent changes the burden.** The useful target
   is no longer “can any discrete global evolution be covariant?” but “can
   its encoding be given the physical observer-record semantics and broader
   limits required here?”
8. **The successor post program and this clock program meet only at a future
   typed record map.** Shared words such as event, count, history, or record
   do not yet identify their objects.

### Unresolved disagreements

With the literal premise, the disagreement is narrow and useful. If “all
observers add new records at the same fundamental rate” means exactly one
physical record for every global `K`, the conventional reunion target cannot
be recovered by distance-dependent message delay. If it means every observer
receives one global update opportunity while local dynamics determines
durable record admission, it is `G-gated` and remains live.

With the successor synthesis, I disagree only that the post program should
monopolize the next immediate allocation. The exact post results remain
correct. A record-clock/QCA semantic audit and a no-`gamma` G-gating attempt
are now comparably crisp, cheaper tests.

With the prior vote, `PHIL-D4 + COM-D9` and `HET-D13 + COM-D4` were
undervalued as combinations. The new benchmark makes both combinations
operational.

### Files, commands, and sources

Files created:

- `explorations/science-council-orthodox-primitive-record-rate-wave-2026-07-24.md`
- `tests/du_record_rate_orthodox_clock_tournament.py`
- `tests/artifacts/du_record_rate_orthodox_clock_tournament_result.json`

Validation and inspection commands:

```text
.venv/bin/python -m py_compile tests/du_record_rate_orthodox_clock_tournament.py
.venv/bin/python tests/du_record_rate_orthodox_clock_tournament.py
jq ... tests/artifacts/du_record_rate_orthodox_clock_tournament_result.json
git diff --check -- tests/du_record_rate_orthodox_clock_tournament.py
shasum -a 256 tests/du_record_rate_orthodox_clock_tournament.py \
  tests/artifacts/du_record_rate_orthodox_clock_tournament_result.json
```

Reproduced result:

```text
19/19 exact checks pass
candidate contract: COMPLETE (not scientific endorsement)
```

SHA-256:

```text
8eff3ded4641733d0338758a5f5876f002e7504a898ce95cffc3c12093cad665  tests/du_record_rate_orthodox_clock_tournament.py
2b59caa6203019836ddd45474fd6a40d6877b3378e41c38c14c2043957acb623  tests/artifacts/du_record_rate_orthodox_clock_tournament_result.json
```

Primary sources and antecedent records used:

- [Knuth and Bahrenyi, *A Derivation of Special Relativity from Causal
  Sets*](https://arxiv.org/abs/1005.4172);
- [Arrighi, Facchini, and Forets, *Discrete Lorentz covariance for Quantum
  Walks and Quantum Cellular Automata*](https://arxiv.org/abs/1404.4499);
- [Bombelli, Henson, and Sorkin, *Discreteness without symmetry breaking: a
  theorem*](https://arxiv.org/abs/gr-qc/0605006);
- [Saravani and Aslanbeigi, *On the Causal Set-Continuum
  Correspondence*](https://arxiv.org/abs/1403.6429); and
- [Malament, *The class of continuous timelike curves determines the topology
  of spacetime*](https://doi.org/10.1063/1.523436).

## 9. Final unconstrained divergent pass

These are memory items, not a ranking or commitment.

| Stable ID | Idea or question | Why it might matter | First object, observation, or calculation | Collision with a known stop or failed class | Earlier relationship | Status |
|---|---|---|---|---|---|---|
| `ORTH-RR-D1` | Physical-record semantics for the exact Clock QCA | It is the strongest known positive answer to the claim that synchronous discreteness and exact covariance are incompatible | Re-express one Clock-QCA boost as global update opportunities, encoded observer clock records, a carried B record, and a reunion | Do not equate simulation step, internal clock state, and durable observer record without a map | Extends the positive antecedent; connects `PHIL-D2` and `COM-D4` | `UNRANKED / UNCOMMITTED / NOT_A_CLAIM` |
| `ORTH-RR-D2` | Derive G-gating from one local reversible or dissipative update law | A derivation could turn Lorentz-ether compensation from a fit into a mechanism | Search a minimal automaton/QCA parameter family for emergent `1/gamma` clock admission and rod response on held-out rational boosts | Inserting `gamma`, target contraction, or a preferred-velocity lookup table repeats the anti-circularity failure | Extends `HET-D13`, `COM-D6`, and `HET-S2-D8` | `UNRANKED / UNCOMMITTED / NOT_A_CLAIM` |
| `ORTH-RR-D3` | Reunion holonomy on an observer-message graph | Path-dependent elapsed records may be expressible without presupposing coordinates | Three worldline chains with two reunions; compare products of exchange factors and accumulated local counts around the loop | A Lorentz transform inserted as edge data is not emergence | Combines `PHIL-D4`, `COM-D9`, and `WF-D10` | `UNRANKED / UNCOMMITTED / NOT_A_CLAIM` |
| `ORTH-RR-D4` | Opportunity/record/copy provenance algebra | It could identify which “new records” are physically new and which are scheduler bookkeeping or message duplication | A four-event DAG with one update opportunity, one gated miss, one durable event, and one transmitted copy; compute invariant provenance classes | Another normalized count proxy without intervention or resource difference is stopped | Directly extends `COM-D4`, `PHIL-D3`, and `HET-S2-D8` | `UNRANKED / UNCOMMITTED / NOT_A_CLAIM` |
| `ORTH-RR-D5` | Rapidity triangle and curvature/holonomy test | Pairwise echoes can all look Lorentzian while failing global multi-observer consistency | Require `k_AB k_BC k_CA=1` in a flat triangle, then perturb one causal loop and measure closure failure | Fitted pairwise factors or arbitrary synchronization can manufacture closure | Relates `WF-D10`, `ORTH-D9`, and the present split-record null | `UNRANKED / UNCOMMITTED / NOT_A_CLAIM` |
| `ORTH-RR-D6` | One global `K` plus field-dependent admission as a gravitational-redshift candidate | It is the natural next extension if velocity-dependent gating survives | Two stationary clock chains at different potential proxies, exchange messages, reunite after transport, and test universality of the admission law | A chosen redshift factor or background metric simply imports general relativity | Connects `PHIL-D4`, `HET-D13`, and `ORTH-D8` | `UNRANKED / UNCOMMITTED / NOT_A_CLAIM` |
| `ORTH-RR-D7` | Higher-dimensional globally scheduled but direction-free causal dynamics | The `1+1` exact constructions may rely on special lightlike factorization | Compare orientation spectra and boosted local statistics of a QCA candidate and a Poisson causal ensemble in `2+1` | One regular lattice realization cannot establish Lorentz invariance; `1+1` results cannot be generalized silently | Relates the Bombelli theorem, Saravani–Aslanbeigi qualifications, and `COM-D11` | `UNRANKED / UNCOMMITTED / NOT_A_CLAIM` |
| `ORTH-RR-D8` | Identifiability of the phase of a hidden global clock for disconnected observers | A real global fact may remain permanently unobservable unless a later interaction stores phase | Construct two globally shifted `K` histories with identical local records and all later causal-message transcripts; search for an invariant separator | Do not call nonidentifiability proof that `K` is unreal; do not call an unobservable phase explanatory | Extends `PHIL-D2`, `COM-D9`, and `RR-S5` | `UNRANKED / UNCOMMITTED / NOT_A_CLAIM` |
| `ORTH-RR-D9` | Causal fanout bound plus independent metrology | A selected propagation cone could make invariant speed substantive instead of radar-definitional | Derive a finite propagation/Lieb–Robinson-type bound from one update rule, then calibrate it with an independently declared rod and record standard | Setting graph distance equal to tick count and reporting `c=1` repeats the radar tautology | Extends `ORTH-D8` and the present moving-lab control | `UNRANKED / UNCOMMITTED / NOT_A_CLAIM` |
| `ORTH-RR-D10` | Thermodynamic cost of gated versus admitted records | Resource closure could distinguish a hidden scheduler from a redescribed local-clock model | Compute work/entropy production per global opportunity, gated miss, durable record, and copied message in one explicit stochastic clock | Landauer language without a declared erasure protocol or native transition generator is bookkeeping | Sharpens `HET-D13`, `COM-D4`, and `COM-D6` | `UNRANKED / UNCOMMITTED / NOT_A_CLAIM` |
