---
title: "ECR-N5-S2 matter-flow metastable write selector"
status: completed
doc_type: run_plan
created: 2026-07-26
run_id: RUN-20260726-dynamic-unity-matter-flow-metastable-write-selector
campaign_id: ECR-N5
swing_id: ECR-N5-S2
mode: execute
lanes:
  - 1
  - 3
  - 4
  - A
channels:
  - CH-FORMAL
  - CH-SYN
  - CH-COLLIDE
starting_revision: 5f3937da089b
completed: 2026-07-26
---

# ECR-N5-S2 matter-flow metastable write selector

## Objective

Test whether one target-independent matter-coupled metastable generator
selects its blank-to-token record architecture up to physical gauge/orbit.
If the stable value does not retain occurrence provenance, return the exact
tokenization obstruction and the minimum added physical premise instead of
choosing a detector by hand.

## Frozen arena

- one finite continuous-time source--ready--record generator;
- source, carrier, complete generator, graph locality, allowed perturbations,
  representation group, write/reset edges, finite horizon, and provenance
  question fixed before any held-out target;
- endpoint value, path occurrence, retained record, redundant copies, and
  certified provenance typed separately;
- environment-selected pointer records, spectrum broadcast structure, and
  redundant records of consistent histories used as mandatory absorbers; and
- no held-out clock, geometry, field, or capability target used to define the
  interface.

## Exact questions

1. Does the weighted generator select ready states, retained phases, write
   edges, reset edges, and persistence bounds up to its automorphism group?
2. Is the resulting architecture stable under a declared timescale-preserving
   perturbation class?
3. Does the terminal metastable value determine whether a write occurred in
   the current physical occurrence?
4. Can redundant copies of that terminal value repair any provenance failure?
5. What is the minimum physically retained epoch/history distinction needed
   for a positive provenance claim?

## Local-model and hardware disposition

`DIRECT_EXACT_ANALYSIS`.

The finite generator is an analytic normal form, not a simulation program.
No local model, parameter sweep, provider adapter, or hardware action is
admitted. A computation may be used only to validate an exact algebraic
boundary unavailable at equal grade by hand.

## Evidence ceiling

A scoped finite selector/orbit theorem and tokenization obstruction built
from known Markov, factorization, metastability, and environment-record
mathematics. No microscopic matter derivation, universal record law,
actualization rule, ontology, prediction, or paper promotion.

## Stop conditions

Stop or downgrade if:

- blank, value, write, provenance, or reset is credited as selected merely
  because its semantic label was supplied;
- an unobserved trajectory is called a retained record;
- terminal-value redundancy is allowed to stand in for an epoch/history
  record;
- a monitoring choice is treated as gauge when it changes the physical
  environment access route;
- a fitted target determines the interface; or
- the same insight requires no model beyond direct exact analysis.

## Planned outputs

- one exact finite selector/orbit and provenance-obstruction result;
- a primary-source collision against metastability, unravelling, SBS, and
  redundant-history work;
- the minimum successor contract for `ECR-N5-S3`;
- concise orientation, Lane, concept, and campaign updates; and
- this Run Plan with a closing receipt.

## Receipt

Completed as `HC-DU-043`.

One exact alternating-rate four-state generator selects, relative to a
frozen typed source--carrier antecedent, the ready and retained classes,
write and reset edge orbits, finite write/retention bounds, and a two-value
record orbit up to \(\mathbb Z/2\). The architecture persists over the
declared perturbation class in which every fast exit rate exceeds every slow
one.

The terminal value is not an occurrence token. A no-jump path and a
four-jump return path can have the same initial and terminal states while
differing on whether a write occurred. Any number of redundant endpoint
copies preserves this factorization failure. The minimum bounded repair is
one physically retained binary epoch distinction plus a certified reset
boundary; exact unbounded occurrence identity still requires growing
support, bounded semantics, or allowed aliasing.

Quantum Darwinism/SBS absorbs value objectivity. Redundant records of
consistent histories identify the stronger positive route without selecting
the physical epoch archive. `ECR-N5-S3`, the matter--environment
provenance-lift and formation-descent test, is now the sole next executable
object.

No local model, simulation, hardware, provider, prediction, paper, Factory,
publication, submission, external-contact, or sibling-repository state
changed.

Result:
`../explorations/metastable-write-architecture-and-occurrence-tokenization-obstruction-2026-07-26.md`.
